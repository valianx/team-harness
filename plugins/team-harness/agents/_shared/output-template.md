# Useful specialist results

## Output Discipline

Return the information the coordinator needs to assess the task: outcome,
changed files or concrete findings, verification performed, coverage limits and
remaining decisions. Use the native response format unless a consuming tool
requires a specific data format.

Link existing evidence instead of duplicating raw logs. A missing check or
unavailable result stays explicit. Recommendations are advisory; the coordinator
has the wider context and decides the next step.
