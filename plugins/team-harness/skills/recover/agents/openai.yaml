interface:
  display_name: "Team Harness Recover"
  short_description: "Resume an explicitly active Team Harness pipeline"
  default_prompt: "Use $team-harness:recover for this request."
policy:
  allow_implicit_invocation: false
