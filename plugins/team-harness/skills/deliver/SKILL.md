---
name: deliver
description: Complete the authorized delivery of an implemented and validated objective.
---

Handle $ARGUMENTS in the current general agent.

Resolve the intended task and current candidate. Reuse implementation, checks and
review evidence where applicable. Use create-pr for PR preparation and publication,
including archive readiness and durable-file scope. Continue other requested
delivery steps with existing authorization and native permissions.

Use Codex-native tools and available agents. Read TH preferences from
`${CODEX_HOME:-$HOME/.codex}/.team-harness.json` only when relevant; absence
uses ordinary defaults. Native permissions and user settings remain in effect.
