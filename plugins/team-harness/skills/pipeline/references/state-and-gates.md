# State and workflow decisions

Use Markdown plans, tasks and useful evidence in the selected workspace. Main
keeps progress coherent; specialists write assigned artifacts. There is no new
TH state machine, lease, nonce or result-envelope requirement. Checkpoints
resolve missing decisions and reuse existing authorization. Native runtime
permissions govern execution. Old v5 logs are compatibility evidence only.
