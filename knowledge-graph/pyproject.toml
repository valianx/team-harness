[project]
name = "knowledge-graph"
version = "1.0.0"
description = "Knowledge Graph MCP server for cross-project memory (current backend: ChromaDB)"
requires-python = ">=3.10,<3.14"
dependencies = [
    "chromadb>=1.0.0",
    "mcp>=1.0.0",
    "uvicorn>=0.20.0",
]
