> Editing mandate for every task below: rewrite affected sections whole (coherent order for the full text), never append patch paragraphs, net word count equal or lower per touched file. Suites anchoring on changed literals migrate in the same task. Runtime projections regenerate in the phase that changes their source.
>
> Projection caveat: `plugins/team-harness/skills/{pipeline,design,implement,validate,deliver,recover,setup,update,init,modes}/**` are hand-authored Codex contracts excluded from `sync-skills.mjs`, and `runtime/codex/instructions/*.md` is the hand-authored source of the generated agent TOMLs. They do not regenerate — a contract rewritten in `agents/**` or `skills/**` is mirrored there by hand in the same phase, then the generated projections are refreshed and committed.

## 1. Convergence Contract

- [x] 1.1 Add the class-sweep obligation and mandatory Coverage Declaration to `agents/qa.md`, `agents/adversary.md`, and `agents/security.md`: forming any finding obliges enumerating every same-class instance within the lens's declared scope in the same pass (one finding per root cause covering all sites); every report ends with files/areas read, areas not examined, and known-unswept classes. Sweep obligations are a floor, never a ceiling.
- [x] 1.2 Give the ledger a transport in the same three return protocols: each `finding_summary` entry gains `id`, `severity` (`critical | high | medium | low | info`), and `class`, plus `classification` on a re-review. Today only `agents/security.md` has an ID and severity vocabulary, so the ratchet's termination predicate would otherwise rest on the coordinator reading severity out of `qa`/`adversary` prose. Reconcile `agents/implementer.md`'s `finding_resolutions.finding_id` and the `correction_findings: [{stable finding id}]` schema in `agents/_shared/orchestrator-state.md` against the same identity.
- [x] 1.3 Define `reviews/findings-ledger.md` in `agents/_shared/orchestrator-state.md`: append-only, coordinator-sole-writer, one row per finding ID (class, severity, disposition `fixed | accepted-residual | open | rejected-with-rationale`, operator rulings and waivers). Coordinator appends from fan status blocks and operator dispositions.
- [x] 1.4 Bind re-reviews to the ledger: every re-review dispatch reads it and classifies each reported finding as `new_in_delta | pre_existing_missed | reopened`; an accepted-residual or operator-ruled entry may not be re-raised without new evidence of a different root cause. Update `agents/_shared/output-template.md` so prior-round review content is preserved via the ledger, not destroyed with the overwritten report.
- [x] 1.5 Rewrite the iteration/correction rules in `agents/ref-pipeline.md`: ratchet termination (a re-review round with zero open critical/high findings and green deterministic conjunctions is convergence-complete; sub-floor findings — including correctable sub-floor security findings that are neither a `broke-it` nor an incomplete-changed-control condition — record as ledger residuals and PR-body concerns, never opening a round), and reclassify round-N findings on unchanged surface in a first-fan-flagged class as `pre_existing_missed` coverage defects rather than "genuinely new evidence". Rewrite sites named: the Phase-3 advance conjunct (`phase3_combined`/`code_hygiene`/"no correctable security finding"), the acceptance-check `code_hygiene` re-assertion, and the Gate-3 preserved-concern semantics in `agents/_shared/gate-contract.md` (ledger residuals are excluded from exception-pause concerns).
- [x] 1.6 Keep the two fail-closed security conditions intact while rewriting: the combined-verdict mapping `broke-it (correctable within approved scope) → fail`, the `fail` predicate that names them as the explicit exception to the QA severity floor, and Gate-3 exception class B all survive unchanged, and neither condition is ever recordable as a ledger residual.
- [x] 1.7 Reconcile every prose carrier of the correctable-must-correct rule in the same PR, so no file still states it unconditionally: `docs/patch-mode.md` (routing table + §4 + §5), `docs/how-it-works.md`, `docs/adversary-cost-model.md`, `docs/observability.md` correction-routing list, `agents/ref-special-flows.md` security-flow invariants, `skills/pipelines/SKILL.md`, and the CLAUDE.md validation-security-floor bullet. Grep the behavioral token, not only the canonical declaration, and assert absence at the old wording where a suite anchors on it.
- [x] 1.8 Emit `new_in_delta` / `pre_existing_missed` / `reopened` counts in `iteration.start` events as one `convergence_counts` field so convergence becomes measurable. The field-level contract has two carriers, both edited: the event `### Schema` table in `agents/_shared/orchestrator-state.md` (a new conditional row beside the `correction_*` rows) and the `iteration.start` producer contract in `docs/observability.md`. Migrate the deterministic suites anchored on iteration/event literals, `tests/test_pipeline_contract.py` included.

## 2. Single-Pass Design

- [x] 2.1 Extend `skills/pipeline/scripts/openspec-overlay.mjs` to derive the overlay skeleton (traceability coordinates and hashes, Gate-1 index scaffold, execution-shard scaffolds) mechanically from the validated change and snapshot.
- [x] 2.2 Rewrite the Design-transaction section of `agents/ref-pipeline.md` and the openspec modes of `agents/architect.md`: one planning dispatch authors change + judgment content (routing, scope decomposition, invariants); the overlay dispatch is retired; validator failure re-dispatches the same planning flow. The same retirement lands in every other carrier: the Codex progress-transport paragraph in `agents/ref-pipeline.md` that names the overlay architect, `docs/openspec-integration.md` steps 4 and 6, the hand-authored `runtime/codex/instructions/architect.md` with the regenerated `.codex/agents/{architect,pipeline-architect}.toml`, and the hand-authored `plugins/team-harness/skills/pipeline/references/design.md`.
- [x] 2.3 Keep `plan-contract.mjs` and the openspec validator chain unchanged as the fail-closed backstop. Reconcile the recovery consumer: `skills/pipeline/scripts/openspec-recovery.mjs` keeps the `overlay` pass value but its resume path now means re-running the derivation, not re-dispatching an architect, and the coordinator-facing recovery prose says so. Migrate the suites anchored on the two-pass transaction: `tests/test_openspec_overlay.mjs`, `tests/test_openspec_design_e2e.mjs`, `tests/test_openspec_recovery.mjs`, `tests/test_pipeline_contract.py`, and `tools/codex-runtime/test_generate.mjs`.

## 3. PR-Review Drift Tolerance

- [x] 3.1 Edit `skills/review-pr/scripts/review_context.py` `compare_contexts`: invalidation keys on `head_oid`/`commits`/`code_hash` only; mergeability and conversation drift become informational fields; `base_oid`/`merge_base_oid` movement keeps downgrading the verdict line. Edit `finalize_hashes` in the same task so `context_hash` no longer composes `mergeability`, and bump `SCHEMA_VERSION` with it — that hash binds artifacts, dispatches and the operator's publish approval in `skills/review-pr/SKILL.md`, so a `compare_contexts`-only edit would leave CI drift breaking the approval binding.
- [x] 3.2 Edit `skills/review-pr/SKILL.md`: drift reports as a preview line instead of restarting Gather and lenses; cap the pre-publish freshness restart at 1 (parity with pre-dispatch); on a second drift, surface to the operator.
- [x] 3.3 Edit `review_context.py` `classify_security_change` and `command_select_security`: extend `NON_EXECUTABLE_SUFFIXES` with the closed config set `.json`/`.yaml`/`.yml`/`.toml`/`.ini`/`.cfg`/`.properties` (`.env` variants excluded), leave the sensitive-path, sensitive-filename and sensitive-token checks running first and unchanged, and make `security_required` a pure function of the resolved reason plus the trigger list so `explicit` and `tier-4` still force it and indeterminate no longer defaults to required. Surface the resolved reason in the preview line of `skills/review-pr/SKILL.md`. Migrate the review-context suites.

## 4. Direct Spec Lane

- [x] 4.1 Write `skills/spec/SKILL.md`: one sentence disambiguating the lane from Discover's spec co-authoring (spec-seed) flow; coordinator-authored proposal+tasks (design/spec deltas only when a specced capability is touched), `openspec validate --strict`, one conversational approval turn, inline implementation on a feature branch with monotonic checkoffs, at most one optional live-requested full-scope review, PR via existing conventions, archive after merge; zero specialist dispatches and no pipeline artifacts.
- [x] 4.2 State the routing predicate and escalation rule in the skill and in `agents/orchestrator.md` / `agents/ref-direct-modes.md`: plain inline for mechanical reversible work; `/th:spec` for single-repo tasks worth written intent (~≤1 day, no security floor, no public-contract break); hard routers to `/th:pipeline` for security-sensitive, multi-specialist, or irreversible work; the lane stops and offers the pipeline when scope grows a second specialist need or a security dimension. Carry the same posture text into `docs/pipeline-lanes.md` and the CLAUDE.md two-posture bullet. If the live escalation guidance offers the lane as a third option, `skills/setup/managed-blocks/orchestrator-dispatch-rule.md` is edited too and re-applied by `/th:update` — it is the block installed into the operator's own instructions, so a guidance change made only in `agents/orchestrator.md` never reaches a live session.
- [x] 4.3 Update `docs/openspec-integration.md` for the two entry points and regenerate runtime projections.

## 5. Archive Lifecycle

- [x] 5.1 Wire `openspec archive <change>` behind a one-line Y/n at the confirmed-merge point of both flows: the terminal-close section of `agents/_shared/orchestrator-state.md` plus the matching `complete` prose in `agents/ref-pipeline.md`, and the spec lane's post-merge step. Publish-only delivery ends at a draft PR, so terminal close offers the archive only when the merge is already confirmed there and otherwise records a pending, re-offerable entry in the close record. Never silent; a declined or deferred offer never blocks close.
- [x] 5.2 Backfill-archive `integrate-openspec-design` and `pipeline-audit-improvements` to create the initial `openspec/specs/` base (one-time chore, operator-confirmed). The archive mutation ships on its own branch and pull request under the repository's ordinary conventions, never on the run's own PR and never as a direct default-branch push.

## 6. Verification and Release

- [x] 6.1 Run the full free verification and behavioral suites plus the projection suites (`sync-skills.mjs --check`, `generate.mjs --check`, `test_generate.mjs`, `tests/test_codex_runtime.py`) and commit every regenerated projection under `plugins/team-harness/**`, `installer-assets/opencode-skills/**`, and `.codex/agents/*.toml`. Register any new suite in the `docs/testing.md` inventory and its runner, renumbering by script if main gained suites mid-run. Record suite evidence against the final tree, re-running after any later commit.
- [x] 6.2 Apply version and changelog updates per the internal distribution rule.
