## 1. Shared preparation and provider ownership

- [x] 1.1 Extend shared provider policy/reference with Semgrep CE, dependency-cruiser, Knip and the selected Sentry method: applicability, official lifecycle, compatible prerequisites, active-host entry and output configuration; keep arc42/ATAM as references.
- [x] 1.2 Connect consuming workflow entry/resumption and active-host setup/update to that preparation, proving missing, healthy and unusable states without reinstalling unrelated tools or changing native permission behavior.

## 2. Diagnosis and review workflows

- [x] 2.1 Refine audit and its maintained reference to trace components, interactions and quality scenarios to evidence, findings and explicit coverage limits.
- [x] 2.2 Add the canonical find-bugs skill for project/module diagnosis without a diff, contextual investigation of analyzer candidates and related occurrences within scope.
- [x] 2.3 Integrate selected external evidence and the installed upstream review method into the existing PR snapshot, independent assessment, finding disposition and verification path without adding a parallel review or verdict gate.
- [x] 2.4 Document tool selection and native workflow discovery, preserving local/Obsidian workspace destinations and durable-artifact boundaries across Codex, Claude Code and OpenCode; regenerate distributed copies.

## 3. Evidence and delivery

- [x] 3.1 Verify generation, reference resolution, package boundaries and relevant snapshot/executable invariants with existing suites, adding behavioral fixtures only where changed executable behavior needs them.
- [x] 3.2 Execute a bounded pilot on fixed historical cases and small representative analyzer fixtures, then run audit and find-bugs on completed TH and check integration of components added in recent commits; retain cases, detection, misses, false positives, costs and limits in the workspace outside the PR.
- [x] 3.3 Execute OpenSpec implementation verification, applicable Superpowers verification and TEA test-review/trace; obtain independent advice, disposition findings and verify justified corrections against the final candidate.
- [x] 3.4 Synchronize living specifications and archive the completed change with implementation, inspect PR contents for transient work files and prepare the single implementation PR through create-pr.
