## 1. Simplify Design and operator planning

- [x] 1.1 Make pipeline Design reuse an existing strict-valid OpenSpec change without architect dispatch and invoke one architect only for missing or operator-requested semantic authorship.
- [x] 1.2 Generate compact read-only `01-plan.md` from canonical OpenSpec with outcome, scope, approach, work batches, risks, decisions, preserved behavior, links, and pinned identity; reject stale projections and manual semantic edits.
- [x] 1.3 Remove semantic overlays, exhaustive pre-Gate execution contracts, permanent future task capsules, and duplicated acceptance prose from the current Design path.
- [x] 1.4 Retire `qa-plan` from canonical and packaged agents, rosters, generated/runtime assets, define-AC routing, plan-review routing, documentation, lint/security inventories, and current tests; route AC authoring through OpenSpec and explicit plan review through the surviving reviewer.

## 2. Build the v5 control-plane core

- [x] 2.1 Implement closed `capability_lease` and `result_envelope` schemas with canonical hashing, bounded diagnostics, provenance, path containment, lifecycle, and replay validation.
- [x] 2.2 Implement atomic append/replay for the sole coordinator-owned control log and derive state, Gate, finding, acceptance, and operator-plan projections without duplicate authority.
- [x] 2.3 Add focused regressions for forged records, interruption, sequence/hash gaps, unsafe paths, symlink escape, secret-shaped content, duplicate results, and stale projections.

## 3. Derive dispatch and recovery just in time

- [x] 3.1 Derive one minimal lease immediately before each dependency-ready coherent worktree batch, binding current authority, ownership, pinned OpenSpec inputs, writable scope, and required verification without a complete future dispatch graph.
- [x] 3.2 Batch compatible same-owner tasks in one worktree lease, serialize committing ownership, and preserve read-only concurrency across immutable evidence.
- [x] 3.3 Accept one terminal result envelope through native transport, reuse valid same-agent context, reduce liveness to facts, and route continuation, replacement, or pause only from changed causal evidence.

## 4. Make validation and quality risk-derived

- [x] 4.1 Implement the closed independent-test predicate for bug reproduction, migration/data safety, public compatibility, security-control change, stale independent evidence, and explicit operator request; skip separate tester dispatch otherwise.
- [x] 4.2 Make one fresh independent verifier own ordinary Freeze evidence audit and semantic OpenSpec acceptance, without a duplicate QA verdict; add fresh security only when impact is true or unknown.
- [x] 4.3 Run cleaner only for a non-empty deterministic behavior-preserving allowlist and run the complete quality set exactly once per candidate tree at Freeze.

## 5. Convert, project, and prove the reduction

- [x] 5.1 Implement create-then-switch v1-v4 to v5 conversion with precise binding diagnostics, verified continuation authority, mixed-schema refusal, and rollback-safe current readers.
- [x] 5.2 Update canonical pipeline/agent contracts, OpenSpec integration, surviving role adapters, packaged helpers, Codex TOMLs, opencode assets, documentation, and changelog; regenerate all projections from canonical sources.
- [x] 5.3 Add focused, full, behavioral, security, strict OpenSpec, generated-freshness, and diff checks covering retained Gate, ownership, Freeze, security, native-permission, and outward-action floors.
- [x] 5.4 Benchmark small-fix, medium-feature, and security-sensitive fixtures for time to Gate 1, agent attempts, tool calls, corrections, final quality, and exclusive defects found per lens; require no architect for an existing valid change, no `qa-plan`, no empty cleaner, and no duplicate complete quality run.
