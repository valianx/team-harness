## 1. Record the smaller workflow contract

- [x] 1.1 Add this bounded proposal and task list with no more than twelve requirements.
- [x] 1.2 Add the native-agent workflow discovery delta for `spec`, `pipeline`, `review-pr`, `create-pr` and `modes`.

## 2. Reconcile living workflow specs

- [x] 2.1 Remove current requirements for TH-owned permission hooks, nonces, leases, control logs and mechanical gates.
- [x] 2.2 Keep OpenSpec canonical for intent and make specialist and review returns advisory to Main.
- [x] 2.3 Keep archive, PR and native permission behavior explicit without adding another approval layer.

## 3. Reconcile native runtime behavior

- [x] 3.1 Update Codex parity and update/reload requirements so operator settings remain native-owned.
- [x] 3.2 Keep restart reporting conditional on an observed native requirement and preserve the no-restart fast path.
- [x] 3.3 Keep PR comment handling self-contained and advisory, and resolve shared references at their installed runtime paths.

## 3a. Preserve workflow capabilities in skills

- [x] 3a.1 Restore documentation, testing and spike methods in self-contained skills, including current inputs and temporary experiment cleanup.
- [x] 3a.2 Keep a resumable shared plan and dependency/consumer checks for multi-repository spec work, and align optional validation helpers with their callers.
- [x] 3a.3 Restore advisory skill comparison and update authoring guidance so workflows live in skills across all three runtimes.
- [ ] 3a.4 Exercise representative requests and obtain independent capability-preservation review of the integrated candidate.

## 4. Validate and close

- [x] 4.1 Run strict OpenSpec validation for this change and the affected living specs.
- [x] 4.2 Run the focused OpenSpec scope and workflow tests after Main integrates generated assets and CI consumers.
- [x] 4.3 Rebuild generated projections and verify Claude Code, Codex and OpenCode discovery surfaces.
- [x] 4.4 Prepare the archive candidate and update living-spec links when Main completes validation and delivery evidence.
