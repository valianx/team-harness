## Context

TH provides workflow guidance to the native general agent. The operator approved the six-front audit cleanup in one PR.

## Decisions

Use current task authorization and native delegation. Keep a shared local or Obsidian workspace, written intent, proportionate checks and independent advisory review. Preserve executable compatibility readers and file integrity protections where they still have consumers; old records do not authorize new work. Installation changes preserve unrelated settings.

## Validation

Exercise changed helpers and installers behaviorally, regenerate distributions, run repository checks, and review the final candidate independently. Archive this completed change with its implementation.

