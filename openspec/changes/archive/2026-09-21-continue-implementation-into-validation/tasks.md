## 1. Consecutive phases

- [x] 1.1 Make the shared method and implement entries continue directly into applicable validation, retaining distinct phase reporting, explicit stop points, existing workspace and publication scope.
- [x] 1.2 Synchronize the native projections and preserve the earlier archived change while representing the new behavior in this separate delta.

## 2. Verification

- [x] 2.1 Apply the bounded TEA testing strategy to continuation, explicit stop, missing decision and host projection scenarios; retain actual provider assessments and limitations in the workspace.
- [x] 2.2 Run affected package, synchronization, reference and OpenSpec checks; execute completion verification and resolve concrete inconsistencies before archive and PR update.
