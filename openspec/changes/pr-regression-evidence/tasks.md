## 1. Bounded comparison evidence

- [x] 1.1 Add a review helper that prepares disposable merge-base/head execution copies from captured Git identities and records an explicitly selected identical external probe, argv command, limits and prerequisites without modifying review inputs or implicitly installing dependencies.
- [x] 1.2 Reuse permitted bounded execution primitives to capture per-side assertion outcomes and diagnostics; classify comparable results, report unavailable or ambiguous execution as inconclusive, and validate evidence identity and integrity before reuse.

## 2. Review integration

- [x] 2.1 Add opt-in regression investigation to the canonical review skill, including hypothesis selection, coordinator-owned execution, evidence coordinates, freshness handling and coverage disclosure in the existing review output.
- [x] 2.2 Extend the existing verifier contract and Codex adapter to inspect optional validated evidence alongside code, preserving read-only execution restrictions, independent causality judgment and existing finding dispositions.
- [x] 2.3 Require verified autonomous operational repair across direct/spec and pipeline contracts, preserving deliverables, acceptance and native authority; diagnose existing failures before reporting a blocker.

## 3. Validation and single-PR delivery

- [x] 3.1 Add fixture-based behavioral tests for introduced and preexisting assertion failures, successful head probes, intentional changes, unavailable or timed-out execution, stale or modified evidence, bounded diagnostics and preservation of ordinary code review.
- [x] 3.2 Update usage documentation, synchronize generated distribution and Codex agent artifacts, and add the required version bump and changelog entry.
- [x] 3.3 Run strict OpenSpec validation, scope checks, relevant focused suites, Codex generator checks and the complete repository suite; resolve failures and record evidence against the final committed change.
- [ ] 3.4 Complete the spec lane's required anchored prepublication checks, address actionable findings, and publish one PR containing the specification, implementation, tests and release metadata.
