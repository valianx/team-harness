schemaVersion: "1"
body: hooks/ts/bodies/notify-stage.ts
kind: hook
runtime: claude-code
emit:
  path: hooks/ts/entry/notify-stage.cc.ts
  frontmatterDialect: none
# Notification/idle_prompt and pipeline stage calls are observational only.
events: {}
ioContract: normalized-v1
capabilities:
  requires:
    - node
  degradeIfMissing: []
# Profile gate: idle-notify class.
# OS detection: windows/mac/linux → native notification sender.
notes: >
  notify-stage is fail-open and profile-controlled. It never emits a
  permissionDecision or changes execution permissions.
