schemaVersion: "1"
body: hooks/ts/bodies/notify-stage.ts
kind: hook
runtime: opencode
emit:
  path: hooks/ts/entry/notify-stage.opencode.ts  # Stub — orchestrator-invoked, not a plugin event
  frontmatterDialect: none
# SPECIAL: notify-stage is NOT a plugin tool:before hook — it is orchestrator-invoked.
# opencode does not expose a stage-boundary notification event in its plugin model.
events: {}
ioContract: normalized-v1
capabilities:
  requires:
    - bun
  degradeIfMissing:
    - notification:stage  # opencode does not expose stage notifications; fail-open
notes: >
  notify-stage on opencode: no equivalent event exists in the opencode plugin model.
  The body is shipped for completeness; the orchestrator would invoke it directly
  (via bun run) if a future opencode version exposes a stage-notification event.
  NEVER blocks. FAIL-OPEN.
