// hooks/ts/entry/notify-stage.opencode.ts
// STUB — notify-stage has no equivalent opencode plugin event.
// notify-stage is orchestrator-invoked; opencode does not expose a stage-boundary
// notification event in its plugin model.
//
// This file exists so the adapter descriptor can reference an emit path.
// The opencode-plugin.ts factory does NOT register notify-stage as a tool:before hook.

export {};
