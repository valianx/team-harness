---
name: th-reload
description: Activate the installed Team Harness version in this OpenCode conversation and report any remaining reconnect requirement.
---

Load the native `reload` skill and execute its OpenCode procedure for this
conversation. Preserve the existing session and distinguish installed resources
from components verified in the active backend. This command does not install
updates or start another server.
