### Changed

- **SEC-DR-2 re-founding (v2.89.0):** The orchestrator disposition is now the CC native architecture — the general top-level agent IS the orchestrator. No filesystem marker (`~/.claude/.dev-mode-active`) is required. The disposition directive fires unconditionally at every session start via `hooks/session-start.sh` (`load_orchestrator`, replaces the marker-gated `load_dev_mode`).
- **`hooks/dev-guard.sh` unconditional gate:** The outward-action gate (git push, gh pr merge/review/comment, GitHub API writes, ClickUp MCP writes) is now armed unconditionally — no marker is read. Covered actions always produce `ask`; non-covered actions always produce `nodecision`. The `--dev-mode-active` marker and all marker-reading logic are removed.
- **Managed blocks retired:** `skills/setup/managed-blocks/dev-mode.md` and `skills/setup/managed-blocks/nested-dispatch-takeover.md` are deleted. `/th:update` now removes these blocks from existing user `~/.claude/CLAUDE.md` installations. Only `orchestrator-dispatch-rule.md` and `voice-rule.md` remain active.
- **`/dev-mode` skill retired:** `skills/dev-mode/` directory is deleted. The disposition is unconditional — no in-session skill is needed to activate it.
- **D3 empirical probe (M1):** Confirmed nested CC foreground subagents retain `Task`. The `dispatch_handoff`/takeover machinery is retained for opencode/legacy compatibility but is scoped to that path in documentation.
- **`output-styles/developer-mode.md`:** Banner updated to "TEAM HARNESS ORCHESTRATOR". Observable-flag section rewritten to unconditional framing (no marker reference). Added "This output style is optional" section.
- **`docs/dev-mode.md`:** Rewritten as "Orchestrator Disposition — Contract". Removed marker-gated activation model; gate table updated for unconditional framing.
- **Test suites updated:** Suite 83 (`test_dev_guard.sh`), Suite 84 (`test_isolated_hook_env.sh`), Suite 85 (`test_hook_gates_hardening.sh`), and Suite 59 assertions in `test_agent_structure.py` all reflect the unconditional gate model.
