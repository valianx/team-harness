### Added
- `agents/review-lenses/loosening-impact.md` (2.B): reviewer lens that fires on deleted/loosened guards, gates, validators, or error handlers — traces consumers, checks whether non-execution was the safety property, and links to the finding-connection cross-check; raises findings only on real downstream impact; zero findings for genuine dead code; additive and never overrides the deterministic bright-line (AC-4)
- `agents/_shared/apply-review-disposition.md` (2.A): author-side conservative disposition — automatically loaded by the orchestrator when it works on a PR carrying reviewer comments; two-axis classification (Nature × Severity), mandatory evidence-backed verification filter for CHANGE comments that delete/loosen, deletion discipline (positive justification required; "never executes" ≠ dead code), per-comment output; performance gate scopes the heavy filter to deleting/loosening changes only
- `agents/_shared/finding-connection.md`: shared single-source cross-check capability — given a change widening path P, scans the other findings/comments in the same review for a declared risk on P or its downstream and forces them to be resolved together; referenced by both 2.A (Step 2.4) and 2.B (point c) so neither side can diverge
