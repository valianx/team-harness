### Added

- `agents/testing-refs/browser-mode.md`: new on-demand reference for Vitest Browser Mode — when to use (layout, observers, media queries, real CSS, animations, viewport-conditional reflow, and browser APIs outside those families — catch-all with per-API verify clause) vs when not to use (full user journey → Playwright E2E; no browser-API dependency → jsdom). Covers setup (`vitest` + `@vitest/browser-playwright` lockstep 4.1.8), `vitest-browser-react` 2.2.0 for React targets, Viewport control subsection (`import { page } from 'vitest/browser'`; `page.viewport(375, 667)`), visual regression (`toMatchScreenshot`, `__screenshots__/` storage, platform-suffix caveat: baseline filenames embed browser + OS; macOS/Windows baselines fail on Linux CI → generate in pinned CI env), Playwright-format traces (`trace: 'retain-on-failure'`), ARIA live-region assertion subsection (DOM state ordering; axe as complement), per-file isolation limitation, and `vi.mock`/`vi.spyOn` caveat.
- `agents/tester.md`: Phase-0 browser-test routing rule — step-3b warranted-type derivation bullet closed to enumerated families plus catch-all for browser APIs outside those families (clipboard, geolocation, fullscreen, notifications, etc. → `browser-mode`; verify API availability under Playwright provider; log per-API decision). First-match precedence statement added: running application + viewport vocabulary → `e2e` still wins (backtick-quoted type token in precedence line). Focus+announcement split example (aria-live → `a11y`; document.activeElement → `browser-mode`). Conventions-vs-reference precedence sentence: discovered repo framework wins (`cypress.config` detection example). `## Test-Type Decisions` table section added to the 03-testing.md template (decision-log landing artifact, auditable from pipeline). `warranted_types:` field added to Return Protocol status block. Binaries-missing clause in the Neither-installed paragraph (packages-present-but-binaries-missing → propose `npx playwright install --with-deps`). n/a re-derive disposition in Router fallback (marked n/a for stack → re-derive via Phase 0 step 3b). Count-free phrasing: "all type files" (not "all six type files").
- `agents/testing-refs/_index.md`: manifest row for `browser-mode` (stacks covered: react-nextjs; n/a: nestjs, go, python). Frontend AC category → test type mapping table: keyboard-nav/focus-order row split out of the a11y row (→ `ui-component` or `browser-mode`; axe cannot verify keyboard operability), catch-all browser-API row (clipboard, geolocation, fullscreen, notifications → `browser-mode`). Engine-overlap note updated to include `a11y` No-Storybook path.
- `tests/test_agent_structure.py`: Suite 92 (`browser-mode-reference`) — 68 checks (54 existing strengthened + 14 new). Existing strengthened: C8 direction-pinned 7f (backtick-quoted `` `e2e` `` + 'still wins' per-line), C8 cell-anchored 7g (backtick-quoted `` `a11y` `` as type cell), C8 arrow-pinned 4f ('AC about layout/geometry' + '→' + `` `browser-mode` `` per-line), C9 7a repointed to `__screenshots__/` storage path, C10 1c filename-matched (stem+".md" not bare stem), C11 7e comment added (known-limitation: file-scoped; future jsdom comparative snippets would need allowlist), C12 4o slice tightened to stop at next `###`. New checks (8a–8n): `page.viewport` + `vitest/browser` import in browser-mode.md; platform-suffix caveat in browser-mode.md; platform-suffix caveat in visual.md; `### ARIA live-region assertion` subsection in browser-mode.md; `setViewportSize` in e2e.md; keyboard-nav row NOT routed to `` `a11y` `` per-line in _index.md; catch-all row ('clipboard' + `` `browser-mode` ``) per-line in _index.md; 'clipboard' in tester.md catch-all branch; 'cypress.config' conventions-precedence in tester.md; `## Test-Type Decisions` template section in tester.md; `warranted_types:` field in tester.md; 'binaries' clause in tester.md; 're-derive' in tester.md Router fallback; 'all type files' present and 'all six type files' absent in tester.md.

### Changed

- `agents/testing-refs/e2e.md`: `## react-nextjs` refreshed to Playwright 1.60 best practices — user-facing locators first (`getByRole`, `getByLabel`, `getByText`; `exact: true` is the opt-in for whole-string match, substring/case-insensitive is the default), web-first assertions, `### Responsive assertions in E2E` subsection (`devices['iPhone 13']`, `page.setViewportSize`), `v1.60.0-noble` CI image tag, `trace: 'on-first-retry'`, `storageState` auth pattern.
- `agents/testing-refs/visual.md`: Option C (Vitest Browser Mode component screenshots) added with `toMatchScreenshot`, `__screenshots__/` storage path, and platform-suffix caveat; corrected Playwright default snapshot location (`<spec>-snapshots/` adjacent to the spec file, not `tests/e2e/__snapshots__/`).
- `agents/testing-refs/a11y.md`: No-Storybook axe path added — `npm install -D axe-core` install line and `axe-core` import in the browser-mode test example.
- `agents/testing-refs/ui-component.md`: boundary note naming `browser-mode.md` and describing the story/raw-API distinction (bidirectional boundary).
