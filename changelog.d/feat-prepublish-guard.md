### Added
- `hooks/prepublish-guard.sh`: deterministic pre-publish gate (TH-native, fail-open-on-fault) with two command-routed checks — Check 1 gates `git push`: if `agents/`, `skills/`, or `hooks/` assets changed vs `origin/main` and neither `.claude-plugin/plugin.json` nor `.claude-plugin/marketplace.json` version was bumped, the push is blocked with a remedy line naming both files; Check 2 gates `gh pr create`: runs the operator-declared `prepublish_check` command from `~/.claude/.team-harness.json` and blocks the PR creation on non-zero exit; repos without plugin files or without a declared `prepublish_check` are unaffected no-ops; deny reason for Check 2 is JSON-encoded via `python3 json.dumps` (SDR-PPG-01); `MSYS_NO_PATHCONV=1` on all `git show` calls for Windows Git Bash compatibility; Suite 118 covers all 17 AC.
