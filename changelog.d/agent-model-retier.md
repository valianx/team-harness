### Changed

- Agent model re-tiering: `adversary` and `reviewer` move `opus → sonnet`, and `acceptance-checker` and `translator` move `haiku → sonnet`. Sonnet 5 at `xhigh` sits near Opus for secondary analysis/review and for rare-but-critical roles at a fraction of the cost, while `architect` and `security` stay on `opus` for the highest-stakes, non-recoverable analysis. The effort ceiling drops from `max` to `xhigh` (`max` retired — marginal gain over `xhigh` at a large cost premium), affecting `adversary`, `security`, `reviewer`, and `agent-builder`. On Claude Code `effort` is session-global, so the per-agent value is opencode-honored and advisory on CC; the model is the load-bearing per-agent lever there. The canonical Roster matrix in `agents/README.md`, its rationale, and `CLAUDE.md` §5 are updated to match; the vestigial low-cost matrix keeps its historical Standard columns behind a disclaimer.
