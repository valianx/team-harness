### Added
- `agents/_shared/gh-fallback.md`: STOP-on-access-failure contract under "Tier A — read a single PR". When the PR head cannot be resolved by `gh`, the curl fallback, or a local `git fetch origin {headRefName}`, the agent STOPs and surfaces `cannot reach PR — authenticate or paste the diff`; it never substitutes the checked-out branch or reviews the primary working tree.

### Changed
- `skills/review-pr/SKILL.md`: Phase 1 step 2 access-failure fallback hardened. The previous "prompt the operator to paste the PR diff manually" sentence is replaced with a hard STOP referencing the gh-fallback STOP-on-access-failure contract. Explicitly forbids checked-out-branch substitution; clarifies the local-diff path operates on `origin/{headRefName}`, never the operator's checkout.
- `agents/orchestrator.md`: Step 6a routing table `review PR` row marked as a **HARD trigger; never inline**. Disambiguation prose extended with explicit prohibition on improvised inline review, working-tree review, and checked-out-branch substitution; cross-references the gh-fallback STOP contract; states binding is prompt-level not a deterministic gate. Standalone-agents note reframed to hard-trigger language while retaining the honest native-selector residual-limit sentence.
- `skills/setup/managed-blocks/orchestrator-dispatch-rule.md`: New bolded paragraph establishes PR-review as a hard trigger for `/th:review-pr` at the earliest prompt surface the top-level agent reads. States the STOP-on-unresolvable-head requirement and notes the binding is prompt-level (strong defense-in-depth), not a deterministic gate.
