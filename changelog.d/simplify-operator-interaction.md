### Changed

- Let established direct modes, including the OpenSpec spec lane, start from unambiguous current live operator intent without requiring a literal slash command; gated-pipeline activation remains explicit-only.
- Made operator prompts accept concise semantic replies and complete natural-language amendments while preserving stable numeric shortcuts, current-presentation nonce binding, exact authority identities, and separate outward-action controls. Existing literal invocations, numeric selections, and prefixed `3:`/`4:` replies remain compatible.
