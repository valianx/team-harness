### Added
- `orchestrator.md`: `frontend_scope: true` propagation into Phase 2.7 (authoring) and Phase 3 (verify) tester dispatches with the apply-the-rule instruction; A1-F3 pre-Phase-3 browser-readiness check (binaries/tooling gate before Phase 3 launches); A1-F4 jsdom-only soft gate (Hot Context note when frontend task ends with no browser-real warranted type; type set includes `e2e`, `browser-mode`, `a11y`, `ui-component`, and `visual`).
- `agents/orchestrator.md` (Direct Modes `test` row): `frontend_scope` bridge — persists flag to `00-state.md § Current State` and threads instruction into tester invocation; see `ref-direct-modes.md § Test Mode` for full field contract.
- `agents/ref-direct-modes.md`: `## Test Mode` and `## Test-Pipeline Mode` sections documenting the `frontend_scope` field contract and bridge steps for `/th:test` and `/th:test-pipeline` direct-mode invocations.
- `agents/ref-special-flows.md` (Test Pipeline Flow, per-module payload): `frontend_scope: {true|false}` field threaded into each `module-test` dispatch; per-file browser-vs-jsdom routing instruction and Vue/Svelte `browser-mode: n/a` fallback documented.
- `agents/tester.md`: Gate-B responsive-intent detection step in Phase 0 (scans Tailwind responsive prefixes, `@media`, `matchMedia`, breakpoints, viewport meta, CSS-in-JS patterns, responsive AC; gates only the viewport/responsive branch); Gate-A label on Phase-0 step 2 (`Gate A — frontend stack detection`); Vue/Svelte n/a disposition (non-react stacks log gap, do not silently degrade); `TESTING.md` project-guide generation contract (R4) with stable machine sentinel `<!-- th:testing-guide v1 -->` and team-harness repo-root guard.
- `skills/test/SKILL.md`: `## Frontend detection` section; Direct Mode Task payload carries `frontend_scope: true` when frontend markers are found.
- `skills/test-pipeline/SKILL.md`: same `## Frontend detection` section and payload update as `/th:test`.
- `agents/ux-reviewer.md`: Browser-real signal hints block in enrich checklist; `Detect the frontend stack` step in enrich Process.
- `agents/qa-plan.md`: Browser-real signal hints block in define-ac frontend template.
- Suite 93 (34 checks) in `tests/test_agent_structure.py`: structural pins extended with 8 cross-file C7 and iteration-2 invariant checks (C7a–d: direct-mode bridge contract; iter2a–d: Gate-A label, TESTING.md sentinel, repo-root guard, Vue/Svelte n/a disposition).

### Changed
- `agents/ux-reviewer.md`: Stage-3 ownership-boundary paragraph added to validate mode — automated tests (Browser Mode viewport, Playwright/axe) are AUTHORITATIVE for the properties they assert; manual validation reserved for properties no automated test covers.
- `agents/testing-refs/browser-mode.md`: `### Where to look when it fails` artifacts section added (screenshots `__screenshots__/`, traces `__traces__/`, verbose output, repro command); A1-F4 browser-real set extended to include `ui-component` and `visual` (engine-overlap alignment); **iteration-3:** `userEvent` import path corrected from `@vitest/browser/context` to `vitest/browser` (new canonical path in Vitest Browser Mode; old path was a substring of the new — positive-only check was a false-green); `toMatchScreenshot` locator subject corrected from `expect(container)` to `expect(screen.getByRole(...))` (correct form targets a semantic query, not the raw container); `__traces__/` entry added to the file-layout block.
- `agents/testing-refs/visual.md`: **iteration-3:** `toMatchScreenshot` locator subject corrected from `expect(container)` to `expect(screen.getByRole(...))` (same fix as browser-mode.md).
- `agents/testing-refs/e2e.md`: `### Where to look when it fails` artifacts section added (HTML report `playwright-report/`, traces, screenshots, repro command; upload `playwright-report/` as CI artifact on `always()`).
- `agents/ref-direct-modes.md`: **iteration-3:** `## Test Mode` and `## Test-Pipeline Mode` sections now surface `A1-F3` (browser binaries readiness check) and `A1-F4` (jsdom-only soft gate) on the console path, so direct-mode invocations (`/th:test`, `/th:test-pipeline`) apply the same pre-run gates as the full pipeline.
- `agents/tester.md` Gate-B: CSS-in-JS and UI-library breakpoint patterns added to responsive-intent evidence list (styled-components/Emotion template literals, MUI `sx` responsive values, MUI `useMediaQuery`, Chakra responsive-array props); CSS-in-JS stack confirmation rule added (Gate-B-NEGATIVE requires explicit confirmation when stack uses CSS-in-JS/UI-library).
- `agents/tester.md` TESTING.md (R4): overwrite/redirect decision now driven by stable machine sentinel `<!-- th:testing-guide v1 -->` instead of undecidable 'unrecognizable as template' heuristic.
- `README.md`, `docs/subagent-orchestration.md`: **iteration-3:** `frontend-scope` code-span sweep corrected to `frontend_scope` (underscore-delimited form matches field name in payloads and status blocks; hyphenated form was a documentation inconsistency).
- `tests/test_agent_structure.py` Suite 92: **iteration-3:** check 8a strengthened with negative guard (`@vitest/browser/context` ABSENT) to close the false-green where the old import path satisfied the positive substring match; new `toMatchScreenshot-subject` negative check pins the locator-subject correction in both `browser-mode.md` and `visual.md`. Suite 93: **iteration-3:** new `iter3-rdm` check pins `A1-F3`/`A1-F4` presence in `agents/ref-direct-modes.md` `## Test Mode` section (anchor-scoped with wide stop at `## ` so `###` subsections are included).
