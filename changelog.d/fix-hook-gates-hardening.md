### Fixed

- `hooks/policy-block.sh`: added bash-native degraded floor (DENIED_BASH + SENSITIVE_PATHS + HIGH_CONFIDENCE_SECRETS) when python3 is absent; script previously exited 127 with no decision (F-002). Entropy scan remains python3-only (documented gap).
- `hooks/policy-block.sh`: normalise Windows backslash paths before SENSITIVE_PATHS matching so `C:\Users\x\.ssh\id_rsa` and similar paths are denied on both python3 and bash paths (F-015).
- `hooks/dev-guard.sh`: replace `[^"]*` extraction with bracket-form `[\\]` escape-aware regex so compound commands (`git commit -m "msg" && git push`) are fully extracted and the git-push is correctly gated (F-016).
- `hooks/dev-guard.sh` + `.claude-plugin/hooks.json` + `hooks/config.json`: add second PreToolUse entry (matcher `mcp__.+__clickup_(update_task|create_task|create_task_comment|attach_task_file)`) so ClickUp MCP outward writes are gated under the active dev-mode marker; server-segment wildcard does not hard-code the registration name (F-008). Fixed server-segment under-match (`[^_][^_]*` → `.+`) so multi-word server names normalised to underscores by Claude Code are correctly matched (SEC-001).
- `hooks/checkpoint-guard.sh`: resolve obsidian vault root from `~/.claude/.team-harness.json` so the gate fires in obsidian logs-mode; previously permanently fail-opened (F-010).
- `hooks/checkpoint-guard.sh`: replace alphabetical sort with portable `ls -t` mtime selection and terminal-status skip so the active workspace is selected instead of the alphabetically-first stale workspace (F-018).
- `hooks/checkpoint-guard.sh`: scope the obsidian search to the repo's own work-logs subtree (`{logs-path}/{logs-subfolder}/{repo}/`, maxdepth 3) and select the active workspace with a single newest-first `ls -t` then break at the first non-terminal candidate — instead of scanning the entire vault and status-checking every historical workspace (~5 subprocesses each). The vault-wide scan spawned hundreds of forks on a large vault and hung the Task dispatch on Windows Git Bash with no output; this restores O(1) selection in the common case while preserving the newest-non-terminal semantics (regression in the F-010 fix).
- `skills/recover/SKILL.md` + `agents/orchestrator.md`: tighten STAGE-GATE cleared predicate to a decision-allowlist (`approved`/`approved-autonomous` for gate-1, `next`/`next-autonomous` for gate-2, `ship` for gate-3); add `gate1_release`/`gate2_release_last`/`gate3_release` fields to the Current State schema and write each at its release handler (F-009).
- `hooks/sketch-guard.sh` (and all four invocation sites: `skills/deliver`, `skills/validate`, `skills/review-pr`, `agents/orchestrator.md`): replace repo-relative path with documented 3-tier resolution chain (plugin cache → `~/.claude/hooks/` → `./hooks/`); emit visible `sketch-guard probe unavailable — skipping` line when unresolvable instead of a silent `2>/dev/null` no-op (F-038).

### Added

- `hooks/_json-extract.sh`: shared escape-aware JSON field extractor (bracket-form `[\\]`) sourced by both `policy-block.sh` and `dev-guard.sh` via the same 3-tier resolution chain.
- `skills/lint/SKILL.md`: Check 8 — Hook runtime health: reports python3 absence (degraded floor advisory by name), wired-but-unresolvable hook scripts (`[FAIL] <hook> wired but not found on disk — gate is dead`), and obsidian-mode checkpoint coverage status.
- `skills/setup/SKILL.md` + `skills/update/SKILL.md`: guided python3 install block — when python3 is absent, recommend installing it (rationale: full secret/entropy scan coverage) and offer an explicit Y/n prompt; on Y, run the OS-appropriate command (`winget install -e --id Python.Python.3.12` / `brew install python3` / `apt-get|dnf|pacman`); re-probe after install with Windows PATH-refresh caveat; decline or failure falls back to degraded advisory and never blocks setup/update (A1, operator-directed amendment at STAGE-GATE-1).
- `tests/test_hook_gates_hardening.sh` + `tests/test_agent_structure_hardening.py`: Suite 85 — 57 SPEC-asserting regression tests covering all eight findings (19 shell behavioral + 38 Python structural); all were authored to FAIL on pre-fix main and PASS after (F-002, F-015, F-016, F-008, F-010, F-018, F-038, F-009, A1 guided install).

Fixes #304
