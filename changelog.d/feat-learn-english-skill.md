### Added

- `/th:learn-english` standalone toggle skill: `on` enables the english-learning correction mode and sets `language: en`; `off` disables corrections and interactively offers to keep or change the response language; `status` / no argument reports current state. All writes use merge-write-whole-document on `~/.claude/.team-harness.json`.
