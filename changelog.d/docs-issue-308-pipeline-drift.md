### Changed

- `docs/pipelines.md`: Added Discover front-end to the feature pipeline (intake survey, spec co-authoring / `00-spec-seed.md`, approach checkpoint, plan sketches + `sketch-guard` at STAGE-GATE-1); added Phases 1.7 (ux-enrich), 2.7 (test authoring), 3.75 (build verification) and Phase 4.5 / sanity-check rows to the feature phase table; corrected STAGE-GATE-2 to fire per-round (not per-PR); clarified PR opens at Phase 5 post-STAGE-GATE-3 (not during the Phase 4 commit step); replaced all stale artifact names (`01-architecture.md` + `02-task-list.md` → merged `01-plan.md`; `01-plan-review.md` → verdict appended to `01-plan.md § Plan Review`; `01-research.md` → `00-research.md`); rewrote Test pipeline section — the pipeline WRITES coverage config + test code in worktrees, iterates to an 80 % branch-coverage gate (max 3 loops), and outputs `05-consolidation.md` (was incorrectly described as read-only); split Plan flow into `/plan` (planning mode → `01-planning.md` → GitHub issues / `tasks/` files, distinct from `/design`) and `/design` (Stage-1 design → `01-plan.md`); added Milestone-Build Flow, parallel dispatch (plan-and-execute), and initiative fan-out sections.
- `docs/how-it-works.md`: Added Tier 0 (no workspaces) to the bug-fix tier table (was Tier 1–4 only); removed the retired Phase 1.6 "inline fallback" phrasing (plan-review is subagent-only; there is no degraded mode); expanded the agent roster to 28 agents (was 18; previously missing: `qa-plan`, `ux-reviewer`, `documenter`, `mentor`, `researcher`, `research-consolidator`, `code-researcher`, `adversary`, `gcp-infra`) — points to `agents/README.md` as the canonical roster; expanded the hooks list (was `policy-block.sh` only) to name `dev-guard.sh`, `checkpoint-guard.sh`, `sketch-guard.sh`, `worktree-guard.sh`, `prepublish-guard.sh` — points to `hooks/README.md`; added a dev-mode note (top-level-is-orchestrator, SEC-DR-2, `dev-guard.sh` outward-action gate); replaced stale artifact references (`01-architecture.md`, `02-task-list.md`) with `01-plan.md`.
