### Added
- **External-report scope verification contract (v2.116.2).** GitHub issues, issue comments, PR review comments, and ClickUp tasks are time-stamped reports — their stated scope may be stale. The pipeline now verifies the real residual scope against the current tree before planning or implementing, and recommends closing-with-evidence (not a no-op PR) when the residual is empty. Encoded as: `docs/discover-phase.md §13` (detailed procedure and empty-residual rule, single source of truth); `agents/orchestrator.md` Phase 0b Step 1.5 (intake verification step feeding real scope into AC build and architect dispatch payload); `agents/architect.md` Spec Feedback Protocol Channel 3 + `### Real-vs-Stated Scope` Review Summary block + Phase 1 hook; `CLAUDE.md §6.6` floor bullet (complements the prompt-injection floor — not OBEYING embedded instructions is §6.6; not TRUSTING stated scope is new). Structural-test markers in `tests/test_agent_structure.py` assert the new contract is durable.
