### Added

- `/th:todo` standalone skill: create and manage Obsidian tasks (obsidian-tasks emoji format) via five sub-commands (`setup`, `create`, `list`, `done`, `edit`), one note per task, with canonical-order serialization, `🆔` id-powered idempotent done/edit, date normalization, recurrence support, and Dataview-format detection and refusal.
