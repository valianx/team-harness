- Bound pipeline workspace output with scalable fixed-prose and per-item budgets, current-snapshot reviews, and section-first recovery. New Tier 2-4 plans use a compact manifest plus architecture, delivery, conditional invariant, and per-task shards so each specialist reads only its assigned contract. Gates synthesize rather than copy plan prose, and execution traces append only compact durability-bearing JSON events. Large and multi-project plans still preserve every required project, task, criterion, invariant, finding, and control.
