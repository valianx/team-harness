### Changed
- `agents/mentor.md` (`/th:learn`): codebase-scoped answers now SHOW the actual code in the FIRST reply — real snippets quoted verbatim with `file:line` citations, walked in execution order, with named entry points whose code is shown — instead of only pointing at them abstractly or waiting for the operator to ask again. The standard inline diagram for codebase scope is now a code-grounded data-flow pipeline whose nodes are the real code symbols, annotated with the data shapes flowing between them (a specialization of Diagram-Always, not a replacement). Sharpened the Core Philosophy bullet, the `codebase` source-strategy row, and Layer 3 of the optional pack, plus a new two-part "Show-the-Code — codebase scope" invariant and a cross-reference clause in Diagram-Always. Composes with conversational-first (no document by default; progressive disclosure governs depth, not whether code is shown); `concept`/`library` scope with no open repo is unchanged. Suite 89 +3 checks (23 → 26; also corrects a stale "22 checks" registry string).
