# Testing Conventions — Canonical Suite Registry

> This file is the single source of truth for named structural test suites. `CLAUDE.md §11` points here; it does not maintain a per-suite inventory. Add each new suite's one-liner here when the suite is introduced.

## Testing principles

These two principles are normative for every agent that authors a test in this repo (primarily `tester`, also `qa` and `security`). They were codified after issue #298 (a permission-widening bug in `hooks/dev-guard.sh` that shipped despite a green suite).

### (i) The oracle is the spec, not the implementation output

The expected value in an assertion must be derived from the contract / specification / documented intent — NEVER from running the code under test and recording what it emits. A test whose expected value is "what the code currently does" is a snapshot of present behaviour: it cannot catch a defect that is already present when the snapshot is taken, and it turns red when the defect is later fixed — so it actively defends the bug.

**#298 case study.** `tests/test_dev_guard.sh` asserted `assert_allow` on exactly the buggy default paths of `hooks/dev-guard.sh`. The oracle was "the script outputs `allow`", which was true and which was the bug. The correct oracle is the permission-gate contract: a guard hook's non-covered default is *defer / no-decision* (exit 0, empty stdout), so the assertion must be `assert_nodecision`. The Suite-83 fix flipped those assertions; this principle prevents the class from recurring.

### (ii) Hook / permission / gate behaviour is validated in an isolated environment

A test that exercises a `PreToolUse` hook, a permission gate, or any config-dependent security control MUST run in a clean environment that contains ONLY the plugin's own hooks and wiring — never the developer's personal `~/.claude` config. Two masks make personal-config testing unsafe for this class:

- **Config bleed** — the developer's own `settings.json`, permission mode, and dev-mode marker can mask a plugin-level bug whose symptom depends on environment.
- **Perceptual mask** — in dev mode, dialog-free operation on Edit/Write/benign-Bash is the *expected feel*; a bug that auto-approves those actions produces exactly the experience the operator already expects, so there is no symptom to notice. (This is precisely how #298 escaped its author.)

`tests/test_isolated_hook_env.sh` (Suite 84) implements this: it builds a throwaway `HOME` wired with only the plugin's hooks (read from `.claude-plugin/hooks.json`), drives the chain with controlled tool payloads, and asserts the emitted decision comes solely from the installed hooks. It proves the hook *defers*; it cannot prove the GUI dialog renders (headless CI cannot observe Claude Code's real dialog — that boundary is stated in the suite scope note).

## Test files — scope and coverage

- **`tests/test_policy_block.sh`** — functional tests for `hooks/policy-block.sh`. Each case feeds a tool-call JSON payload and asserts the output (deny → JSON with `permissionDecision: "deny"`; allow → empty stdout). ~48 cases: `rm` destructive vs safe (`/`, `~`, `$HOME`, `--`, wildcard), git destructive vs safe (`--force`, `--no-verify`, `reset --hard`, `clean -f`), SQL DROP/TRUNCATE, sensitive paths (`.env`, `.pem`, `.ssh/`, `.aws/credentials`, `secrets.*`), allow-list variants (`.env.example`/`.sample`/`.template`), malformed payloads (fail-open).
- **`tests/test_agent_structure.py`** — structural tests across `agents/`, `skills/`, `hooks/`. Assertions across all suites: tool allowlists, 5-column Roster matrix, pipeline phases (1.5 / 1.6 / 2.5 / 3.5 / 3.6 / 4.5), per-agent contract sections, workspaces hygiene guardrails, inviolable Phase 1.6 gate + inline fallback, task-list Status + AC checkbox mirror, `PreToolUse` wiring, README cross-references, observability stack (Suite 20), KG hygiene (Suite 21), stage-end notifications + SEC guards (Suite 22), plan-review panel centralization contract (Suite 34), KG MCP tool-name contract (Suite 35), KG write-integrity beacon contract (Suite 36), KG write-policy `_shared` snippet consolidation (Suite 37), review-pipeline-guardrails (Suite 38), nested-dispatch takeover contract (Suite 39), fail-closed security gating (Suite 40), hotfix flow correctness (Suite 41), frontend-scope ux-reviewer wiring (Suite 42), delivery flow hardening (Suite 43), mentor teaching contract (Suite 89). Suite 5 verifies Reconcile Mode in `qa-plan.md` (moved from `qa.md` as part of the qa/qa-plan split, 2026-06-02).
- **`tests/test_agent_frontmatter.py`** — YAML frontmatter validity for every `agents/*.md`. Uses PyYAML via `uv run --with PyYAML python` to catch the silent-agent-drop class of bug (an unquoted `": "` inside a description breaks YAML parsing; Claude Code then silently drops the agent from the registered `subagent_type` list with no error surfaced). Currently validates all agent files in `agents/`.
- **`tests/run-all.sh`** — wrapper that runs all three suites and exits 0 if all pass.

## Named suites — per-suite scope

### Suite 34 — plan-review panel centralization

43 checks (34 original + 9 PR-H extension). Asserts the plan-review panel centralization contract structurally: up to 3 reviewers (qa ratify-plan → security design-review conditional → plan-reviewer last) fold findings into ONE `01-plan.md`; zero side-files; one `## Plan Review` section with `**Substance (qa):**`, `**Security design-review (security):**`, and `**Combined verdict:**` as bold inline labels (not `###` headings); `plan-reviewer` is the sole writer of the header + combined verdict. Self-referential guard asserts `Suite 34` literal in `docs/testing.md` (canonical registry). PR-H extension (checks 31–39, marker: `pr-h-plan-review-integrity`): (31–33) preserve-in-place contract — `_pr_consol` slice declares preserve-in-place of upstream sub-verdicts AND the overwrite-contradiction (`replaces the entire` in `_pr_consol`, `**ALWAYS** overwrite` in Critical Rules) is absent; (34–36) deterministic worst-of roll-up — `_pr_consol` and `_orch_pr` slices declare `worst-of (fail > concerns > pass)` with security verdict mapping (`clean→pass`, `risks-found→fail`), and STAGE-GATE-1 reads `**Combined verdict:**` not just "Plan-reviewer verdict"; (37–39) vacuous-success guard — `_pr_consol` declares qa-label-always + security-label-when-ran → blocked if absent; `_qa_panel` declares unconditional label writing; `_ref_panel` declares direct-mode read-backs (research/diagram) with guard language. All new checks are anchor-scoped (anti-false-green). Written failing-first in Phase 2.0 (2026-05-31).

### Suite 35 — KG MCP tool-name contract

6 checks. Asserts that every `mcp__memory__<tool>` reference in `agents/*.md` is a subset of the canonical context-harness-mcp tool set, and that bare deprecated tokens (`create_entities`, `delete_*`) appear zero times across all agent files. Also asserts orchestrator frontmatter grants only canonical KG tools. Self-referential guard asserts `Suite 35` literal in `docs/testing.md` (canonical registry).

### Suite 36 — KG write-integrity beacon

11 checks. Asserts the write-integrity beacon contract: `kg_write` event schema in `docs/observability.md` (event, attempted, succeeded, reason codes), emission rules in `agents/orchestrator.md` § "Emitting kg_write events" (all 3 sites + 4 reason codes), delivery § "kg_write site:delivery-passive-capture" (passive-capture naming + ok/skipped:mcp-down), rollup in `skills/trace/SKILL.md` § "KG write-integrity rollup" (format-agnostic: jsonl + .md fence extraction), and resilience clauses (best-effort / never-fail language). All checks are anchor-scoped to resist false-greens. Self-referential guard asserts `Suite 36` literal in `docs/testing.md` (canonical registry).

### Suite 37 — KG write-policy `_shared` snippet consolidation

7 checks. Asserts the existence and structure of `agents/_shared/kg-write-policy.md`, the two KG writer agents' references to it (orchestrator Phase 6 + delivery Step 11.5), the non-reference by read-only agents (qa, tester, security, architect — scope-creep guard), and the CLAUDE.md §11 self-referential guard. Checks are anchor-scoped (anti-false-green dispatch); the read-only and self-referential checks are intentionally file-wide (same precedent as Suite 35 check 6 and Suite 36 check 11). Self-referential guard asserts `Suite 37` literal in `docs/testing.md` (canonical registry) and `KG write-policy _shared snippet` marker in the test file itself.

### Suite 38 — review-pipeline-guardrails

13 checks. Asserts all four guardrails of the review-pipeline-guardrails feature structurally using the `_slice_section` anchor idiom (same as Suites 36/37): (1) `## Scope Discipline` section in `reviewer.md` — tokens `In scope` / `Out of scope` / `attribution` / `Fuera de alcance`; cross-refs in Patterns & Consistency and Tests; `### Fuera de alcance` in the `review_body` template; (2) `### AI-Authored PR Review Lens` in `reviewer.md` — `Existence check` / `Plausible-but-wrong` / `Vacuous-test` / `CRITICAL` / `context7`; (3) context7 grant in frontmatter — `mcp__context7__resolve-library-id` + `mcp__context7__query-docs`; (4) `## No-Publish Invariant` — `NEVER` / `publishes` / `draft` / `operator`; (5) repoint guard — `gh-fallback.md § Policy` absent from `reviewer.md` AND `## Policy` absent from `gh-fallback.md`; (6) attribution guard in `reviewer-consolidator.md § Verdict rule` — `Attribution guard` / `out-of-scope` / `Fuera de alcance` / `any-CHANGES_REQUESTED`; (7) behavioral step in `SKILL.md § Step 1.6` — `best-effort` / `isCrossRepository` / `no-command` / `timeout`; (8) fork-exclusion gate — `isCrossRepository` + `fork` + `skipped:fork`; (9) `ref-direct-modes.md § Review Mode` — `No-publish invariant` + `NEVER`; (10) self-referential guard — literal `Suite 38` in `docs/testing.md` and in the test file itself. A missing anchor always produces failure (slice returns empty string → token check fails; no false-green possible). Self-referential guard asserts `Suite 38` literal in `docs/testing.md` (canonical registry) and `review-pipeline-guardrails` marker in the test file.

### Suite 39 — pr-a-takeover-contract

11 checks. Asserts the nested-dispatch takeover contract hardening (PR A of the pipeline-flows-hardening programme): (1) `orchestrator.md § Dispatch-blocked exit` — never-th:orchestrator binding with `th:architect` (boot), `00-state.md` (mid-pipeline), `NEVER` guard; (2) `subagent-orchestration.md § Takeover Protocol` — consume-side guard: if `next_dispatch.agent == th:orchestrator` it is malformed → dispatch phase agent / `th:architect` instead; (3a-3c) `subagent-orchestration.md § dispatch_handoff Schema` — section exists, contains a `json` fenced block, all 8 required fields (`schema_version`, `next_dispatch.agent`, `type`, `phase`, `autonomy.granted`, `round`, `state_ref`, `probe_error`); (4) `orchestrator.md § Dispatch-blocked exit` — `dispatch.blocked` event emitted with `reason` + `action`; (5) schema referenced by canonical name; (6) gate-manifest relabel + Phase Dispatch pointer in Takeover Protocol; (7-8) `skills/README.md` references `§ 14` (not the stale §13 reference) and the stale cross-reference string is absent repo-wide; (9) `skills/setup/SKILL.md` nested-dispatch-takeover block — never-th:orchestrator reinforcement; (10) `dispatch_handoff Schema` type row carries `type: null` security note; (11) self-referential guard — `Suite 39` in `docs/testing.md` (canonical registry) and `pr-a-takeover-contract` marker in test file. All anchor-scoped (anti-false-green: missing anchor → empty slice → check fails).

### Suite 40 — pr-b-security-failopen

12 checks. Asserts the fail-closed security gating fixes (PR B): (1) `orchestrator.md § Bug tier` — hotfix pinned to Tier-3 hard floor + override-clamp (override cannot lower hotfix below Tier 3); (2) `ref-special-flows.md § Hotfix sub-flow` — security-always justified by Tier-3 pin; (3) `orchestrator.md Signal 2 slice` — GATE tokens present (Phase 2 + git-diff/touched-paths + MUST/FUERZA/forces); (4-5a) `subagent-orchestration.md § dispatch_handoff Schema` — `type: null` security note present; no placeholder `hardened in PR B`; (6) `orchestrator.md § Phase 0a` — boot `type=null` forces classify-first before type-gated manifest; (7) `ref-direct-modes.md § Plan-review direct mode` — keyword trigger in plan-review; (8) `orchestrator.md § Phase 0a` — security defaults to RUN when type unknown; (9) this file contains `Suite 40` + `_slice_section` + `pr-b-security-failopen`; (10) `tier_promote: 3` literal in Signal 2 slice (deterministic gate); (11) no placeholder `hardened in PR B` anywhere in `subagent-orchestration.md`; (12) self-referential guard — `Suite 40` in `docs/testing.md` (canonical registry) and `pr-b-security-failopen` marker in test file. All anchor-scoped.

### Suite 41 — pr-c-hotfix-correctness

10 checks (+1 SEC-001 check). Asserts hotfix flow correctness fixes (PR C): (1) `plan-reviewer.md § Session Context Protocol` — defect phrase `should have skipped Phase 1.6 entirely for hotfix` ABSENT; (2) same slice — affirmative tokens `Phase 1.6` + one of `runs normally` / `still run` / `runs for hotfix` PRESENT; (3) `orchestrator.md § Bug-fix flow row mappings` — renderer defect `Phase 1.6 row is skipped for type: hotfix` ABSENT; (4) same slice — `Rule 7` / `Rule 8` affirmative annotation PRESENT; (5) `ref-special-flows.md § Modified phases` (inside Hotfix sub-flow) — canonical `still run` + `1.6` preserved; (6) same slice — `orchestrator` + `Review Summary` (orchestrator authors the Review Summary before STAGE-GATE-1); (7) `orchestrator.md § Phase 2.0` — hotfix auto-promote tokens `bug-not-reproducible` + `hotfix` + `auto-promote` co-present within 300 chars; (8) `orchestrator.md § STAGE-GATE-1` — `hotfix` + `Review Summary` + `authors`/`writes` within 400 chars; (9) same slice — type-aware guard tokens (`type-aware` / `self-authored` / `orchestrator author`); (10) self-referential guard — `Suite 41` in `docs/testing.md` (canonical registry) + `pr-c-hotfix-correctness` + `_slice_section` in test file; (SEC-001) `orchestrator.md § Phase 2.0` — auto-promote re-entry includes `Phase 1.6` + `STAGE-GATE-1` within 500 chars of `auto-promote`. All anchor-scoped; `_s41_claude` reads `CLAUDE.md` for whole-file reference only (not for the Suite literal).

### Suite 42 — pr-d-frontend-wiring

9 checks (+1 consistency check). Asserts frontend-scope ux-reviewer wiring (PR D): (1) `orchestrator.md § When frontend_scope: true — ux-reviewer enrich (Phase 1.7)` — dispatches ux-reviewer enrich (`1.7-ux-enrich`) after architect before Phase 1.5, with input `01-plan.md` and output `01-ux-review.md`; (2) `orchestrator.md § When frontend_scope: true — ux-reviewer validate (Phase 3.4)` — adds `3.4-ux-validate` to the Phase 3 parallel block, input `02-implementation.md + 01-ux-review.md`, output `04-ux-validation.md`; (3) `orchestrator.md § UX gate — frontend_scope: true` — reads `04-ux-validation.md`; critical (WCAG A) fails gate → Case A → implementer; high/medium do NOT block; (4) `orchestrator.md § Phase 3.6` — `04-ux-validation.md` listed in input pointer block; (5) `orchestrator.md § Phase Checklist — frontend_scope additions` — `1.7-ux-enrich` + `3.4-ux-validate` + `[~skipped: frontend_scope:false]` + `phase.start` / `phase.end` + ordering note; (6) `orchestrator.md § ux-reviewer fallback` — inline/nested fallback documented, verdict from `findings.critical`; (7) `agents/ux-reviewer.md § AC sink — 01-plan.md § Task List` — enrich-mode pins AC into `01-plan.md § Task List`; (8) this file contains `Suite 42` + `_slice_section` + `pr-d-frontend-wiring`; (9) self-referential guard — `Suite 42` in `docs/testing.md` (canonical registry) + `pr-d-frontend-wiring` in test file; (10) consistency: both Phase 1 enrich slice and Phase 3 validate slice reference `ux-reviewer` + `04-ux-validation.md`. All anchor-scoped (anti-false-green).

### Suite 44 — pr-th-update-fix

32 checks. Asserts the fix for `/th:update` corrupting `~/.claude/CLAUDE.md` by moving the three managed blocks out of inline fenced copies in `setup/SKILL.md` into canonical source-of-truth files under `skills/setup/managed-blocks/`: (1a-3c) existence and marker-presence of each canonical file (`orchestrator-dispatch-rule.md`, `nested-dispatch-takeover.md`, `voice-rule.md`); (4a-4c) byte-faithfulness — canonical file content (stripped) equals the inline block that was in `setup/SKILL.md`; (4d-4i) key-content checks for distinctive headings and literals inside each canonical file; (5a-5c) double-marker removal — each `<!-- X:start -->` marker appears exactly once in `setup/SKILL.md` (was 2; root cause of corruption); (5d) corruption-prose removal — `append this block:` no longer in `setup/SKILL.md`; (6a-6c) `setup/SKILL.md` references each canonical file path; (7a-7c) `update/SKILL.md` references each canonical file; (7d-7e) per-OS command blocks present (PowerShell + bash); (7f) DESTRUCTIVE replace declaration present; (8/ac-8) self-referential guard — `Suite 44` and `pr-th-update-fix` in `docs/testing.md` (canonical registry); `Suite 44` NOT in `CLAUDE.md §11` (hygiene contract). All checks guard the canonical-file design; byte-faithfulness is the invariant that the block content was moved, not rewritten.

### Suite 43 — pr-e-delivery-hardening

7 checks. Asserts the five delivery-hardening gaps closed by PR E of the pipeline-flows-hardening programme: (1) `delivery.md § Step 9.0` — explicit enumeration of all 5 version sites (`plugins[0].version`, `cmd/install/main.go`, `CLAUDE.md §3`, `CHANGELOG.md`); schema/top-level marketplace version fenced off; no Glob-first-match as discovery mechanism; (2) `delivery.md § Step 9e` — CHANGELOG release cut step exists, gated on Step 9 version bump; moves `[Unreleased]` entries under a new `## [<version>] - <date>` heading and recreates an empty `[Unreleased]`; (3) `delivery.md § Step 9b` — reads `CLAUDE.md §4 Golden Commands` as DoD source; emits `dod: no gates discovered` when all rows are skipped; (4) `delivery.md § Step 2b` — captures active `gh` account via `gh api user -q .login`; reports `gh_account:` in status block; documents the known limitation (operator-owned, by design); (5) `gh-fallback.md § blocked-pr-pending` — structured status for `has_gh=true` push-ok / `gh pr create` failed case: compare URL, body file, `pr opened #N` resume protocol; `delivery.md` references `blocked-pr-pending`; (6) self-referential guard — test file contains `Suite 43` + `_slice_section` + `pr-e-delivery-hardening`; (7) `docs/testing.md` canonical registry names `Suite 43` and `pr-e-delivery-hardening`; `CLAUDE.md §11` does NOT contain the literal `Suite 43` (hygiene contract). All anchor-scoped (anti-false-green); checks 6-7 are file-wide by design.

### Suite 46 — pr-f-observability

16 checks. Asserts the 6 observability-gate hardening fixes of PR F (pipeline-flows-hardening programme): (1-3) `orchestrator.md § Final Pipeline Sanity Check` — asserts `00-pipeline-summary.md` AND the events file exist + non-empty, ≥1 `phase.end` per `[x]` phase in Phase Checklist, and routes mismatch to `blocked-incomplete` (not `pipeline.complete`); (4) `orchestrator.md § Phase 3.5 — Acceptance Gate` — regression-still-passing condition for `type:fix/hotfix` Tier 2-4 (`regression_test_path` PASS + not `skip`/`xfail`); (5-6) Phase 2-close scope check — `git diff --name-only` vs `01-root-cause.md § Scope of Fix` + `[SCOPE-DRIFT]` annotation + coordination note with PR B (diff-vs-sensitive-paths) as distinct and complementary; (7-8) `orchestrator.md § Phase 3` and `Phase 6 — Knowledge Save` — inline pointer to `### Emitting kg_write events` after each write site, mirroring `delivery.md` inline pattern; (9-12) `ref-special-flows.md § Documentation Flow` — lists `00-execution-events`, specifies `phase.start`/`phase.end` per phase (1/2a/2b/3), has `DOC-GATE` gate event, and explicitly declares no KG capture; (13-14) Tier 0 observability carve-out — `CLAUDE.md §5` explicitly exempts Tier 0 from the observability invariant, and `ref-special-flows.md` Tier 0 rows reference the carve-out; (15-16) self-referential guard — `Suite 46` in `docs/testing.md` (canonical registry) and NOT in `CLAUDE.md §11` (hygiene contract, same precedent as Suites 43-45). All content checks use the `_slice_section` anchor idiom (anti-false-green: missing anchor → empty slice → token check fails). Written FAILING-FIRST in Phase 2.0; passes after all 6 implementer fixes land. Marker: `pr-f-observability`.

### Suite 47 — pr-g-docs-fidelity

12 checks. Asserts the 4 docs-flow fidelity hardening fixes of PR G (pipeline-flows-hardening programme): (1) `qa.md § "Docs Validation Mode"` — spot-verify of concrete technical claims (endpoints, params, env vars, config keys, CLI flags) against the real source file (not only `00-research.md`); (2) same slice — an unbacked documented fact is a fidelity finding that FAILS the DOC-GATE (not a soft warning); (3) `documenter.md § "Provenance and Fail-Closed Contract"` — file:line provenance required for every concrete technical claim; (4) same slice — return `blocked` (not invent) when `00-research.md` lacks backing — fail-closed; (5) `orchestrator.md § "Artifact Verification Protocol"` table has at least one docs-flow row with docs-flow context; (6) same table — `00-research.md` row present; (7) same table — `02-documentation.md` row present; (8) same table — `04-validation.md` row with docs-flow qualifier present; (9) `ref-special-flows.md § "DOC-GATE — Human Checkpoint"` — declares pages-on-disk == `pages_created` existence assertion (`pages_created` + disk/exist token); (10) same slice — mismatch routes to `blocked` (fail-closed); (11) self-referential guard — `Suite 47` and `pr-g-docs-fidelity` in `docs/testing.md` (canonical registry) + `Suite 47` and `_slice_section` in this test file; (12) `CLAUDE.md §11` does NOT contain `Suite 47` (hygiene contract — same precedent as Suites 43-46). All content checks use the `_slice_section` anchor idiom (anti-false-green: missing anchor → empty slice → token check fails). Written FAILING-FIRST in Phase 2.0; passes after all 4 implementer fixes land. Marker: `pr-g-docs-fidelity`.

### Suite 45 — pr-th-update-backup-prune

17 checks. REWRITTEN (Phase 2.0, fix #278) — previously asserted prune-presence; now asserts the sandbox-guard fix state. Asserts that the `/th:update` Step 6 per-OS blocks in `skills/update/SKILL.md` are remove-free and `-Force`-free on the protected `/dev-mode` path. Both per-OS block anchors use the `_slice_section` idiom (anti-false-green: missing anchor → empty slice → check fails). (0a-0b) anchor-presence precondition guards for PS and bash slices. (1a-1c/AC-1) PowerShell block (anchor `Windows (PowerShell) — run this block verbatim on Windows:`): no `Remove-Item`; no `.bak-` timestamp glob; contains single rolling backup `Copy-Item $claudeMd "$claudeMd.bak"`. (2a-2d/AC-2) bash block (anchor `Unix/macOS (bash) — run this block verbatim on Linux/macOS:`): no `xargs -r rm`; no `tail -n +4`; no `.bak-` timestamp glob; contains single rolling backup `cp "$CLAUDE_MD" "$CLAUDE_MD.bak"`. (3a-3e/AC-3) neither block: no `New-Item -ItemType Directory -Force`; no `Copy-Item $outputStyleSrc $outputStyleDst -Force`; no `Copy-Item $devModeSkillSrc $devModeSkillDst -Force`; PS block contains `Get-Content $outputStyleDst -Raw` (skip-if-identical compare); bash block contains `cmp -s` (skip-if-identical guard). (4a-4b/AC-4) whole SKILL.md prose: no `last 3` wording; `CLAUDE.md.bak` single-rolling-backup name present. (5/AC-7) self-referential guard: `Suite 45` and `pr-th-update-backup-prune` in `docs/testing.md`; `Suite 45` + `_slice_section` in test file; `CLAUDE.md §11` does NOT contain `Suite 45` (hygiene contract). All 13 content-assertion checks FAIL against current `main` (tripping tokens present); 4 guard checks pass (anchors found, `CLAUDE.md.bak` already in file, self-ref). Passes after the implementer lands the full sandbox-guard rewrite. Marker: `pr-th-update-backup-prune`.

### Suite 48 — pr-i-recover-dedup

21 checks (7+5 keyword checks counted individually + 9 structural checks). Asserts all 8 fixes of PR I (FINAL of pipeline-flows-hardening programme) PLUS 3 residual-fix groups (closing-review-remaining, v2.40.14). Original 13 checks (PR I): (1) `skills/recover/SKILL.md § Recover Safety Rules` — re-emit imperative + STAGE-GATE + never-infer-next_action rule; (2) `orchestrator.md § Recovery Instructions` — same re-emit rule + structural gate-released signal (SEC-DR-4: gate-cleared determined by checklist/events, not prose); (3) same recover section — idempotency: skip-[x]-phases + de-dup structural (not regex); (4) same orchestrator section — idempotency rule mirrored; (5) `skills/recover/SKILL.md` dead pointer fixed: `00-execution-log.md` absent, `00-execution-events` present; (6) `orchestrator.md § Direct Modes` deliver row — STAGE-GATE-3 + Phase 4.5 referenced (gate before push/PR); (7) `ref-special-flows.md § Tier System` — pointer `§ "..."` to orchestrator canonical table, full table rows absent (dedup confirmed); (8) orchestrator.md canonical — `[TIER: 0]` + unambiguous Tier1→2 auto-promotion statement; (9) `orchestrator.md § Phase 0a` --fast skip-set — explicit carve-out: `security design-review` NOT skipped when `security-sensitive` (SEC-DR-1 predicate match); (10) `orchestrator.md § Phase 1.6 is inviolable` — security (design-review) dispatch wired in-pipeline when security-sensitive; (11) `ref-direct-modes.md § Review Panel` — each of the 7 new keywords (`deserialize`, `unserialize`, `pickle`, `SSRF`, `webhook`, `upload`, `sanitize`) present in the canonical design-review trigger list (7 individual checks); (12) CLAUDE.md §N section-ref resolver — every intra-file §N resolves to a real `## N.` heading (cross-file out of scope); (13a-b) self-referential guard — `Suite 48` and `pr-i-recover-dedup` in `docs/testing.md`; `Suite 48` NOT in `CLAUDE.md §11` (hygiene contract). Residual-fix extension checks (closing-review-remaining, v2.40.14, written FAILING-FIRST 2026-05-31): (14) `ref-direct-modes.md § Review Panel` — each of the 5 SEC-DR-3 completion keywords (`xxe`, `ssti`, `traversal`, `redirect`, `cors`) present in the canonical design-review trigger list (5 individual checks, anti-false-green, reuses existing `_s48_rdm_panel_slice`); (15a) `orchestrator.md § Phase 1.6 — Plan Review (Stage 1 closing gate)` dispatch instruction — preserve-in-place token present AND destructive `replace section if it exists` phrase absent (AC-4); (15b) `orchestrator.md § Inline fallback when Task subagent invocation is not available` step 4 — same preserve-in-place assertion (AC-5); (16) `documenter.md § Provenance requirement` step 1 — `00-research.md` present as provenance source AND `source (code file` absent (AC-8). All content checks use the `_slice_section` anchor idiom (anti-false-green: missing anchor → empty slice → token check fails). AC-7 checks are split into two separate checks (9) and (10) per qa + security review observation (SEC-DR-1). Original 13 checks written FAILING-FIRST Phase 2.0 (2026-05-31, PR I); checks 14-16 written FAILING-FIRST Phase 2.0 (2026-05-31, closing-review-remaining); both sets pass after implementer fixes land. Marker: `pr-i-recover-dedup`.

### Suite 49 — pr-cost-rollup-surface

6 checks. Asserts the Phase A token-emission contract (v2.42.1) is locked against regression: (1) `orchestrator.md § Phase Transition Protocol` declares `tokens` REQUIRED for every `phase.end` event (not optional); (2) same slice declares `tokens:0` / `"tokens":0` FORBIDDEN; (3) `orchestrator.md § Execution Events JSONL` schema table carries a `tokens` row with "Required for `phase.end`" annotation (second enforcement site — dual-source contract); (4) same section contains the zero-forbidden wording; (5a) `docs/testing.md` canonical registry names `Suite 49` and `pr-cost-rollup-surface`; this test file contains `Suite 49`, `_slice_section`, and `pr-cost-rollup-surface`; (5b) `CLAUDE.md §11` does NOT contain `Suite 49` (hygiene contract). Both anchors (`### Phase Transition Protocol (atomic` and `## Execution Events JSONL (canonical`) are unique in `orchestrator.md`; a missing anchor returns empty slice → check fails immediately (no false-green). The test blinders Phase B (cost-rollup-surface) against any future softening of the "tokens is mandatory" language — if someone changes REQUIRED to optional or removes the FORBIDDEN clause, checks (1)-(4) fail. Marker: `pr-cost-rollup-surface`.

### Suite 56 — reasoning-partner-posture

11 checks. Asserts the reasoning-partner posture text (PR-B) is present anchor-scoped in both surfaces: (b1a/b1b) disagreement license — `authorized` + `expected` to disagree, `suspicious` (no-concerns), `triggered` not constant; (b2a/b2b) standards anchor — `codified`/`documented` standard, `§6`/`§5`/`working agreements`, `legible`/`defensible`, never the model's `taste`; (b3a/b3b) win-condition reframe — `clarity`, `bar`, `why`, `NOT` artifact/plan, `WHY`/pedagogy, `seminar`/delivery bound; (b4a/b4b) concise-engagement counterweight — no `over-explain`, `internal` reasoning, `salient`/`decision-relevant`, `§7.1` anchor; (b5a) `_slice_section` anchor-scoped idiom used (no loose file-wide search); (b5b) no behavioral check phrase (`disagrees well` / `agent disagrees` / `agent behaves`) present in Suite 56; (self-ref) `Suite 56` and `reasoning-partner-posture` in `docs/testing.md`; `Suite 56` NOT in `CLAUDE.md §11` (hygiene contract). All content checks use `_slice_section` anchored to `"Reasoning-partner posture (checkpoint)"` in `orchestrator.md` and `"## Postura"` in `docs/reasoning-checkpoint.md`. Marker: `reasoning-partner-posture`.

### Suite 57 — review-mode-hard-gates

18 checks. Asserts the three hard gates added by PR-1 of review-gates-and-batch-economy (#251 + #252) are present and cannot be removed without turning this suite red. All checks use anchor-scoped `_slice_section` (3-arg variant, stop markers `("\n### ", "\n## ", "\n---\n")`). (a1-a4) `ref-direct-modes.md § Layer 4 — Mode-transition gate`: section present; NEVER auto-routes; explicit confirmation prompt; neutralizes the global routing rule within review context. (b1-b4) `orchestrator.md § Step 6a-pre` (review_context guard): section present; consults `review_context` before `full pipeline`; routes corrective language to Layer 4; documents lifecycle (write + clear). (c1-c5) `ref-direct-modes.md § Layer 5 — Branch-author guard`: section present; `gh api user` referenced; `fail-closed`; `unknown==unknown→fail-open` prohibited; operator identity indeterminacy named. (d1-d7) `ref-direct-modes.md § Publish Gate (preview-and-confirm)` (anchor scoped to `"\n### Publish Gate"`): section present; `gh pr review` listed; PUT update-body covered; reply verb covered; dismiss covered; all 3 execution sites named; `--auto-publish` documented. (e1-e3) `orchestrator.md § Direct Modes review row` (sliced to `"When invoked with a \`Direct Mode Task\`"`): references Publish Gate; Layer 4 + Layer 5; `review_context`. (f1-f2) `skills/review-pr/SKILL.md § Phase 4 Decision Menu`: present; shows draft to operator. (g1-g2) `skills/review-pr/SKILL.md § Flag parsing`: present; `--auto-publish` documented. (h1-h2) `reviewer.md § No-Publish Invariant`: present; enumerates all 3 execution sites. (i) self-ref: Suite 57 uses `_slice_section` + contains `review-mode-hard-gates`. Marker: `review-mode-hard-gates`.

### Suite 50 — patch-mode-iteration-contract

8 checks. Asserts the Phase D patch-mode iteration contract (v2.45.0) is locked against regression: (a1) `agents/tester.md § Failure Brief` template contains `**Blast radius:**` below `**Root cause type:**`; (a2) `agents/qa.md § Failure Brief` template contains `**Blast radius:**`; (a3) `agents/security.md § Failure Brief` template contains `**Blast radius:**`; (b) `agents/qa-plan.md` does NOT contain `**Blast radius:**` (not an iteration verifier); (c1) `agents/orchestrator.md § If any agent fails` declares the mandatory coherence gate (token: `Coherence gate`) post-localized-patch; (c2) same section declares `structural` always falls to full re-dispatch (tokens: `structural` + `full re-dispatch`); (5a) `docs/testing.md` canonical registry names `Suite 50` and `patch-mode-iteration-contract`; this test file contains `Suite 50`, `_slice_section`, and `patch-mode-iteration-contract`; (5b) `CLAUDE.md §11` does NOT contain `Suite 50` (hygiene contract). All content checks are anchor-scoped via `_slice_section` (anti-false-green: missing anchor → empty slice → token check fails). Marker: `patch-mode-iteration-contract`.

### Suite 61 — configurable-agent-language

24 checks. Asserts the configurable default language feature (v2.55.0) is present and structurally sound: (1-3) `orchestrator.md § Step 1c` — 4-level precedence levels (Session override / Config default / Detection / Default) all present; malformed-code WARN + fallback + never-abort documented; absent-key → level 3 with no warning; (4-5) `orchestrator.md § Step 6a` — language-persistent-default-set and language-session-override intent rows present; Y/n confirmation gate documented; merge-write invariant (never partial payload) specified; config JSON never written without explicit persistence signal; (6) `orchestrator.md § operator_language recovery` — resolves via precedence chain (not just `en`); (7) `orchestrator.md § Language propagation (agent dispatch)` — config `language` key mentioned as config-sourced level; (8-10) managed-block parity — canonical `orchestrator-dispatch-rule.md` and `SKILL.md` inline copy both mention config language key and 4-level precedence (identical text); (11-13) `skills/setup/SKILL.md § Step 3.5` — present; validates ISO 639-1 format; specifies merge-write of complete document; (14) `SKILL.md § Step 5` manifest includes `language` key; (15-17) `CLAUDE.md §5` documents `language` as chat-settable persistent key (new category), with confirmation gate, distinguished from session-override whitelist; (18-19) self-referential guard — `Suite 61` and `configurable-agent-language` in `docs/testing.md`; this test file contains both markers. All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → token check fails). Marker: `configurable-agent-language`.

### Suite 65 — obsidian-worklog-interlinking

30 checks. Asserts the Obsidian work-log interlinking feature (v2.57.0) is structurally present in `agents/delivery.md` § Step 11.6: (1) section exists with the obsidian-only gate phrasing (`logs_mode == "obsidian"` check) and Tier-0/no-workspace no-op; (2) the three exact MOC/index names are present (`_MOC-work-logs`, `_MOC-{repo}`, `{feature_dir}/{feature_dir}.md`); (2b) AC-8 — the repo-MOC template includes a `## Repo Docs` section for stray repo-root docs; (3) the H1-label fallback chain (H1 → frontmatter `title:` → humanized filename) is documented; (4) knowledge-only allowlist (`00-research`, `01-plan`, `01-root-cause`) — three individual checks, one per allowlisted basename; (4b) the four plumbing files (`00-state.md`, `00-execution-events.md`, `00-execution-events.jsonl`, `session.json`) remain explicitly excluded (4 individual checks); (4c) the feature-index template uses `## Knowledge` heading (not `## Stage Docs`); (5) forward-slash wikilink rule is stated; (6) alias escaping (`escape_alias`) and path sanitization are documented; (7) idempotent whole-tree regeneration declared; (8) forward-only reconciliation declared; (9) best-effort posture declared; (10) `docs/obsidian-linking.md` exists and documents the Obsidian dot-label limitation and all three tier names (2 conditional checks); (11) `CLAUDE.md §5` contains a pointer to `docs/obsidian-linking.md`; (12) CLAUDE.md hygiene — `Suite 65` NOT in `CLAUDE.md §11`; (13) self-referential guard — `Suite 65` and `obsidian-worklog-interlinking` in `docs/testing.md` (canonical registry). Step 11.6 is physically placed AFTER Step 11.5 (KG capture) in the delivery doc; the slice stops at `## Session Documentation`. All content checks are anchor-scoped to the `### Step 11.6` section (anti-false-green: missing anchor → empty slice → checks fail). Marker: `obsidian-worklog-interlinking`.

### Suite 67 — tester-lean-core-testing-refs

47 checks. Asserts the tester lean-core refactor (#N, v2.58.0) is structurally correct: (1) core retains every named mode/contract — Pre-Fix Regression (`pre-fix-regression`, `02-regression-test.md`), authoring (`Phase 2.7`, `does NOT validate AC verdicts`), verify-run (`run-only`, `does NOT write new AC tests`), Review mode (`read-only`), Phase 0 Discovery (`context7`), `coverage-config`/`test-infra`/`module-test` (slice non-empty), Return Protocol (all 7 mode tokens + 5 ratchet fields), KG Access (`read-only`, no `create_nodes`) — 22 checks, all anchor-scoped; (2) `## Reference Router` section is non-empty and contains `_index.md`, `Read`, `AC`, and a never-fabricate/degrade token — 5 checks; (3) manifest ↔ filesystem bidirectional agreement: `_index.md` exists and names all 6 types, each named type exists on disk, each disk type is listed in the manifest — 13 checks; (4) all 6 type files named in the router mapping exist on disk — 1 check; (5) each type file contains `## Principles` and `## react-nextjs` sections and has no YAML frontmatter — 18 checks (3 × 6); (6) NestJS pitfall tokens (`TypeORM`, `setImmediate`, `useFakeTimers`) present in `unit.md` or `integration.md` AND absent from `tester.md` — 6 checks (2 × 3); (7) `EXPECTED_AGENTS` lists `tester` exactly once, no tester-variant file exists — 2 checks; (self-ref) `docs/testing.md` names `Suite 67` and `tester-lean-core-testing-refs`; `CLAUDE.md` does not contain `Suite 67` — 2 checks. All mode/contract checks are anchor-scoped (anti-false-green: absent anchor → empty slice → check fails). Marker: `tester-lean-core-testing-refs`.

### Suite 66 — plan-consolidation-invariant

23 checks. Asserts the plan-consolidation invariant (#276, v2.57.0) is structurally present: (1a) `agents/_shared/plan-consolidation.md` exists; (1b) snippet contains the invariant statement (01-plan.md is a snapshot, not a log); (1c) snippet names both canonical fields — `base branch` and `version bump`; (1d) snippet contains the no-forked-file prohibition (`01-plan-*.md` siblings prohibited); (1e) snippet contains the section-ownership map; (2) each of the five plan-writer/auditor agents (`architect`, `plan-reviewer`, `qa-plan`, `qa`, `orchestrator`) references `plan-consolidation.md` and contains the no-forked-file prohibition — 2 checks × 5 agents = 10 checks; (3) `plan-reviewer.md` Rule 3 defines pattern 3h (canonical-field contradiction scan) and names both canonical fields — 3 checks; (4) `qa.md` defines `## Validation Outcome` fold-in and the expanded write-scope — 2 checks; (5) self-referential guard — `Suite 66` and `plan-consolidation-invariant` in `docs/testing.md`; CLAUDE.md hygiene — `Suite 66` NOT in `CLAUDE.md §11`. All checks use direct string search (no anchor scoping needed — snippet existence is the anti-false-green for snippet checks; agent-file reads are direct). Marker: `plan-consolidation-invariant`.

### Suite 68 — multi-project-initiative-overview

57 checks. Asserts the multi-project initiative overview layer (v2.59.0, refined v2.60.0) is structurally present: (AC-1a-1b) `agents/orchestrator.md` defines `initiative` state field with `null = today's behaviour exactly` semantics; (AC-2a-2c) Step 2 path-resolution branch contains the verbatim no-initiative path expressions (`{logs-path}/{logs-subfolder}/{repo_name}` for obsidian, `"workspaces"` for local) and a backward-compatibility guarantee statement; (AC-3a-3d) Step 2 shows the initiative-set obsidian path (repo-base-first, date-prefixed initiative folder `{repo_base}/{YYYY-MM-DD}_{initiative}`), the obsidian `overview.md` path at the dated initiative folder, states local per-project `base_path` stays `"workspaces"` unchanged, and that the local overview lives at the common parent of sibling repos; (AC-4a-4c) `Step 6d-initiative` sub-step exists, contains the generic-root guard list (`projects`, `repos`, `src`, `code`, …), and states NOT to propose on directory layout alone; (AC-5a-5d) Step 6d-initiative contains confirmation prompt with `[Y/n]` + `WAIT`, states never auto-create, and `docs/discover-phase.md § 11 Initiative detection` sub-section exists with confirmation gate described; (AC-6a-6d) Phase 0a Step 1f exists with replace-in-place JOIN rule, append-if-absent rule, and never-duplicate idempotency statement; (AC-7a-7c) Step 1f states rows keyed by project slug, best-effort posture, and WARN on failure; (AC-8a-8f) `## overview.md Template` section exists (renamed from `## 00-overview.md Template`) with frontmatter keys, `## Review Summary`, `## Functional Description`, `## Projects` table (Branch/Version/PR/Status columns), `## Big-Picture Plan`, and section-ownership map naming orchestrator + delivery; (AC-9a-9b) no-fork invariant — snapshot not log, never create `00-overview-*.md` siblings; (AC-10a-10e) `delivery.md ### Step 11.7` exists and is gated on initiative (no-op when null), updates branch/version/PR/status, is best-effort, uses read-modify-write; (AC-11a-11f) `CLAUDE.md §5` initiative-layer bullet exists with detect-confirm, `docs/discover-phase.md § 11` non-empty, `docs/observability.md` states `overview.md` is NOT an events file, CLAUDE.md §3 version reads `2.60.0`; (AC-12a-12f) plugin.json version `2.60.0`, marketplace.json `plugins[0].version` `2.60.0`, CHANGELOG.md contains `## [2.59.0]` section with initiative entry, self-referential guard (`Suite 68` + `_slice_section` + `multi-project-initiative-overview` in test file), `docs/testing.md` registers Suite 68 and the marker, `CLAUDE.md` does NOT contain `Suite 68` (hygiene); (AC-13a-13g) `ac13a` — Step 1f contains date-agnostic JOIN match (`*_{slug}` glob + `initiative:` frontmatter); `ac13b` — Step 2 states per-project `docs_root = {base_path}/{project}` with no `{date}_{feature}` leaf when initiative is set; `ac13c` — delivery.md Step 11.7 overview_path uses the dated `{YYYY-MM-DD}_{initiative}` folder; `ac13d` — `00-overview.md` is absent from all five scoped files; `ac13e`/`ac13e-ownership` — `## Functional Description` is in the template body between Review Summary and Projects, and appears as a distinct row in the section-ownership map; `ac13f-trigger-plan`/`ac13f-trigger-complete` — ownership map declares the on-plan-change reconcile trigger (orchestrator, after Design/STAGE-GATE-1) and the on-completion final-reconcile trigger (delivery, all rows delivered); `ac13g-rows-keyed`/`ac13g-reconcile`/`ac13g-parallel-dispatch-oos` — no-fork invariant states per-project rows keyed one-per-project, functional/big-picture sections reconcile-in-place last-writer-wins, and single-session parallel dispatch is out of scope. All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Marker: `multi-project-initiative-overview`.

### Suite 69 — parallel-multi-project-dispatch

37 checks. Asserts the first-class parallel multi-project dispatch contract (v2.61.0) is structurally present: (AC-1) `agents/orchestrator.md` contains a `## Parallel Multi-Project Dispatch` section documenting fan-out-at-Stage-2 as the concurrency model — Stage 1 serial per-project, concurrent Stage-2 implement+verify lanes, re-convergence for ACCEPTANCE/STAGE-GATE-3/delivery (4 checks); (AC-2) fan-out section references the in-message concurrent-Task mechanism, states no Workflow tool required, and states no nested-dispatch required (3 checks); (AC-3) `### Eligibility-detection contract` section present and documents exclusions for deferred/blocked/delivered projects, A-blocks-B sequencing, and shared-contract-in-flux pairs (6 checks); (AC-4) `### Fan-out confirm gate` section present with WAIT for operator approval, serial choice, and --serial-always-wins absolute override (4 checks); (AC-5) `### Gate semantics with N concurrent projects` section present — STAGE-GATE-1 per-project/serial, STAGE-GATE-3 batched at re-convergence with per-project delivery, failure isolation (one lane fail does not block siblings) (5 checks); (AC-6) `### No-fork / consolidation invariant` no longer contains the out-of-scope line; reconcile-ordering rule present; keyed-rows and last-writer-wins rules confirmed present (no regression) (4 checks); (AC-7) `docs/observability.md` contains `## Initiative-level fan-out trace` section with all four event names (`fanout.start`/`lane.start`/`lane.end`/`fanout.converge`), project key, `/th:pipelines` rendering, and orchestrator.md fan-out section documents observability (8 checks); (AC-8) `### Safety floors` section present with: initiative-null gate, --serial floor, security-per-lane unchanged, in-flux-contract exclusion (5 checks); (AC-9) packaging complete — plugin.json `2.61.0`, marketplace.json `plugins[0].version` `2.61.0`, CLAUDE.md §3 `2.61.0`, CLAUDE.md §5 parallel dispatch bullet, changelog fragment exists with `### Added`, self-referential guard, docs/testing.md registry, CLAUDE.md hygiene (8 checks). All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Marker: `parallel-multi-project-dispatch`.

### Suite 71 — delivery-pr-mergeability-check

27 checks. Asserts the post-create PR mergeability + CI check (Step 11.4) added to `agents/delivery.md` (v2.63.0, issue #263): (a1) `delivery.md` contains a `Step 11.4` section; (a2) the section queries `mergeable,mergeStateStatus,statusCheckRollup`; (a3) step is gated on `has_remote`, `has_gh`, and PR number known; (a4) Step 11.4 appears before Step 11.5 (post-create, before KG capture); (b1) bounded 3-attempt backoff (0s/2s/4s) for `UNKNOWN` documented; (b2) `CONFLICTING`/`DIRTY` reported explicitly as non-clean delivery; (b3) `UNKNOWN`-after-retries → undetermined, never clean; (b4) graceful gh-fallback skip with `not-verified: gh-unavailable`; (b5) step is report-only (never changes delivery exit status); (c1) `ci_state` field documented with `passing/failing/pending/none/not-verified`; (c2) empty `statusCheckRollup` treated as "no checks configured" (not a failure); (c3) offer-to-resolve is an offer, NOT an automatic action; (d1) `skills/deliver/SKILL.md` documents post-create merge-state verification; (d2) SKILL.md notes conflicting PR reported explicitly; (d3) SKILL.md notes graceful skip when gh unavailable; (e1) delivery Return Protocol declares `mergeable_state:` field; (e2) delivery Return Protocol declares `ci_state:` field; (f1-f3) self-referential guard — this test file contains `Suite 71` + `_slice_section` + `delivery-pr-mergeability-check`; `docs/testing.md` registers Suite 71; `CLAUDE.md` does NOT contain `Suite 71` (hygiene contract); (g1-g4) packaging — `plugin.json` version `2.63.0`, `marketplace.json` `plugins[0].version` `2.63.0`, `CLAUDE.md §3` mentions `2.63.0`, `CHANGELOG.md` contains `## [2.63.0]` section with `mergeabilit` keyword. All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). CRITICAL: changelog assertion targets the DURABLE `CHANGELOG.md [2.63.0]` section — NEVER `changelog.d/delivery-pr-mergeability-check.md` existence (fragment is assembled+deleted at delivery, so a fragment-existence assertion fails on CI: this guard prevents the bug that recurred 3× in this session). Written FAILING-FIRST Phase 2.0 (2026-06-08). Marker: `delivery-pr-mergeability-check`.

### Suite 70 — fix-plan-execution-workspace-continuity

40 checks (27 original v2.64.0 + 13 GAP1 additions M1.6 for plan-output-quality-guards, v2.72.0). Regression tests for workspace-continuity and the FINAL milestone standard (v2.62.0 original three defects + v2.64.0 Defect D: wrong milestone model from #286). GAP1 additions (M1.6, v2.72.0, Marker: `no-task-division-guards`): (c7b) `agents/ref-special-flows.md § Milestone-Build Flow` prohibits ALL stage-file suffixes, names `02b-implementation.md` / "second delivery cycle" alongside `-m{N}` and `{NN}_{milestone}/`, and states inventing an undocumented file-naming convention is itself a defect — 3 checks; (c7c) no agent doc TEACHES a `-b`/second-cycle convention — every agent file containing `02b`, `second-cycle`, or `second delivery cycle` also contains a prohibition/guard keyword (scoped so the prohibition text itself does not trip the assertion) — 1 check; (c7d) operator-authority invariant present in `ref-special-flows.md § Milestone-Build Flow` AND referenced by a one-line guard in BOTH `architect.md` (`#### Default: one PR per service` slice) AND `orchestrator.md` (direct text search) — 3 checks; (AC-4) transport-only case named as INVALID split reason in BOTH `architect.md` invalid-split-reasons list AND `plan-reviewer.md` Rule 1 — 2 checks; (AC-5) `orchestrator.md` post-approval-division re-gate trigger present + references architect re-run + Phase 1.6 + STAGE-GATE-1 — 2 checks; (AC-6) `implementer.md` one-line guard against suffixed/second-cycle stage files + `delivery.md` one-line guard against opening a PR not in the approved Task List on own authority — 2 checks. All GAP1 checks anchor-scoped (anti-false-green: missing anchor → empty slice → check fails). Group (a) UTC pin — 3 checks: `orchestrator.md` Step 0 workspace-root definition explicitly references `UTC`, states the `{YYYY-MM-DD}` prefix is cosmetic/display-only, and states the prefix is ignored when matching. Group (b) identity-keyed date-agnostic resume — 2 checks: `orchestrator.md` "At task start" uses a date-agnostic `*_{feature-name}` glob and a frontmatter `feature:` confirm step. Group (c) milestone FINAL-contract assertions — 12 checks (REVISED v2.64.0): KEPT: one-build-one-workspace model; build-level milestone index documented; detect-and-continue by identity; `01-plan.md` as plan artifact; `01-planning.md` preserved (no mass-rename). FLIPPED from #286 child-workspace/per-milestone-PR to FINAL standard: (c2) `02-implementation.md`/`03-testing.md`/`04-security.md`/`04-validation.md` are FLAT whole-task docs with no per-milestone subsections; (c7) per-milestone-suffixed filenames (`-m{N}`) and `{NN}_{milestone}/` child folders are explicitly PROHIBITED; (c8) milestones are explicitly NOT deliverables and NOT PRs — each maps to ONE commit; (c9) independent milestone implementations are PARALLELIZED (reusing #285 concurrent-Task mechanism at milestone granularity within ONE workspace); (c10) ONE PR opened only when ALL milestones complete; (c11) one commit per milestone on the single feature branch; (c12) Milestone Index has a `Commit` column (no per-milestone `PR` column). Group (d) backward-compat — 2 checks (FROZEN): single-shot feature/fix keeps unchanged `{date}_{feature}` behavior; new milestone-continuity path activates only for multi-milestone `type:plan` builds. Group (e) self-ref / registry — 3 checks. Group (f) packaging — 6 checks: `plugin.json` version `2.64.0`; `marketplace.json` version `2.64.0`; `CLAUDE.md §3` mentions `2.64.0`; `CHANGELOG.md` contains `## [2.64.0]` section (durable — NEVER assert `changelog.d/standardize-milestone-concept.md` existence; fragment is assembled+deleted at delivery); `CHANGELOG.md [2.64.0]` section documents the milestone-standard fix; `CLAUDE.md §5` milestone bullet states milestones = commits, NOT PRs. All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Written FAILING-FIRST Phase 2.0 (2026-06-09). Marker: `fix-plan-execution-workspace-continuity`.

### Suite 72 — review-pr-routing-and-comments

27 checks. Regression suite for issue #290 — two independent defects in the PR-review path (v2.65.0). Group (a) Facet A — Step 6 PR-review intent row (3 checks): (a1) `orchestrator.md` declares a PR-review intent row whose route resolves to the `/th:review-pr` skill flow; (a2) that row is anchor-scoped to the Step 6 intent table (not elsewhere in the file); (a3) both `plan-review` and PR-review rows coexist (distinct entries). Group (b) Facet A — `:143/:150` reconciliation (3 checks): (b1) `reviewer` is still classified as a standalone agent (classification preserved); (b2) reconciliation wording states the orchestrator routes PR-review intent to `/th:review-pr` — NOT bare-dispatch `reviewer`; (b3) no remaining contradiction — `reviewer` stays standalone AND intent routes to the skill flow. Group (c) Facet B — `agents/_shared/gh-fallback.md` (4 checks): (c1) a `Tier A — read PR comments` subsection exists; (c2) issue-level fetch documented (`gh pr view {number} --comments` or `issues/{number}/comments`); (c3) line-level fetch documented (`pulls/{number}/comments`); (c4) best-effort degradation note present. Group (d) Facet B — `skills/review-pr/SKILL.md` Phase 1 (4 checks): (d1) Phase 1 contains a comment-fetch step; (d2) step references `agents/_shared/gh-fallback.md`; (d3) fetched comments passed into Phase 3 reviewer dispatch payload via `PR Comments:` field; (d4) graceful degradation documented (`comments not fetched — gh unavailable`). Group (e) Facet B — `agents/reviewer.md` (3 checks): (e1) clause to consume `PR Comments:` context declared; (e2) clause states it does NOT re-raise points already resolved in the thread; (e3) No-Publish Invariant intact (frozen regression guard — must pass pre-fix AND post-fix). Group (f) publish path frozen (2 checks): (f1) Phase 5 single atomic `POST .../reviews` still present; (f2) Phase 3.5 reply step still present (frozen regression guards — pass both before and after the fix). Group (g) self-referential / registry (3 checks): (g1) test file contains `Suite 72` + `_slice_section` + `review-pr-routing-and-comments`; (g2) `docs/testing.md` registers Suite 72 and the feature marker; (g3) `CLAUDE.md` does NOT contain `Suite 72` (hygiene contract). Group (h) packaging (5 checks): `plugin.json` version `2.65.0`; `marketplace.json` `plugins[0].version` `2.65.0`; `CLAUDE.md §3` mentions `2.65.0`; `CHANGELOG.md` contains `## [2.65.0]` section (durable — NEVER assert `changelog.d/fix-review-pr-routing-and-comments.md` existence; fragment is assembled+deleted at delivery); `CHANGELOG.md [2.65.0]` section documents PR-review routing fix or comment-ingestion. All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Written FAILING-FIRST Phase 2.0 (2026-06-09). Marker: `review-pr-routing-and-comments`.

### Suite 74 — d2-likec4-obsidian-output

34 checks. Structural tests for the obsidian-mode render+embed contract added to the D2 and LikeC4 diagram flows (v2.67.0). Group (a) D2 agent contract (3 checks): `agents/d2-diagrammer.md` contains an Obsidian Output Mode section; the section documents SVG render via the `d2` CLI into the vault folder; the section (full agent scope) contains `![[diagram.svg]]` embed and `05-diagram.md`. Group (b) LikeC4 agent contract (3 checks): `agents/likec4-diagrammer.md` contains an Obsidian Output Mode section; the section documents PNG export via `npx likec4 export png`; the section (full agent scope) contains a PNG embed (`![[...png]]`) and `05-diagram.md`. Group (c) CLI-absent degradation (4 checks): both `agents/d2-diagrammer.md` and `agents/likec4-diagrammer.md` contain a "not rendered" marker (full agent text scope); both agents document a `render:` status field. Group (d) D2 skill mirror (3 checks): `skills/d2-diagram/SKILL.md` has an `Obsidian Output Mode` section; the section (full skill scope) documents `![[diagram.svg]]`; the section documents CLI-absent degradation. Group (e) LikeC4 skill mirror (3 checks): `skills/likec4-diagram/SKILL.md` has an `Obsidian Output Mode` section; the section (full skill scope) references a PNG embed; the section documents CLI-absent degradation. Group (f) ref-direct-modes (4 checks): D2 Diagram Mode section documents `![[` embed + `05-diagram.md` (anchor-scoped, stops at next `##` or `---`); LikeC4 Diagram Mode section documents the same; Excalidraw Diagram Mode still routes to `diagram.excalidraw` (frozen regression guard — MUST pass pre-fix AND post-fix); local-mode `diagram.d2` path still referenced. Group (g) Excalidraw parity guard (2 checks, frozen): `agents/diagrammer.md` does NOT gain D2/LikeC4 obsidian-embed additions; `skills/excalidraw-diagram/SKILL.md` does NOT gain D2/LikeC4 obsidian-embed additions. These checks MUST pass both before and after the fix. Group (h) self-referential / registry (3 checks): test file contains `Suite 74` + `_slice_section` + `d2-likec4-obsidian-output`; `docs/testing.md` registers Suite 74 and the feature marker; `CLAUDE.md` does NOT contain `Suite 74`. Group (i) packaging (5 checks): `plugin.json` version `2.67.0`; `marketplace.json` `plugins[0].version` `2.67.0`; `CLAUDE.md §3` mentions `2.67.0`; `CHANGELOG.md` contains `## [2.67.0]` section (durable — NEVER assert `changelog.d/d2-likec4-obsidian-output.md` existence; fragment is assembled+deleted at delivery); `CHANGELOG.md [2.67.0]` section documents D2/LikeC4 obsidian-mode embed contract. All agent/skill content checks use the full file text where the content is inside fenced code blocks (anti-false-truncation), and anchor-scoped `_slice_section` where the section structure allows (anti-false-green). Written FAILING-FIRST Phase 2.0 (2026-06-09). Marker: `d2-likec4-obsidian-output`.

### Suite 73 — fix-agent-builder-and-diagram-routing

21 checks. Regression suite for issue #290 residuals — two routing-determinism gaps that survived the #291 fix (v2.66.0). Group (a) Facet A — D2 engine route (2 checks): (a1) `orchestrator.md` Step 6a intent table contains a D2-engine disambiguation row routing D2 keywords to `d2-diagram` (anchor-scoped to the intent table); (a2) the row includes a D2/diagrama D2/dot keyword in the Intent Pattern column. Group (b) Facet A — LikeC4/C4 engine route (2 checks): (b1) `orchestrator.md` Step 6a intent table contains a LikeC4/C4 row routing to `likec4-diagram` (anchor-scoped); (b2) the row includes a LikeC4/C4/architecture-as-code keyword. Group (c) Facet A — generic Excalidraw default (1 check): (c1) the existing generic `diagram`/Excalidraw route is still present in the Step 6a table (frozen regression guard — MUST pass pre-fix AND post-fix). Group (d) Facet A — disambiguation note (2 checks): (d1) the `**Disambiguation —` block names all three diagram engines (`diagram`, `d2-diagram`, `likec4-diagram`); (d2) the block explicitly names Excalidraw as the default engine for the generic route. Group (e) Facet B — agent-builder intent row (2 checks): (e1) `orchestrator.md` Step 6a intent table contains a row routing agent/skill-building intent to the agent-builder canonical flow (anchor-scoped); (e2) the row includes an agent/skill-building keyword. Group (f) Facet B — `:150` reconciliation (2 checks): (f1) `agent-builder` is still classified as standalone in the `:150` note (preserved invariant); (f2) the note contains a reconciliation sentence specifically routing agent-builder intent to its canonical flow (proximity-based check for unique phrasing absent pre-fix). Group (g) Facet B — residual-limit documented (2 checks): (g1) the `:150` standalone note documents the host-layer auto-selection bypass (anchor-scoped — `native`/`host`/`auto-select` in the standalone slice); (g2) the note states the Step 6a route covers only orchestrator-mediated requests (no over-claim). Group (h) self-referential / registry (3 checks): (h1) test file contains `Suite 73` + `_slice_section` + `fix-agent-builder-and-diagram-routing`; (h2) `docs/testing.md` registers Suite 73 and the feature marker; (h3) `CLAUDE.md` does NOT contain `Suite 73` (hygiene contract). Group (i) packaging (5 checks): `plugin.json` version `2.66.0`; `marketplace.json` `plugins[0].version` `2.66.0`; `CLAUDE.md §3` mentions `2.66.0`; `CHANGELOG.md` contains `## [2.66.0]` section (durable — NEVER assert `changelog.d/agent-builder-and-diagram-routing.md` existence; fragment is assembled+deleted at delivery); `CHANGELOG.md [2.66.0]` section documents diagram-engine disambiguation or agent-builder routing. Includes AC-6 brittle-test hardening: the existing `plan-review(20/ac-7)` assertion in Suite 34 was converted from a `find("revisar") + [-200:+1800]` positional window to an anchor-scoped `_slice_section` bounded by the `**Disambiguation —` line (behavior-preserving — same tokens asserted, stable anchor replaces sliding offset). All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Written FAILING-FIRST Phase 2.0 (2026-06-09). Marker: `fix-agent-builder-and-diagram-routing`.

### Suite 75 — hooks-secretscan-checkpoint-b2b3

9 checks. Structural assertions for the hooks-secretscan-checkpoint-b2b3 feature (v2.68.0, issue #289). Group (a) Facet A — secret-scanner in `hooks/policy-block.sh` (2 checks): (a1) `hooks/policy-block.sh` references the `AKIA` AWS-key high-confidence pattern (presence of secret-scan tier); (a2) `hooks/policy-block.sh` emits `ask` as a `permissionDecision` (medium-confidence tier + `ask()` helper added). Group (b) Facet B — B2/B3 boundary-keyed gate in `hooks/checkpoint-guard.sh` (2 checks): (b1) `hooks/checkpoint-guard.sh` references `research-next` (B2 boundary value); (b2) `hooks/checkpoint-guard.sh` references `postverify-next` (B3 boundary value). Group (c) packaging (4 checks): `plugin.json` version `2.68.0`; `marketplace.json` `plugins[0].version` `2.68.0`; `CLAUDE.md §3` mentions `2.68.0`; `CHANGELOG.md` contains `## [2.68.0]` section (durable — NEVER assert `changelog.d/feat-hooks-secretscan-checkpoint-b2b3.md` existence; fragment is assembled+deleted at delivery; recurring-bug guard). Group (d) self-referential / registry (3 checks): test file contains `Suite 75` + `hooks-secretscan-checkpoint-b2b3`; `docs/testing.md` registers Suite 75; `CLAUDE.md` does NOT contain `Suite 75` (hygiene contract). All checks use direct string search (hook files are not markdown; no anchor scoping needed — pattern presence is the anti-false-green for hook checks; `_s59_ver_tuple` helper reused for version comparisons). Written FAILING-FIRST in Phase 2.0 (2026-06-09). Marker: `hooks-secretscan-checkpoint-b2b3`.

### Suite 83 — dev-guard-default-nodecision

16 cases (8 flipped + 8 new failing-first). Functional behavioral tests for the `hooks/dev-guard.sh` default-path fix (issue #298, v2.71.x). File: `tests/test_dev_guard.sh`. The suite is wired as "Suite 5" in `tests/run-all.sh` (run-all numbering space is separate from this named-suite registry; Suite 5 in `test_agent_structure.py` refers to the Reconcile Mode qa-plan check — see below).

**ANTI-FALSE-GREEN NOTE:** The default/fail-safe terminal paths assert **no permissionDecision** (`assert_nodecision` — exit 0, empty stdout, defer to normal permission flow), NOT `"allow"`. On current (unfixed) code every `assert_nodecision` case emits `permissionDecision: "allow"` and **FAILS**. Green on these assertions comes ONLY after the implementer replaces the four default `allow()` calls with `nodecision()` (exit 0, empty stdout) in `hooks/dev-guard.sh`.

**Flipped cases (were `assert_allow`, now `assert_nodecision`):** (1) no-marker block — gh pr merge / git push / gh pr review / curl with no marker (4 cases, line :151 `allow()` bug); (2) Case 12 — `git status` innocent with marker (line :235 `allow()` bug); (3) Case 13 — anti-spoof `dev_mode: false # was true` marker (line :137 `allow()` bug); (4) Case 15 — `ls -la` innocent with marker (line :235 `allow()` bug); (5) Case 20 — marker present with `dev_mode: false` explicit (line :137 `allow()` bug).

**New failing-first cases (AC-1 / AC-2 / AC-3 for #298):** (6-7) Edit-shaped payload `{"tool_name":"Edit","tool_input":{...}}` with marker present and with no marker → `assert_nodecision` (worst path: line :111 `allow()` fires before marker is read; dev-mode-independent auto-approval of every file edit); (8-9) Write-shaped payload `{"tool_name":"Write","tool_input":{...}}` with and without marker → `assert_nodecision`; (10-11) benign Bash `git status` / `ls -la` with no marker → `assert_nodecision`; (12) benign Bash `git status` with marker → `assert_nodecision`; (13) `dev_mode: false` marker + `git log` → `assert_nodecision`.

**Preserved-behavior cases (pass on current AND fixed code):** outward-action cases (git push, gh pr merge/review/comment, gh api, curl — Cases 1-11) → `assert_ask`; activation-write cases (Cases 16, 17, 19: `echo/printf 'dev_mode: true' > marker`) → `assert_allow`; fail-CLOSED case (Case 14: marker present-but-empty + `git push`) → `assert_ask`; sentinel-bypass case (Case 18: marker present + `dev_mode_choice: "off"` in config + `git push`) → `assert_ask`.

**Suite-5 number collision note:** `tests/run-all.sh` labels `test_dev_guard.sh` as "Suite 5" in its banner (shell run-all local numbering). The structural `test_agent_structure.py` internal Suite 5 refers to the Reconcile Mode check in `qa-plan.md`. These are two independent numbering spaces. This named-suite registry resolves the ambiguity by registering the dev-guard functional tests under **Suite 83** with the `dev-guard-default-nodecision` marker. Written FAILING-FIRST Phase 2.0 (2026-06-10). Marker: `dev-guard-default-nodecision`.

### Suite 84 — isolated-hook-env-harness

11 checks. Clean-environment functional harness for the plugin's PreToolUse hook chain (issue #298 reinforcement, v2.7x.0). File: `tests/test_isolated_hook_env.sh`. Wired into `tests/run-all.sh`. SCOPE — the distinct value vs Suite 83 (`test_dev_guard.sh`, which drives the hook script directly) is ISOLATION: this harness constructs a throwaway `HOME` whose `.claude/` directory wires ONLY the plugin's hooks (parsed from `.claude-plugin/hooks.json`), with NO bleed from the developer's personal config, then drives the chain (`dev-guard.sh` + `policy-block.sh`) and asserts the emitted `permissionDecision` comes solely from the installed hooks. KEY PROPERTY (the #298 signal): in a clean env with dev mode OFF, an Edit-shaped payload (no `command` field) must NOT be auto-approved — the chain must emit NO `permissionDecision` (defer). Scenarios cover dev mode ON and OFF × {Edit payload, Write payload, benign Bash, covered outward action, destructive Bash}. CAN PROVE: the hook chain defers on non-covered paths and asks on covered outward actions, in isolation from personal config. CANNOT PROVE: that Claude Code's real GUI permission dialog renders (headless CI has no dialog surface — asserted only on the hook's deterministic emitted decision). Design property: would have FAILED on pre-#298 `dev-guard.sh` (default `allow`) and PASSES on the fixed (≥v2.71.x) code. Self-referential guard: `Suite 84` + `isolated-hook-env-harness` in `docs/testing.md` (canonical registry) and in the test file; `Suite 84` NOT in `CLAUDE.md §11` (hygiene contract). Marker: `isolated-hook-env-harness`.

### Suite 85 — hook-gates-hardening

Behavioral (shell): `tests/test_hook_gates_hardening.sh`. Structural (Python): `tests/test_agent_structure_hardening.py`. Wired as Suite 10 (shell) and Suite 11 (Python) in `tests/run-all.sh`. Written FAILING-FIRST Phase 2.0 (2026-06-10) for issue #304. All checks FAIL on current tree (pre-fix @ fad06ea) and are designed to PASS after the implementer applies Milestones A–D.

**Shell suite (F-002, F-015, F-016, F-010, F-018) — 19 assertions:**

- **F-002 degraded floor (6 cases):** with python3 masked from PATH via a shim that exits 127, `policy-block.sh` must still emit exact-shape `permissionDecision: deny` for `rm -rf ~` (DENIED_BASH denylist) and `AKIA…`-pattern AWS key (HIGH_CONFIDENCE_SECRETS). PRE-FIX: script exits 127 with no output (fail-open). Decision-JSON shape is asserted in full (`hookSpecificOutput.permissionDecision == "deny"`) not as substring — a malformed deny silently becomes allow (qa-plan C-1).
- **F-015 backslash floor (6 cases):** Windows backslash paths `C:\Users\x\.ssh\id_rsa`, `D:\app\.env`, `..\secrets.yaml` must produce `permissionDecision: deny` from SENSITIVE_PATHS (same as their forward-slash equivalents). PRE-FIX: SENSITIVE_PATHS patterns use only forward-slash anchors `(^|/)` — backslash paths bypass silently.
- **F-016 escape-aware extraction (3 cases):** with python3 masked, `git commit -m "msg" && git push origin feat/x` compound command must produce `permissionDecision: ask`. PRE-FIX: grep fallback `[^"]*` truncates at the first embedded quote — the `git push` is invisible to the guard, no decision emitted.
- **F-010 obsidian checkpoint resolution (2 cases):** with `.team-harness.json` set to `logs-mode: obsidian` and `00-state.md` in the vault directory outside `$CWD`, `checkpoint-guard.sh` must (a) find and read the state file (no fail-open for an armed boundary) and (b) pass-through when the vault state is non-blocking. PRE-FIX: find is scoped to `$CWD` only — vault-resident state is invisible.
- **F-018 mtime selection (2 cases):** given two workspaces — `alpha-old/00-state.md` (stale, status: complete, alphabetically first) and `zeta-active/00-state.md` (armed, newest-mtime, status: in-progress) — `checkpoint-guard.sh` must select `zeta-active`. PRE-FIX: `sort -t'/' -k1 | head -5` then `[0]` selects alphabetically first → `alpha-old` (wrong workspace, fail-open on the armed boundary).

**Structural suite (F-008, F-038, F-009, A1, Lint Check 8) — 34 assertions:**

- **F-008 ClickUp matcher (5 checks):** `.claude-plugin/hooks.json` and `hooks/config.json` (all three OS blocks) must each contain a PreToolUse entry whose matcher covers `mcp__.*__clickup_*` tools. Matcher must use wildcard, not a hard-coded server name. PRE-FIX: only `Bash` and `Bash|Write|Edit|NotebookEdit` matchers exist; no ClickUp entry.
- **F-038 sketch-guard resolution chain (6 checks):** the four invocation sites (`skills/deliver`, `skills/validate`, `skills/review-pr`, `agents/orchestrator.md`) must not contain bare `bash hooks/sketch-guard.sh`; must document the 3-tier resolution chain (plugin-cache → `~/.claude/hooks/` → `./hooks/`); must use a visible-skip sentinel when unresolvable. PRE-FIX: all four sites use bare repo-relative path which is dead on plugin installs.
- **F-009 recover predicate (5 checks):** `skills/recover/SKILL.md § Recover Safety Rules` and `agents/orchestrator.md § Recovery Instructions` must reference the decision allowlist (`approved`, `approved-autonomous`, `next`, `ship`), per-gate release fields (`gate1_release`, `gate2_release_last`, `gate3_release`), and negative decisions (`rejected`, `edit`, `stop`, `abort`) that do NOT clear a gate. `agents/orchestrator.md § Current State` schema must declare the three gate fields. PRE-FIX: predicate is event-presence only; no allowlist; no field schema.
- **A1 guided install (14 checks, AC-13):** `skills/setup/SKILL.md` and `skills/update/SKILL.md` must each contain: Y/n consent-gate, OS command literals (`winget install`, `brew install python3`, `apt-get`/`dnf`/`pacman`), post-install re-probe (`command -v python3`), Windows PATH caveat (`restart`). Negative: install command appears after consent gate, never as bare unconditional call. PRE-FIX: neither file has any python3 probe or install block.
- **Lint Check 8 (4 checks, AC-10):** `skills/lint/SKILL.md` must contain a `Check 8` (Hook runtime health) section documenting python3-absence WARN, wired-but-missing FAIL, and obsidian checkpoint coverage status. PRE-FIX: lint/SKILL.md has no Check 8.
- **Self-referential guards (3 checks):** `docs/testing.md` registers `Suite 85` and `hook-gates-hardening`; this test file contains `Suite 85` + `_slice_section` + `hook-gates-hardening`; `CLAUDE.md §11` does NOT contain `Suite 85`.

Marker: `hook-gates-hardening`.

### Suite 86 — workspace-repo-boundary

8 checks. Structural regression test for the workspace↔repository boundary-contamination defect (issue #303 follow-up, v2.77.0). Written FAILING-FIRST Phase 2.0 (2026-06-11). File: `tests/test_agent_structure.py`. All 8 checks FAIL on the pre-fix tree (guards absent) and are designed to PASS after the implementer adds the canonical boundary invariant + three short guards.

Group (r) — boundary guard assertions (5 checks): (r1) `docs/plan-sketches.md` contains the canonical boundary invariant — co-presence of `workspace`, `repository`, `openapi`, `format`; (r2) `agents/implementer.md` Step 3b slice contains a repo-spec format-preservation guard — `openapi` + (`preserve` OR `keeps its existing`) + `format` (anchor-scoped to Step 3b, anti-false-green); (r3) `agents/delivery.md` Step 8 slice contains the same preservation guard — empty slice (missing anchor) → FAIL (anti-false-green); (r4) `agents/architect.md` api-contract skeleton slice contains a workspace-scoping note — `workspace` present in the slice anchored to `**\`sketches/api-contract.md\`**`; (r5) **negative no-conversion guard** — none of the four anchored files contains `convert` + `openapi` + `json` all within a 200-character window (format-agnostic intent: asserts *absence of conversion instruction*, NOT a YAML requirement). Group (h) — hygiene / registry (3 checks): (h1) self-referential: test file contains `Suite 86` + `workspace-repo-boundary`; (h2) `docs/testing.md` registers `Suite 86` + `workspace-repo-boundary`; (h3) `CLAUDE.md` does NOT contain `Suite 86` (§11 hygiene contract).

**Anchor-substring contract for the implementer:** `docs/plan-sketches.md` must contain `workspace`, `repository`, `openapi`, `format`; `agents/implementer.md` (Step 3b) must contain `openapi` + `preserve`/`keeps its existing` + `format`; `agents/delivery.md` (Step 8) same three tokens; `agents/architect.md` (api-contract skeleton) must contain `workspace`. No file may contain `convert`+`openapi`+`json` in a 200-char window.

Marker: `workspace-repo-boundary`.

### Suite 87 — gcp-guard-hook-behavior

31 checks. Behavioral tests for `hooks/gcp-guard.sh` — the deterministic PreToolUse verb-classifying gate for the `gcp-infra` agent (AC-5, v2.78.0). File: `tests/test_gcp_guard.sh`. Mirrors the `test_dev_guard.sh` (Suite 83) shape: payload builders, `assert_nodecision` / `assert_ask` / `assert_deny` / `assert_never_allow` helpers, one case per contract clause.

**Cases (10 numbered + additional contract assertions):** (1) read-only verb (`list`) → nodecision; (2a/2b) read-only flags (`--dry-run` / `--validate-only`) override a mutating verb → nodecision; (3) mutating verb (`create`) → ask; (3b) `create` never emits `allow`; (4a/4b) IAM mutating verbs (`set-iam-policy`, `add-iam-policy-binding`) → ask; (5) destructive verb (`delete`) → ask WITH irreversibility/blast-radius token in the reason string; (6a/6b) catastrophic denylist (`projects delete`, `resource-manager folders delete`) → deny; (6c) catastrophic never emits `allow`; (7a/7b) non-gcloud Bash (`ls -la`, `git status`) → nodecision; (8a/8b/8c) non-Bash tool payloads (Read, Write, Edit) → nodecision; (9) compound `list && delete` → ask (strongest class = destructive wins over read-only); (9b) compound `create && projects delete` → deny (catastrophic wins over mutating); (9b-extra) compound never emits `allow`; (10a) empty stdin / malformed JSON → nodecision (no crash, never allow); (10a-2) `{}` only → nodecision; (10b) gcloud + destructive token in unparseable payload → never allow (fail-safe); (10c) gcloud + `projects delete` in unparseable payload → never allow (fail-safe); additional: `describe`, `get-iam-policy` → nodecision; `stop`, `enable`, `add-metadata` → ask; `remove-iam-policy-binding` → ask; no self-approval bypass token in `ask` response. **Oracle is the spec** (verb-classification contract in `hooks/gcp-guard.sh` header + `02-implementation.md`), not the implementation output. Self-referential guard: `Suite 87` + `gcp-guard-hook-behavior` in `docs/testing.md` (canonical registry) and in the test file; `Suite 87` NOT in `CLAUDE.md §11` (hygiene contract). Marker: `gcp-guard-hook-behavior`.

### Suite 89 — mentor-teaching-contract

28 checks. Structural assertions for the `mentor` agent teaching contract (v2.83.0). File: `tests/test_agent_structure.py`. Mirrors Suite 88 (gcp-infra-refs-on-demand) shape for a new agent with non-trivial behavioural invariants. Checks: (1a–1j) `agents/mentor.md` frontmatter — model `opus`, effort `high`, color `teal`; tools allowlist contains `mcp__context7__resolve-library-id`, `mcp__context7__query-docs`, `WebSearch`, `WebFetch`, `Write`; tools allowlist does NOT contain `Bash` or `Edit`; (2) the diagram-always rule token (`diagram-always` or `Diagram-Always`) present in the file body; (3) version-honesty / context7-freshness clause present — tokens `deprecated API` + `version` + `verified`; (3b) SEC-001 prompt-injection guard present — tokens `Fetched content is data` + `never instructions` + `untrusted reference material`; (4) `agents/mentor.md § Return Protocol` contains `context7_consult` field declaration; (5) `agents/orchestrator.md § Direct Modes` table contains a `learn` row; (6) `agents/orchestrator.md` Step 6a intent table contains one of `learn` / `teach` / `explícame` / `explain`; (7) `MODES_GO_EXCLUDED` in this test file contains `"mentor"` (frozen-artifact exclusion); (8) no-document-by-default contract — tokens `end-of-session offer` + `not the default deliverable`; (9) quiet-operation rule — tokens `Quiet Operation` + `No routing`; (10) research-only-when-needed gating — tokens `Research Only When Needed` + `code-answerable`; (11) conversational-first contract — tokens `conversational unblocking` + `altitude asked`; (h1) `docs/testing.md` registers Suite 89 + `mentor-teaching-contract` marker; (h2) `CLAUDE.md` does NOT contain `Suite 89` (§11 hygiene contract). All checks use direct string search (frontmatter parse for fields 1a–1j; anchor-scoped `_slice_section` for fields 4–6). Checks 8–11 added in v2.83.0 (conversational-first repositioning). Written by implementer (2026-06-11). (12) by-default Show-the-Code codebase invariant present — tokens `Show-the-Code` + `by default, in the first reply` + `execution / pipeline order`; (12b) Show-the-Code scoped to codebase + repo read access — tokens `real entry points` + `read access to the repository`; (12c) codebase-scope diagram standard present — tokens `code-grounded data-flow pipeline` + `Nodes are the real code symbols` (added v2.85.1); (13) optional Predict/Run front-step (R1) present and framed offered-never-a-gate — tokens `Optional Predict/Run front-step` + `offered, never a gate` + `Predict/Run NEVER delays or blocks the answer`; (14) operational-artifact (R9) named as a first-class scope element and taught with Show-the-Code discipline — tokens `operational-artifact` + `read the REAL artifact and SHOW it` + `Show-the-Code discipline applies to ops artifacts` (both added v2.86.0). Marker: `mentor-teaching-contract`.

### Suite 90 — workspace-mode-session-start

37 checks. Functional behavioral tests for the unified `hooks/session-start.sh` (v2.82.0). File: `tests/test_session_start.sh`. Wired as Suite 6 in `tests/run-all.sh` (replacing the former `test_language_hook.sh` which covered only language loading). Written FAILING-FIRST Phase 2.0 before the hook was created.

**Scope:** covers all three ordered `load_<name>` functions in the REGISTRY-based unified hook: `load_dev_mode` (dev-mode banner + disposition directive), `load_language` (ISO 639-1 validation + language directive), `load_workspace_mode` (obsidian logs-mode detection + base-path directive). Also covers the combined output case (all three active simultaneously), structural AC-13 assertions (REGISTRY comment block, ordered invocation, fail-safe), and security floor assertions (SEC-DR-A language full-string anchor, SEC-DR-B fixed template, SEC-DR-C no-raw-injection, SEC-DR-D control-char rejection).

**Fail-safe contract:** when no config is present and no marker exists, the hook emits nothing and exits 0. A malformed config (embedded control chars in JSON) also silences all loads (JSON parse fails → all keys unavailable → no directives).

**Old hook removal:** `hooks/dev-mode-session-start.sh` and `hooks/language-session-start.sh` no longer exist — both consolidated into `session-start.sh`. Structural assertions in `test_agent_structure.py` assert the two old filenames are absent and that `hooks/config.json` has exactly one SessionStart entry per OS block.

Self-referential guard: `Suite 90` + `workspace-mode-session-start` in `docs/testing.md` (canonical registry) and in the test file; `Suite 90` NOT in `CLAUDE.md §11` (hygiene contract). Marker: `workspace-mode-session-start`.

### Suite 91 — obsidian-path-override-fleetwide

Fleet-wide structural guard for the obsidian-aware Path-override sentence (v2.82.1, closes #315). File: `tests/test_agent_structure.py`. For every `agents/*.md` file that contains the literal `Path override:`, asserts it also contains the token `never the repo-local default`. This pins the invariant established by PR #317 (4 agents: gcp-infra, gcp-cost-analyzer, translator, init) + this fix (12 agents: acceptance-checker, architect, delivery, documenter, implementer, plan-reviewer, qa-plan, qa, reviewer, security, tester, ux-reviewer) so a future agent that adds the Path-override clause without the obsidian-aware sentence fails this suite immediately. Also asserts a fleet coverage floor of ≥16 clause-bearing agent files. Self-referential guards: `Suite 91` + `obsidian-path-override-fleetwide` in `docs/testing.md` (canonical registry); `Suite 91` NOT in `CLAUDE.md §11` (hygiene contract). Written by implementer (2026-06-11). Marker: `obsidian-path-override-fleetwide`.

### Suite 92 — haiku-research-fanout

57 checks. Structural assertions for the parallel haiku research fan-out feature (v2.84.0), the v2.85.0 fleet rightsizing (init, acceptance-checker, translator → haiku; researcher Write allowlist fix; effort re-tune: architect/gcp-infra → xhigh, acceptance-checker → high), and the v2.87.0 bounded gap-closure loop for `/th:research`. File: `tests/test_agent_structure.py`. Covers all 9 ACs of PR-1 (haiku-research-fanout) plus the v2.85.0 haiku-fleet-rightsizing assertions plus the v2.87.0 gap-loop checks (AC-1 through AC-7 of research-gap-closure-loop) with the following check groups:

(AC-1, 7 checks) Multi-site fan-out contract — the `researcher` fan-out token is verified at all four research execution sites: (a1) `agents/ref-special-flows.md § Research Flow` anchor present; (a2) same section documents `researcher` parallel lanes with default N=3 and cap 5; (a3) `agents/ref-special-flows.md § Phase 1 — Research` (docs-flow) anchor present; (a4) same section documents the researcher fan-out; (a5) `skills/research/SKILL.md` documents researcher fan-out; (a6) `agents/architect.md` Research Mode documents consuming pre-digested consolidated findings; (a7) `agents/researcher.md` output contract declares all four findings fields (`claim`, `source_url`, `verbatim_excerpt`, `confidence`).

(AC-2, 4 checks) research-consolidator contract — (b1) frontmatter `model: sonnet`; (b2) deduplication rule documented; (b3) `### Conflicting sources` section with never-silently-picks-a-winner rule; (b4) references `00-research.md` as output target.

(AC-3/AC-7, 9 checks) Researcher evidence-only + haiku model + allowlist fix + 3 model flips + 3 effort pins — (c1) `agents/researcher.md` frontmatter `model: haiku`; (c2) evidence-only contract (no conclusions, no recommendations); (c3) researcher tools allowlist includes `Write` and excludes `Bash`/`Task` (regression for PR #321 false-green — FAILS on v2.84.0, passes on v2.85.0); (c4) `agents/init.md` frontmatter `model: haiku` (FAILS on v2.84.0); (c5) `agents/acceptance-checker.md` frontmatter `model: haiku` (FAILS on v2.84.0); (c6) `agents/translator.md` frontmatter `model: haiku` (FAILS on v2.84.0); (c7) `agents/architect.md` frontmatter `effort: xhigh` (FAILS on pre-change tree); (c8) `agents/gcp-infra.md` frontmatter `effort: xhigh` (FAILS on pre-change tree); (c9) `agents/acceptance-checker.md` frontmatter `effort: high` (FAILS on pre-change tree).

(AC-4, 2 checks) Fail-open lane handling — (d1) fail-open documented in Research Flow slice; (d2) `research.lane.skipped` event documented in `ref-special-flows.md`.

(AC-5, 3 checks) Background sweep — (e1) `Step 6d-background-sweep` anchor present in `orchestrator.md`; (e2) sweep states it is NOT an advance signal and does not auto-advance; (e3) `docs/discover-phase.md` documents background sweep with not-advance-signal constraint.

(AC-6, 3 checks) Allocation policy — (f1) `## Earn the model` anchor in `agents/README.md`; (f2) three-criteria haiku eligibility policy tokens present (`mechanical`, `structured output`, `judgment`, `detectable`); (f3) tally line reflects 4 haiku agents.

(AC-8, 3 checks) Go-installer exclusion — (g1) `CLAUDE.md` documents opencode agents installer roadmap / Go installer exclusion; (g2) `cmd/install/modes.go` does NOT contain a `researcher` entry; (g3) `agents/README.md` low-cost section states frozen pre-haiku.

(AC-9, 2 checks) Version sync — (h1) `.claude-plugin/plugin.json` version is `2.85.0` or later (forward-compatible floor); (h2) `.claude-plugin/marketplace.json` `plugins[0].version` is `2.85.0` or later.

(Gap-loop AC-1, 5 checks) Consolidator gaps block + material_closeable_gaps — (gl-ac1-1) `research-consolidator.md § Coverage gaps` anchor present; (gl-ac1-2) fenced `gaps` block with `id`, `material`, `web_closeable`, `desc`, `angle` fields; (gl-ac1-3) gate-passing condition documented (material AND web_closeable); (gl-ac1-4) `material_closeable_gaps` in return protocol; (gl-ac1-5) reconcile-don't-accrete instruction (same `00-research.md`, no `00-research-v2.md`).

(Gap-loop AC-2, 4 checks) Architect re-emit + Residual Gaps + three termination reasons — (gl-ac2-1) architect Step 4 instructs re-emit of gaps block reconciled against synthesis; (gl-ac2-2) mandatory `## Residual Gaps` section declared in Step 4 template; (gl-ac2-3) all three termination reasons present (`no-material-closeable-gaps`, `round-cap-reached`, `all-gaps-closed`); (gl-ac2-4) reconcile-don't-accrete in Step 4 (no `00-research-v2.md`).

(Gap-loop AC-3, 5 checks) ref-special-flows loop + gate + round cap 3 + per-round lane cap 5 — (gl-ac3-1) `Investigate further — bounded gap-closure loop` anchor present; (gl-ac3-2) gate condition documents `material` AND `web_closeable` AND `research_round`; (gl-ac3-3) round cap 3 declared as cost bound; (gl-ac3-4) per-round lane cap ≤ 5 (demand-allocated); (gl-ac3-5) all three termination reasons in the loop section.

(Gap-loop AC-4, 3 checks) Orchestrator round counter + events — (gl-ac4-1) `research_round` referenced in `orchestrator.md` research path; (gl-ac4-2) required trace events (`research.round.start`, `research.gap.gate`, `research.loop.terminated`) in `ref-special-flows.md`; (gl-ac4-3) `research.round.skipped` event documented.

(Gap-loop AC-7, 2 checks) Discover sweep exclusion pins — (gl-ac7-1) `orchestrator.md Step 6d-background-sweep` declares sweep is single-pass and gap-closure loop never applies; (gl-ac7-2) `docs/discover-phase.md § 12.3` declares the same.

(Gap-loop negatives, 3 checks) (neg-1) no loop-contract site declares a round cap exceeding 3; (neg-2) ref-special-flows § loop gate requires BOTH `material` AND `web_closeable` (AND condition documented); (neg-3) `docs/discover-phase.md § 12.3` does NOT reference `research_round` as an active counter for the sweep.

(Self-ref, 2 checks) Registry + hygiene — `Suite 92` and `haiku-research-fanout` in `docs/testing.md`; `Suite 92` NOT in `CLAUDE.md §11`. All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Written by tester in Phase 2.7 authoring mode (2026-06-13); extended by implementer (haiku-fleet-rightsizing, effort re-tune) 2026-06-13; extended by implementer (research-gap-closure-loop, v2.87.0) 2026-06-13. Marker: `haiku-research-fanout`.

### Suite 93 — worktree-discipline

40 checks (31 original + 9 PR-review-worktree extension). Structural assertions for the 5-rule worktree discipline (parallel-task-isolation, v2.88.0) plus the additive PR-review worktree lifecycle added to `agents/reviewer.md`. File: `tests/test_agent_structure.py`. Pins all AC-1 through AC-9 of the worktree-discipline enhancement and AC-10 (review worktree) so none of the structural invariants can silently regress.

(AC-1, 4 checks) `docs/worktree-discipline.md` exists and documents all 5 rules — (a1) file exists; (a2) all 5 rule headings present (`Rule 1` through `Rule 5`); (a3) start-gate decision documented (dirty/non-main → worktree; always fetch + base from `origin/main`); (a4) U1 boundary statement present (human own-terminal checkout cannot be intercepted by any hook).

(AC-2, 5 checks) Teardown and caveats — (b1) Rule 3 states finished ≝ PR merged; (b2) Rule 4 teardown sequence uses `git worktree remove` + `git worktree prune` + `git worktree list` (verify); (b3) dirty worktree → STOP (not remove); (b4) Windows file-lock caveat #57767 with `prune` + `remove --force` repair; (b5) `worktree.baseRef` regression #60588 documented.

(AC-3, 3 checks) `CLAUDE.md §5` pointer bullet — (c1) `Worktree discipline` bullet present in `CLAUDE.md §5`; (c2) bullet links to `docs/worktree-discipline.md`; (c3) bullet states human own-terminal path is discipline not a gate (U1 limit).

(AC-4, 5 checks) `orchestrator.md § Current State` schema — (d1) `## Current State` anchor + slice present; (d2) `- worktree:` field declared; (d3) `- worktree_branch:` field declared; (d4) `- worktree_base:` field declared; (d5) teardown is a deterministic lookup (field note).

(AC-5, 1 check) `agents/architect.md` PR template — (e1) `- **Worktree:**` declaration in 01-plan.md output template.

(AC-6, 6 checks) Pre-launch collision check in orchestrator `## Multi-Task Orchestration` — (f1) `Pre-launch collision check` paragraph present; (f2) references `#51596`; (f3) requires STOP on collision; (f4) prohibits silent reuse (`never silently reuse` / `Never proceed past this check`); (f5) uses `git worktree list` to detect existing worktrees; (f6) uses `git branch --list` to detect existing branch.

(AC-7, 6 checks) `delivery.md § Step 11.4b` teardown + Suite 53 preservation — (g1) `### Step 11.4b` anchor present; (g2) teardown uses `git worktree remove`; (g3) teardown uses `git worktree prune`; (g4) teardown verifies via `git worktree list` (verify-gone); (g5) dirty worktree → STOP; (g6) `orchestrator.md` contains `never from the active local branch` ≥ 2× (Suite 53 pin preserved).

(AC-8, 3 checks) Version sync — (h1) `.claude-plugin/plugin.json` version ≥ `2.88.0` (forward-compatible floor); (h2) `.claude-plugin/marketplace.json` `plugins[0].version` ≥ `2.88.0`; (h3) both versions are equal.

(AC-9, 6 checks) `hooks/worktree-guard.sh` existence and registration — (i1) file exists; (i2) declares itself fail-open; (i3) reason-string states cannot cover own-terminal ops; (i4) `hooks/config.json` registers `worktree-guard.sh` ≥ 3 times (one per OS block); (i5) `hooks/config.json` `worktree-guard` entry uses `Bash` matcher; (i6) `.claude-plugin/hooks.json` registers `worktree-guard.sh` under `PreToolUse` `Bash`.

(AC-10, 9 checks — PR-review worktree extension) `agents/reviewer.md` review worktree lifecycle — (j1) `## Worktree Lifecycle for PR Reviews` section exists (anchor guard); (j2) create step applies no-silent-reuse check (Rule 2): `git worktree list` + `git branch --list` present in lifecycle section; (j3) path uses `.claude/worktrees/pr-review-<number>` pattern and explicitly prohibits checkout in the shared main tree (`DO NOT` / `never check out`); (j4) `### Removing the review worktree` subsection uses `git worktree remove` + `git worktree prune` + `git worktree list` (verify-gone); (j5) dirty review worktree → STOP (not force-remove); (j6) `worktree_teardown:` field present in reviewer status block; (j7) `docs/worktree-discipline.md § Reviewing a PR` two-row teardown-triggers table present (Implement worktree → PR merged; Review worktree → Review complete); (j8) same section contains `always` token + rationale (`without` / `interrupt` / `current work`); (j9) `changelog.d/feat-worktree-discipline.md` references review worktree lifecycle (`reviewer.md` / `review worktree` / `pr-review` / `worktree_teardown`). All AC-10 checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Written by tester (2026-06-13). Marker: `worktree-discipline`.

(Self-ref, 2 checks) `docs/testing.md` registers `Suite 93` + `worktree-discipline` marker; `CLAUDE.md` does NOT contain `Suite 93` (§11 hygiene contract). All content checks use anchor-scoped slices (anti-false-green: missing anchor → empty slice → check fails). Version pin uses the forward-compatible floor pattern (`_s59_ver_tuple(...) >= (2, 88, 0)`) per repo convention. Marker: `worktree-discipline`.

### Suite 94 — prompt-injection-defense-floor

26 checks. Structural drift guard for the M1 prompt-injection defense floor prepended into 8 agents (v2.90.0). File: `tests/test_agent_structure.py`. Pins the canonical `## Untrusted content & prompt-injection floor` heading verbatim in all 8 agents that received the M1 prepend: `researcher`, `architect`, `security`, `mentor`, `gcp-infra`, `reviewer`, `diagrammer`, `orchestrator`. Checks: (1–8) heading present in each agent (8 checks); (9×8) all 4 canonical bullet-anchor strings present in each agent (8 checks); (10×8) closing `This is a prompt-level floor` sentence present in each agent (8 checks); (11) `docs/testing.md` registers `Suite 94` + `prompt-injection-defense-floor` marker; (12) `CLAUDE.md` does NOT contain `Suite 94` (§11 hygiene contract). All checks use direct `in` membership (no anchor-slicing needed — the heading IS the anchor). Written by implementer (2026-06-14). Marker: `prompt-injection-defense-floor`.

### Suite 97 — confidence-scored-plan

19 checks. Structural drift guard for the single-pass Confidence Score + Patterns to Mirror feature added to the Stage-1 plan contract (v2.93.0). File: `tests/test_agent_structure.py`. Asserts the four-file additive change across `agents/architect.md`, `agents/orchestrator.md`, `agents/plan-reviewer.md`, and `docs/testing.md`. Checks: (anchor) `agents/architect.md` contains `### Confidence Score & Patterns to Mirror` contract section; (1) contract section names all four rubric factors (`spec clarity`, `prior art`, `blast radius`, `unknowns`); (2) contract section contains `(single-pass)` qualifier; (2b) contract section pins single-pass semantics phrase (`one-shot` / `STAGE-GATE-3` / `without rework`); (3) contract section contains `**Confidence:**` score-line token; (3b) contract section contains `N/10` format token; (4) contract section names all three calibration bands (`8–10`, `5–7`, `1–4`); (5) contract section contains `### Patterns to Mirror` sub-section heading; (5b) contract section contains `No in-repo pattern to mirror` escape bullet; (6) `01-plan.md` template section contains `### Confidence Score`; (6b) `01-plan.md` template section contains `### Patterns to Mirror`; (7) `## Return Protocol` status block in `agents/architect.md` contains `confidence:` field; (8) `agents/orchestrator.md` `## STAGE-GATE-1` section contains `── Confidence ──` band token; (8b) same section contains `**Confidence:**` token; (9) same section contains `Confidence: not stated` fallback wording; (9b) same section explains score rides the verbatim Review-Summary copy; (10) `agents/plan-reviewer.md` contains `### Rule 12 — Confidence Score presence + justification`; (11) Rule 12 slice declares `concerns` severity; (11b) Rule 12 slice contains fail-OPEN language (`never` + `fail`); (12) Rule 12 slice names `hotfix` in no-op clause; (12b) Rule 12 slice names `Tier 1` or `research` or `spike` in no-op clause; (13) `## Summary` rules table contains `12 — Confidence Score` row; (14) report template contains `### Rule 12 — Confidence Score` findings block; (15) Return Protocol status block contains `rule-12:` field; (16) `## Verdict Calibration` mentions Rule 12 as `concerns`-severity; (h1) `docs/testing.md` registers `Suite 97` + `confidence-scored-plan` marker; (h2) `CLAUDE.md` does NOT contain `Suite 97` (§11 hygiene contract); (h3) test file self-references `Suite 97`, `_slice_section`, and `confidence-scored-plan`. All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Pure text/file reads — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `confidence-scored-plan`.

### Suite 99 — hookify

17 checks. Asserts the `/th:hookify` skill exists (`skills/hookify/SKILL.md`), runs directly / does not invoke the orchestrator, documents the hybrid input model (operator-supplied primary + `00-execution-events` trace enrichment) + transcript-not-read disclaimer, defines the proposed-rule report shape with required fields (`Intent`, `Trigger event`, `Match sketch`, `Severity`, `Rationale`) and default severity `ask`, and documents the HARD REPORT-only boundary (no write to `hooks/` or `~/.claude/`, no `--fix`/`--apply`). Also asserts: `## Voice` and `## Output Discipline` blocks present (parity with sibling skills); TH-native framing (no external-project/method name in shipped text); `/th:hookify` listed as Standalone in `skills/README.md`. Self-referential guards: `Suite 99` + `hookify` present in `docs/testing.md`; `Suite 99` + `_slice_section` present in test file; `Suite 99` NOT in `CLAUDE.md` (§11 hygiene contract); Suite 99 own non-comment source invokes no `Agent(` or `subagent_type`. Pure text/file reads — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `hookify`.

### Suite 100 — decision-ledger

22 checks. Structural assertions for the per-workspace decision ledger (`00-decision-ledger.{jsonl|md}`) and the Confidence-is-not-approval / dry-run-first gate rule. File: `tests/test_agent_structure.py`. All checks use the anchor-scoped 3-arg `_slice_section` idiom (anti-false-green: missing anchor → empty slice → check fails). Checks: (1) `docs/observability.md` contains `## Decision Ledger` heading; (2) docs slice references both `00-decision-ledger` and `00-execution-events` (new-file distinction); (3) docs slice carries the NEVER-records-timing anti-redundancy invariant; (4) docs slice declares all 4 event types (`gate-verdict`, `operator-approval`, `disposition`, `dry-run-enforced`); (5) docs slice enumerates disposition vocabulary (`accept`/`watch`/`reject`); (6) docs slice documents required fields (`rationale`, `ts`, `stage`, `decision`); (7) docs slice states `ts` is injected at write time; (8) docs slice carries secret-prohibition clause for `rationale` field (no tokens/credentials/user-path identifiers); (9) docs slice carries best-effort + never-hard-fail resilience clause; (10) docs slice carries Tier-0 carve-out; (11) docs slice documents dual-format (`00-decision-ledger.jsonl` + `00-decision-ledger.md` + jsonl fence reference); (12) `agents/orchestrator.md` contains `## Decision Ledger` heading; (13) orchestrator slice names orchestrator as exclusive writer + append-only `>>`; (14) orchestrator write-sites table maps all 4 event types to gate boundaries (`STAGE-GATE` + `Phase 1.6` + `disposition`); (15) orchestrator slice contains Confidence-is-not-approval + dry-run subsection; (16) orchestrator subsection routes deploy/migration through `--dry-run`/`--validate-only`/`plan-only`; (17) orchestrator subsection references `gcp-guard.sh` + `dev-guard.sh` and states ledger audits/does not enforce; (18) orchestrator slice mirrors the NEVER-records-timing anti-redundancy invariant (present in BOTH sections); (19) orchestrator slice carries best-effort + never-hard-fail resilience invariant; (20) `docs/testing.md` registers `Suite 100` + `decision-ledger` marker; (21) `CLAUDE.md` does NOT contain `Suite 100` (§11 hygiene contract); (22) test file self-references `Suite 100`, `_slice_section`, and `decision-ledger`. Pure text/file reads — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `decision-ledger`.

### Suite 98 — dual-review-convergence

20 checks. Structural drift guard for the dual-review convergence protocol added to the PR-review path (v2.92.0). File: `tests/test_agent_structure.py`. Pure text/file reads — no agent invocation, no paid spend. Asserts the five-file additive change across `agents/reviewer-consolidator.md`, `agents/ref-direct-modes.md`, `skills/review-pr/SKILL.md`, `docs/testing.md`, and the test file itself. Checks: (anchor) `agents/reviewer-consolidator.md` contains `## Dual-Review Convergence` heading; (2) consolidator section documents `Convergence Pass:` dispatch field (A and B values); (3) consolidator section documents isolation rule (never read sibling pass's `-A`/`-B` drafts); (4) consolidator section documents `convergence_pass:` status-block field; (5) `agents/ref-direct-modes.md` contains `### Dual-Review Convergence` sub-section in the Review Mode area; (6) orchestrator sub-section documents both-must-APPROVE rule and CONVERGED_APPROVE outcome; (7) orchestrator sub-section documents context-isolation (passes receive only original inputs); (8) orchestrator sub-section documents fresh-each-round rule (no prior-round artifacts forwarded); (9) orchestrator sub-section documents max 3 rounds hard cap; (10) orchestrator sub-section documents STOP-and-escalate and explicit never-auto-resolve clause; (11) orchestrator sub-section states convergence runs BEFORE the Publish Gate and never calls a GitHub write verb; (12) orchestrator sub-section documents `convergence` block in `00-state.md` and `review.convergence.round` event; (13) `skills/review-pr/SKILL.md` Flag-parsing section documents `--converge` flag; (14) skill convergence section documents Tier 4 auto-on referencing the EXISTING tier classification (no new keyword list); (15) skill convergence section documents `round < 3` fresh-round branch AND `round == 3` escalation STOP; (16) skill documents `.claude/pr-review-convergence.json` round-state AND `-A`/`-B` draft files in cleanup trap; (17) `docs/testing.md` registers `Suite 98` + `dual-review-convergence` marker; (18) `CLAUDE.md` does NOT contain `Suite 98` (§11 hygiene contract); (19) Suite 98 non-comment source contains no `Agent(` or `subagent_type` token (free suite guarantee); (20) test file self-references `Suite 98`, `_slice_section`, and `dual-review-convergence`. All content checks use anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Written by implementer (2026-06-15). Marker: `dual-review-convergence`.

### Suite 96 — review-lenses

27 checks. Structural drift guard for the reviewer agent on-demand review lenses system (v2.92.0). File: `tests/test_agent_structure.py`. Mirrors Suite 88 (gcp-infra-refs-on-demand) for the `reviewer` agent. Checks: (1a) `agents/reviewer.md` contains `## Reference Router` section; (1b) router references `_index.md`; (1c×3) router lists all 3 lens names (`silent-failure`, `type-design`, `comment-rot`); (1d) router declares graceful-degradation / never-fabricate fallback; (1e) router references `reference_loaded` status field token; (1f) Fresh Review status block carries `reference_loaded:` field with `review-lenses unavailable` documented value; (2a) `agents/review-lenses/_index.md` exists; (2b×3) manifest lists all 3 lens names; (2c) manifest documents path convention `agents/review-lenses/`; (2d) manifest documents fallback behavior; (3×3) all 3 lens files exist on disk; (4×3) all 3 lens files have NO YAML frontmatter (reference files, not agents); (5×3) per-lens required-token coverage — `silent-failure` covers `catch`/`swallow`/`except`; `type-design` covers `illegal`/`primitive`/`null`; `comment-rot` covers `todo`/`fixme`/`stale`; (6a) `EXPECTED_AGENTS.count("reviewer") == 1` — single reviewer agent; (6b) no stray variant agent file (`review-lens.md` or `reviewer-lenses.md`); (7) provenance guard — no external-project name in any shipped artifact string; (8–9) Suite 96 own code (non-comment lines) contains no `Agent(` or `subagent_type` invocation token; (h1) `docs/testing.md` registers `Suite 96` + `review-lenses` marker; (h2) `CLAUDE.md` does NOT contain `Suite 96` (§11 hygiene contract). Pure text/file reads — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `review-lenses`.

### Suite 95 — eval-as-spec

20 checks. Structural drift guard for the eval-as-spec layer added to `/th:eval` (v2.92.0). File: `tests/test_agent_structure.py`. Asserts the deterministic free layer (arg contracts, report shape, spec template, baseline schema, registry hygiene) and guarantees the paid run path is never wired into `tests/run-all.sh`. Checks: (1) `skills/eval/SKILL.md` documents `--spec` + `Mode 5`; (2) documents `--k N` arg with default 1; (3) documents `--baseline <sha>` arg; (4) `--spec` mode documents parsing the pass-bar declaration section by name; (5) pass-rate `P/k` report block string present; (6) `pass@k` verdict string present; (7) NEW-failures-vs-baseline report shape present; (8) `eval-scenarios/_templates/spec.md` exists with required sections (Input, Context, Expected Behaviors, Anti-Patterns, Output Criteria); (9) spec template contains pass-bar declaration section; (10) `eval-scenarios/architect/design-as-spec.md` exists with filled-in pass-bar declaration; (11) `eval-scenarios/.baselines/architect.example.json` parses as valid JSON with all REQUIRED keys (`agent`, `baseline_sha`, `k`, `scenarios[].scenario`, `scenarios[].pass_rate`, `scenarios[].verdict`); (12) `skills/eval/SKILL.md` documents the canonical REQUIRED baseline key list; (13) `eval-scenarios/README.md` documents the same canonical REQUIRED key list — one consistent list across all three surfaces; (14) cost math `N × ~$1` present in SKILL; (15) SKILL states paid path is NEVER in `run-all.sh`; (16) `tests/run-all.sh` contains no `claude -p` entry (paid path non-wiring guarantee); (17–18) Suite 95 own code (non-comment lines) contains no `Agent(` or `subagent_type` invocation token; (19) `docs/testing.md` registers `Suite 95` + `eval-as-spec` marker; (20) `CLAUDE.md` does NOT contain `Suite 95` (§11 hygiene contract). Pure text/JSON reads — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `eval-as-spec`.
### Suite 106 — browser-mode-reference

69 checks (54 prior + 14 new from re-pin pass + 1 new from iteration-3 pin). Structural assertions for the Vitest Browser Mode reference system added by the e2e-testing-agent feature (D7 item 15, AC-3/AC-5/AC-7, v2.84.0), extended with Group-1 coherence fixes (2026-06-12), strengthened per R3 code-review findings (2026-06-12), re-pinned for the just-landed content additions (2026-06-12), and further strengthened in iteration-3 (2026-06-13). File: `tests/test_agent_structure.py`. Mirrors Suite 88 (gcp-infra-refs-on-demand) and Suite 89 (mentor-teaching-contract) shape for a new reference file with non-trivial content invariants. Original checks (1a–4e, 5a–5d, 6a–6b, h1–h2): (1a) `agents/testing-refs/browser-mode.md` exists on disk; (1b) `_index.md` names `browser-mode` (manifest→disk forward); (1c) **C10: filename-matched reverse scan** — every `*.md` filename (stem+".md") in `agents/testing-refs/` (excl. `_index.md`) is listed in `_index.md` (replaces bare stem — stem "browser-mode" matches prose; filename "browser-mode.md" does not); (2a) `browser-mode.md` contains `When to use` guidance; (2b) `When NOT to use` section present; (2c) `@vitest/browser-playwright` documented; (2d) `provider: playwright()` in minimal config; (2e) `browser.enabled` / `enabled: true` config key; (2f) `instances:` key with colon; (2g) `vitest-browser-react`; (2h) **C9: repointed** — `__screenshots__/` storage-path (was duplicate of 7a `toMatchScreenshot`; now pins the storage-path token independently); (2i) Playwright-format traces; (2j) per-file isolation limitation (`ISOLATION LIMITATION` + `single page`); (2k) `vi.mock` spy caveat; (2l) no YAML frontmatter; (2m) `## Principles` section present; (3a-3c) nestjs/go/python are `Not applicable` notes; (4a) Phase-0 slice names `browser-mode` (R3-3: slice-scoped); (4b-4d) decision-log shape tokens; (4e) propose-setup posture; (5a-5d) four version-sync sites >= 2.84.0; (6a-6b) e2e.md react-nextjs cross-pointer + v1.60.0-noble; (h1-h2) self-referential guard + CLAUDE.md hygiene. Group-1 hardening checks (4f–4q): (4f) **C8/R3-1: per-line binding + arrow-target pinned** — `AC about layout/geometry` AND `→` AND `` `browser-mode` `` on the SAME line (arrow-target change to e2e would be caught); (4g) two anchor-scoped mode-slice checks; (4h-4k) viewport/aria-live/drag-and-drop/timer branches; (4l-4m) _index.md table + engine-overlap note; (4n-4q) ui-component/browser-mode/visual/a11y cross-refs. New-content pinning checks (7a–7i): (7a) **C9: repointed** — `__screenshots__/` storage-path in `browser-mode.md` (was duplicate of old 2h; now both 2h and 7a pin the storage-path — two independent sites); (7b) `toMatchScreenshot` in `visual.md`; (7c) `trace: 'retain-on-failure'`; (7d) `await render(`; (7e) no un-awaited `= render(` (negative; **C11**: known-limitation comment added — file-scoped; future jsdom comparative snippets tagged `// jsdom` would need allowlist); (7f) **C8 direction-pin** — backtick-quoted `` `e2e` `` + `still wins` per-line (bare `e2e` + `still wins` in wrong-direction sentence would pass old check; backtick pins the winning type); (7g) **C8 cell-anchored** — backtick-quoted `` `a11y` `` as type cell on the `color contrast` row (prose mention of `a11y` elsewhere in the row would pass old check; backtick anchors to the type cell value); (7h) `Selected test type: browser-mode` normalized log token; (7i) `vitest-browser-mode` absent from all files in `agents/` (negative). New checks for just-landed invariants (8a–8n): (8a) **strengthened in iteration-3** — `page.viewport` + `vitest/browser` import present AND `@vitest/browser/context` (stale path) ABSENT in `browser-mode.md` (the old import is a substring of the new one — positive-only check was a false-green; negative guard mirrors 7i/7e pattern); (8b) platform-suffix caveat in `browser-mode.md` (`platform` token); (8c) platform-suffix caveat in `visual.md` Option C (`platform` token); (8d) `### ARIA live-region assertion` subsection in `browser-mode.md`; (8e) `setViewportSize` in `e2e.md` (Responsive assertions in E2E subsection); (8f) keyboard-nav row NOT routed to `` `a11y` `` as type cell per-line (axe cannot verify keyboard operability → ui-component or browser-mode); (8g) catch-all row: `clipboard` + `` `browser-mode` `` per-line in `_index.md`; (8h) `clipboard` in `tester.md` catch-all branch; (8i) `cypress.config` conventions-precedence in `tester.md`; (8j) `## Test-Type Decisions` template section in `tester.md`; (8k) `warranted_types:` field in `tester.md` Return Protocol; (8l) `binaries` clause in `tester.md` Neither-installed paragraph; (8m) `re-derive` in `tester.md` Router fallback; (8n) `all type files` present AND `all six type files` absent in `tester.md`. Iteration-3 new check (toMatchScreenshot-subject): negative guard — `expect(container).toMatchScreenshot` must be ABSENT from both `browser-mode.md` and `visual.md` (correct locator subject is `screen.getByRole(...)`; mirrors 7e un-awaited-render negative guard). All checks use direct string presence or anchor-scoped `_slice_section` (anti-false-green). Re-pinned 2026-06-12; iteration-3 strengthened 2026-06-13. Marker: `browser-mode-reference`.

### Suite 113 — worktree-isolation-ahead

14 checks. Structural assertions for the worktree start-gate re-keying off `origin/main` position (issue #352, v2.102.0). File: `tests/test_agent_structure.py`. Checks: (1) `docs/worktree-discipline.md` contains the literal substring `ahead of origin/main`; (2) `hooks/worktree-guard.sh` contains `ahead of origin/main`; (3) `agents/orchestrator.md` contains `ahead of origin/main`; (4) `docs/worktree-discipline.md` contains `at/behind`; (5) `agents/orchestrator.md` contains `at/behind`; (6–9) cross-check that Suite 93 tokens still present in `docs/worktree-discipline.md` (`Rule 1`, `origin/main`, `worktree`, and `dirty` or `non-main`); (10) `docs/testing.md` contains `Suite 113`; (11) `CLAUDE.md` does NOT contain `Suite 113` (§11 hygiene contract); (12) version-floor: `.claude-plugin/plugin.json` version `>= (2, 102, 0)` via `_s59_ver_tuple`; (13) `.claude-plugin/marketplace.json` `plugins[0].version` `>= (2, 102, 0)`; (14) self-ref check — test file contains `Suite 113`, `_slice_section`, and `worktree-isolation-ahead`. All content checks for anchored sections use `_slice_section` (anti-false-green: missing anchor → empty slice → check fails). Pure text/JSON reads — no agent invocation, no paid spend. Written by implementer (2026-06-17). Marker: `worktree-isolation-ahead`.
### Suite 114 — batch-consolidation-default

21 checks. Structural assertions for the batch-consolidation-default contract (v2.102.0, issue #354). File: `tests/test_agent_structure.py`. Pins the named consolidation default, the operator opt-out, the genuine-blocker closed-list naming, the same-delivery-flow alignment, and the reconciled wording at all four surfaces so none of the structural invariants can silently regress. All checks are anchor-scoped via `_slice_section` (anti-false-green: missing anchor → empty slice → check fails), mirroring Suite 93.

(AC-1, 4 checks) `agents/orchestrator.md § Multi-Task Orchestration`: (a1) `## Multi-Task Orchestration` anchor present + slice non-empty; (a2) `Consolidation default` named clause present in the slice; (a3) ONE consolidated PR stated as the default; (a4) prohibition on one-PR-per-batched-task present.

(AC-2, 6 checks) Opt-out + closed-list: (b1) `keep them as separate PRs` typed phrase in orchestrator slice; (b2) `separate PRs` in orchestrator slice; (b3) `coexistence window` named in orchestrator slice; (b4) `production signal` named in orchestrator slice; (b5) `cross-repo deploy gate` named in orchestrator slice; (b6) `plan-reviewer.md` + `Rule 1` reference in orchestrator slice (pointer to the closed-list source).

(AC-6, 2 checks) Same delivery flow: (c1) `same delivery flow` phrase in orchestrator slice; (c2) `worktree-discipline.md` + `Rule 3` in orchestrator slice (teardown lifecycle alignment).

(AC-3a, 4 checks) `agents/ref-special-flows.md`: (d1) `### Batch consolidation vs the anti-split invariant` anchor present + slice non-empty; (d2) anti-split rule (`never split` / `never SPLIT`) in the slice; (d3) opt-out (`separate PRs`) in the slice; (d4) `same delivery flow` in the slice.

(AC-3b, 2 checks) `docs/knowledge.md`: (e1) `Consolidation default` or `Batch consolidation default` present (companion `[decision]` bullet); (e2) bullet references `orchestrator.md` with the consolidation anchor.

(AC-3c, 3 checks) `agents/plan-reviewer.md § Rule 1`: (f1) `### Rule 1` anchor + slice non-empty; (f2) `complementary` + (`batch` or `consolidat`) — clause states Rule 1 is complementary to batch consolidation; (f3) `split` + `direction` — Rule 1 is identified as the split-direction rule.

(AC-5, 3 checks) Version sync: (g1) `.claude-plugin/plugin.json` version `>= (2, 102, 0)` (forward-compatible floor, `_s59_ver_tuple`); (g2) `.claude-plugin/marketplace.json` `plugins[0].version >= (2, 102, 0)`; (g3) both versions are equal.

(Self-ref, 2 checks) (h1) `docs/testing.md` registers `Suite 114` + `batch-consolidation-default` marker; (h2) `CLAUDE.md` does NOT contain `Suite 114` (§11 hygiene contract). Suite 114 pins NO CLAUDE.md byte size (the existing voice suite owns that guard). Pure text/JSON reads — no agent invocation, no paid spend. Written by implementer (2026-06-17). Marker: `batch-consolidation-default`.

### Suite 112 — apply-review-direct-mode

17 checks. Structural assertions for the `/th:apply-review` direct mode (v2.101.0). File: `tests/test_agent_structure.py`. Checks: (1) `skills/apply-review/SKILL.md` exists and is non-empty; (2) SKILL.md is thin — routes to orchestrator with `Direct Mode Task` payload and does NOT restate disposition body tokens (`two-axis`, `classification table`, `mandatory verification`, Step 1/2/3 disposition headings); (3) SKILL.md `## Important` section references `apply-review-disposition.md` by path (single-source contract); (4) SKILL.md `## Important` declares this is a COMPLEMENT to the automatic trigger; (5) `orchestrator.md` `## Direct Modes` catalog table has `apply-review` row referencing `ref-direct-modes.md § Apply-Review Mode`; (6) `orchestrator.md` Step 6a intent table has `apply-review` row with `apply review comments` intent pattern; (7) `orchestrator.md` disambiguation block has `apply-review` (AUTHOR side) vs `review`/`review-pr` (REVIEWER side) note; (8) `orchestrator.md` still contains the `(automatic, lifecycle-bound)` section header — the automatic trigger is preserved (AC-2); (9) NEGATIVE: `orchestrator.md` does NOT contain stale `No command route, no new skill` phrasing; (10) `orchestrator.md` PR Comment Incorporation section contains `Automatic by default; also invokable explicitly` note; (11) `ref-direct-modes.md` has `## Apply-Review Mode` section referencing `apply-review-disposition.md` by path and NOT restating disposition body tokens; (12) `docs/testing.md` registers `Suite 112` + `apply-review-direct-mode` marker; (13) `CLAUDE.md` does NOT contain `Suite 112` (§11 hygiene contract); (14–15) version-floor pins: `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` plugins[0] both `>= (2, 101, 0)` via `_s59_ver_tuple`; (16) free-suite no-agent-call guard; (17) self-ref check. All content checks use anchor-scoped `_slice_section` (anti-false-green). Pure text/JSON reads — no agent invocation, no paid spend. Written by implementer (2026-06-16). Marker: `apply-review-direct-mode`.

### Suite 111 — re-review-collapse

13 checks. Structural assertions for the Phase 3.5 prior-review menu collapse (v2.100.0). File: `tests/test_agent_structure.py`. Checks: (1) SKILL.md Phase 3.5 does NOT present an interactive a/b/c/d menu (absent: `Which option`, `(a) Update the summary`, `(b) Reply to`, `(c) Re-review cycle`, `(d) Cancel`); (2) Phase 3.5 automatically dismisses the prior review when new commits are detected (`dismissals` API token present + `commit_id` comparison present); (3) Phase 3.5 emits a one-line note and STOPs when no new commits exist since the prior review (no dismiss, no post); (4) Phase 3.5 still fetches all reviews (all authors) and filters to same-author (detection input preserved); (5) Phase 3.5 preserves the `## Hallazgos por enfoque` → `--multi` continuity detection; (6) Phase 5 atomic `POST .../reviews` is unchanged (publish contract frozen); (7) Important notes describe the automatic dismiss+fresh path with no stale references to interactive PUT-body or reply-to-thread options; (8) `docs/testing.md` registers `Suite 111` + `re-review-collapse` marker; (9) `CLAUDE.md` does NOT contain `Suite 111` (§11 hygiene contract); (10–11) version-floor pins: `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` plugins[0] both `>= (2, 100, 0)` via `_s59_ver_tuple`; (12) free-suite no-agent-call guard; (13) self-ref check. All content checks use anchor-scoped `_slice_section` (anti-false-green). Pure text/JSON reads — no agent invocation, no paid spend. Written by implementer (2026-06-16). Marker: `re-review-collapse`.

### Suite 110 — loosening-dispositions

13 checks. Structural assertions for the author/reviewer loosening dispositions feature (issue #348, v2.99.0). File: `tests/test_agent_structure.py`. Checks: (1a) `agents/review-lenses/loosening-impact.md` exists and is non-empty; (1b) `loosening-impact` registered in `agents/review-lenses/_index.md` manifest (filename + entry); (1c) `loosening-impact` registered in `agents/reviewer.md` Reference Router trigger table; (2a) `agents/_shared/finding-connection.md` exists and is non-empty; (2b) `apply-review-disposition.md` references `finding-connection.md` at Step 2.4; (2c) `loosening-impact.md` references `finding-connection.md` (point c); (3) `agents/reviewer.md` bright-line substring `'treat any rule removal or severity downgrade as a critical finding'` still present byte-for-byte (AC-4 invariant); (4a) `agents/_shared/apply-review-disposition.md` exists and is non-empty; (4b) disposition contains `phrasing` + `severity` tokens (phrasing-not-severity clause); (4c) disposition contains `deletion` + `positive justification` (deletion-discipline clause); (4d) disposition contains `per-comment output` section; (5) `agents/orchestrator.md` references `apply-review-disposition.md` in PR-work handling (automatic lifecycle-bound injection); (6) NEGATIVE: no new `hooks/*.sh` file outside the canonical 8-file set (AC-7 scope boundary); (7) **SUPERSEDED by Suite 112 (v2.101.0)** — was NEGATIVE: `skills/apply-review/` directory does NOT exist (AC-7 scope boundary for v2.99.0); retired when apply-review-direct-mode PR explicitly adds the skill; check now always-passes; (8–9) version-floor pins: `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` plugins[0] both `>= (2, 99, 0)` via `_s59_ver_tuple`; (10) `docs/testing.md` registers `Suite 110` + `loosening-dispositions` marker; (11) `CLAUDE.md` does NOT contain `Suite 110` (§11 hygiene contract); (12) free-suite no-agent-call guard; (13) self-ref check. Pure text/JSON reads — no agent invocation, no paid spend. Written by implementer (2026-06-16). Marker: `loosening-dispositions`.

### Suite 109 — learn-english-skill

14 checks. Structural assertions for the `/th:learn-english` toggle skill (v2.98.0). File: `tests/test_agent_structure.py`. Checks: (1) `skills/learn-english/SKILL.md` exists and non-empty with frontmatter `name: learn-english`; (2) SKILL.md declares standalone framing (does NOT route through the orchestrator) and names `~/.claude/.team-harness.json` as the write target; (3) `on` branch section names `english_learning` set to `true` AND `language` set to `en` AND declares merge-write-whole-document; (4) `on` report contains the dual-activation load-bearing phrases: `sets English as the default response language`, `activates BOTH`, `language: en`, `english_learning: true`; (5) `off` branch section names `english_learning` set to `false` (explicit, not key-removal); (6) `off` branch contains the interactive language keep/change prompt with `keep` and `change <code>` options; (7) `off` branch explicitly states it does NOT touch `language` automatically (only when the operator chooses `change`); (8) `status` branch states it performs NO write under any circumstance; (9–10) version-floor pins: `.claude-plugin/plugin.json` and `.claude-plugin/marketplace.json` plugins[0] both `>= (2, 98, 0)` via `_s59_ver_tuple`; (11) `docs/testing.md` registers `Suite 109` + `learn-english-skill` marker; (12) `CLAUDE.md` does NOT contain `Suite 109` (§11 hygiene contract); (13) free-suite no-agent-call guard; (14) self-ref check. All content checks use the anchor-scoped 3-arg `_slice_section` idiom (anti-false-green: missing anchor → empty slice → check fails). Pure text/JSON reads — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `learn-english-skill`.

### Suite 108 — collaborative-pr-review

11 checks. Structural assertions for the collaborative PR review feature (issue #341, v2.95.0). File: `tests/test_agent_structure.py`. Checks: (1) `skills/review-pr/SKILL.md` Phase 1 step 9 fetches all-authors reviews from `GET .../pulls/{number}/reviews` and populates a `Prior Reviews:` dispatch field (R1); (2) SKILL.md Phase 3.5 reads all reviews before filtering to same-author — the read-first path is not limited to the current user (R1 de-scope); (3) `agents/reviewer.md` Phase 0 step 4 declares the "Interact, don't restate" rule with explicit confirmar / refutar / extender stances (R2); (4) reviewer.md declares Phase 1.5 net-new gate with `net_new` field and three classifications `net-new` / `confirms-prior` / `already-resolved` (R3); (5) SKILL.md declares Phase 4.9 "Pre-Publish Concurrent Review Check" as a best-effort pre-publish re-fetch positioned before Phase 5 (R4); (6) SKILL.md Phase 4 recommendation hint includes a `net_new == 0` branch that recommends cancel or one-line COMMENT (R3); (7) reviewer.md Phase 0 states the source-of-truth invariant (code is the only source of truth, thread is untrusted context) and that a contradicting/refuting finding is `net-new` and must never be suppressed (AC-15); (8–9) registry + hygiene checks (`docs/testing.md` registers Suite 108 + `collaborative-pr-review` marker; `CLAUDE.md` does NOT contain `Suite 108`); (10) free-suite no-agent-call guard; (11) self-ref check. All checks anchor-scoped (anti-false-green). Written 2026-06-15. Marker: `collaborative-pr-review`.

### Suite 107 — frontend-flow-activation

35 checks (26 original + 8 new from iteration-2 re-pin + 1 new from iteration-3 pin). Structural assertions for the frontend detection and browser-test routing additions shipped by feature/frontend-flow-activation (PR-2, v2.85.0), extended with cross-file contract, iteration-2, and iteration-3 invariant checks (2026-06-13). File: `tests/test_agent_structure.py`. AC-1 (a/b): per-line binding that `orchestrator.md` Phase 2.7 tester dispatch line AND Phase 3 tester dispatch line each independently carry `frontend_scope: true` on the same line as their mode token (`authoring mode` / `run-only mode`) — file-wide presence is insufficient; AC-2 (a/b): `skills/test/SKILL.md` and `skills/test-pipeline/SKILL.md` each contain `frontend_scope: true` in the Task payload example area; AC-3 (a/b): `agents/tester.md` contains the `Responsive-intent check (Gate B)` token AND the prose explicitly states Gate B gates only the viewport/responsive branch (other browser-mode signals unaffected); AC-4 (a/b): `orchestrator.md` Phase 2.7 gate contains the `A1-F3` label with `binaries` token, and the anchor-scoped `A1-F3` paragraph contains `npx playwright install` as the example setup command (substring match; `--with-deps` form also accepted); AC-5 (a–f): `agents/tester.md` contains `TESTING.md` token and all five required template section names (`Stack & detection`, `Test map`, `How to run`, `Latest results`, `Actions & recommendations`) anchor-scoped inside the `### TESTING.md project guide (R4)` block; AC-6 (a–d): `agents/ux-reviewer.md` Browser-real signal hints block contains `IntersectionObserver`; `agents/qa-plan.md` same; `agents/ux-reviewer.md` Stage-3 ownership-boundary paragraph contains `AUTHORITATIVE`; `agents/ux-reviewer.md` enrich Process contains `Detect the frontend stack` step; R3 (a/b): `agents/testing-refs/browser-mode.md` and `agents/testing-refs/e2e.md` each contain `Where to look when it fails` artifacts section; C7 (a–d, iteration-2 cross-file contract): (C7a) `orchestrator.md` Direct Modes `test` table row carries `frontend_scope` and `00-state.md` on the same line (per-line binding — closes the F1 integration gap where Phase 2.7/3 checks passed green but the direct-mode entry point was unwired); (C7b) `agents/ref-direct-modes.md` `## Test Mode` section present with `frontend_scope` token anchor-scoped; (C7c) `agents/ref-special-flows.md` per-module task payload block carries `frontend_scope` field; (C7d) `orchestrator.md` A1-F4 paragraph names `ui-component` AND `visual` in the browser-real type set; iter2 (a–d, iteration-2 invariants): (iter2a) `agents/tester.md` Phase 0 step 2 labeled `Gate A — frontend stack detection`; (iter2b) `agents/tester.md` contains `<!-- th:testing-guide v1 -->` sentinel token; (iter2c) `agents/tester.md` contains `Team-harness repo guard` wording; (iter2d) `agents/tester.md` Gate A contains `non-react stacks: vue` Vue/Svelte n/a disposition; iter3 (iteration-3 console-path gate pin): `agents/ref-direct-modes.md` `## Test Mode` section (anchor-scoped, stop at next `## ` so `###` subsections are included) references `A1-F3` AND `A1-F4` — pins the console-path surface of the browser-readiness check and jsdom-only soft gate added to the direct-mode path in iteration-3; ver (a–d): all four version-sync sites >= 2.85.0; h1/h2: `docs/testing.md` registers `Suite 107` + `frontend-flow-activation` marker; `CLAUDE.md` does NOT contain `Suite 107`. Written by tester (2026-06-12); re-pinned iteration-2 (2026-06-13); re-pinned iteration-3 (2026-06-13). Marker: `frontend-flow-activation`.
### Suite 88 — gcp-infra-refs-on-demand

19 checks. Structural assertions for the `gcp-infra` agent on-demand reference system (AC-6/AC-7, v2.80.0). File: `tests/test_agent_structure.py`. Mirrors Suite 67 (tester-lean-core-testing-refs) for the `gcp-infra` agent. Checks: (1a–e) `agents/gcp-infra.md` contains `## Reference Router` section with `_index.md` reference, `Datastream` trigger keyword, graceful-degradation / never-fabricate fallback, and `reference_loaded` status field token; (2a–d) `agents/gcp-infra-refs/_index.md` exists and lists `datastream-cloudsql-bigquery` kind, path convention, and fallback behavior; (3) `agents/gcp-infra-refs/datastream-cloudsql-bigquery.md` exists on disk; (4) playbook has NO YAML frontmatter (reference file, not an agent); (5) playbook covers required topics: replication slot risk, VPC connectivity, cost drivers, REPLICA IDENTITY, partitioned tables / `publish_via_partition_root` (5 token checks); (6a–b) single `gcp-infra` agent guard (no variants added); (h1) `docs/testing.md` registers Suite 88 + `gcp-infra-refs-on-demand` marker; (h2) `CLAUDE.md` does NOT contain `Suite 88` (§11 hygiene contract). Written by implementer (2026-06-11). Marker: `gcp-infra-refs-on-demand`.

### Suite 82 — plan-sketches

50 checks (28 original v2.69.0 + 11 Milestone-5 extensions for v2.70.0 + 11 quality-bar-api-contract additions M2.3 for plan-output-quality-guards, v2.72.0; M4 reconcile for sketch naming + JSON api-contract, v2.75.0). Group (a) Manifest drift guard (6 checks): all 7 trigger→sketch pairs present across three representations — `docs/plan-sketches.md` (a1) and `agents/architect.md` (a5) use `sketches/`-prefixed form (e.g. `sketches/api-contract.md`); `hooks/sketch-guard.sh` SKETCH_MAP (a3) uses bare type filenames (e.g. `api-contract.md`, resolver composes `sketches/` prefix at runtime) — including the 8th pair `spans_multiple_services`→`sketches/service-interaction.md` (docs/arch) / bare `service-interaction.md` (guard); data-migration pair present in each representation (a2, a4, a6) in the same native form. Group (b) `sketch-guard.sh` structural properties (4 checks): (b1) fail-open early exits emit `verdict:pass`; (b2) uses python3 + grep fallback; (b3) never emits `verdict:fail`; (b4) JSON shape has `verdict/required/missing/concerns` fields. Group (c) 3 separate agent-contract checks (3 checks, AC-12 split): (c1) `agents/plan-reviewer.md` contains `Rule 11` with sketch content (anchor-scoped); (c2) `agents/qa-plan.md` references `sketches/` paths in Ratify-Plan Mode; (c3) `agents/acceptance-checker.md` references `sketches/` paths in Phase 3.6. Group (d) orchestrator integration (3 checks): (d1) `agents/orchestrator.md` workspace doc list includes `sketches/` paths; (d2) `orchestrator.md` references `sketch-guard.sh` invocation; (d3) `hooks/config.json` does NOT contain `sketch-guard` (not a PreToolUse hook). Group (e) ref-special-flows.md (1 check): documents per-type sketch applicability using `sketches/*.md` naming (fix Tier 0 exempt, etc.). Group (f) direct-entry skills + CLAUDE.md §5 (4 checks): `skills/review-pr`, `skills/deliver`, `skills/validate` each reference `sketch-guard`; CLAUDE.md §5 contains `docs/plan-sketches.md` bullet. Group (g) packaging (4 checks): `plugin.json` version `2.70.0`; `marketplace.json` `plugins[0].version` `2.70.0`; `CLAUDE.md §3` mentions `2.70.0`; `CHANGELOG.md` contains `## [2.69.0]` section (durable — NEVER assert `changelog.d/` fragment existence; recurring-bug guard). Group (h) self-referential / registry (3 checks): test file contains `Suite 82` + `plan-sketches`; `docs/testing.md` registers Suite 82 + `plan-sketches`; `CLAUDE.md` does NOT contain `Suite 82`. Group (i) Milestone-5 extensions — service-interaction + consolidated layout + anti-gaming + consumption clauses (11 checks, AC-8): (i1) `docs/plan-sketches.md §3` service-interaction row has Mermaid `sequenceDiagram` + changed-call-paths fidelity ceiling + `sketches/service-interaction.md` filename; (i2) `spans_multiple_services`→`service-interaction.md` pair present in all three representations in their respective native forms; (i3) `docs/plan-sketches.md §4` documents consolidated `sketches/` layout with project-prefixed per-project sketches and un-prefixed shared `service-interaction.md`; (i4) `hooks/sketch-guard.sh` Step 5b resolves consolidated `sketches/` path when parent `overview.md` exists; (i5) anti-gaming signal — `hooks/sketch-guard.sh` Step 3 emits `concerns_verdict` (not `pass_verdict`) when block absent + contract-surface keywords present in `01-plan.md` Files:; (i6) anti-gaming concerns branch exits 0 (fail-OPEN preserved; `verdict:fail` absent); (i7–i9) `implementer`, `tester`, `qa` each reference `sketches/` paths as required reading and carry `sketches_read` status field; (i10) `acceptance-checker.md` Phase 3.6 includes service-interaction diff row for `spans_multiple_services: true`; (i11) `reviewer.md` contains required-reading clause for triggered `sketches/*.md` files. Written FAILING-FIRST Phase 2.0 (2026-06-09); extended Milestone 5 (2026-06-09); M4 reconcile (2026-06-10). Marker: `plan-sketches`. quality-bar-api-contract additions (M2.3, v2.72.0; anchor updated M4 for JSON skeleton, v2.75.0, Marker: `quality-bar-api-contract`): 11 checks asserting the api-contract sketch quality bar (AC-7/AC-8) across four surfaces — (qb1a) `docs/plan-sketches.md §3` contains `### Sketch quality bar` heading; (qb1b) OpenAPI/REST conventions clause present; (qb1c) operation-completeness clause present (EVERY distinct operation; no multiplexing collapse); (qb1d) body-shape specificity clause present (opaque `{}` / `"...": "object"` placeholder on a changed field PROHIBITED — JSON-example terms); (qb2a) `agents/architect.md` `sketches/api-contract.md` skeleton directive references the quality bar (anchor `**\`sketches/api-contract.md\`**`); (qb2b) architect directive covers the no-opaque-object rule; (qb3a) `agents/qa-plan.md` Phase 1.5 ratify check contains the api-contract quality clause; (qb3b) qa-plan clause covers operation completeness; (qb3c) qa-plan clause covers opaque `{}` / placeholder no-opaque-object check; (qb4a) `agents/plan-reviewer.md` Rule 11 contains an api-contract completeness sub-check (step 6); (qb4b) plan-reviewer Rule 11 sub-check covers opaque object on changed field. All checks anchor-scoped (anti-false-green: missing anchor → empty slice → check fails).

### Suite 12 — security-self-scan

5 checks (one per security check). REPORT-only scanner that audits the repo's shipped assets (`agents/`, `skills/`, `hooks/`, `.claude-plugin/`) for security invariants. File: `tests/test_security_scan.py`. Wired as Suite 12 in `tests/run-all.sh`. Provides positive (red-on-regression) fixtures for all five checks (AC-9). All checks exit 0 on the v2.91.0 clean tree (AC-6).

Check 1 (FAIL) — read-only-tier agent (`READ_ONLY_AGENTS` set: `architect, security, qa, qa-plan, acceptance-checker, plan-reviewer, mentor`) carrying `Bash` in frontmatter `tools:`. The `READ_ONLY_AGENTS` set is mirrored verbatim from `test_agent_structure.py:86` (single source of truth). Only `Bash` is flagged; `Write`/`Edit` are permitted (read-only agents author workspace docs).

Check 2 (FAIL) — agent whose frontmatter `tools:` grants `WebFetch` or `WebSearch` but whose body lacks the `## Untrusted content & prompt-injection floor` heading (§6.6 prompt-injection defense). Web-facing agents are derived strictly from frontmatter `tools:` over `EXPECTED_AGENTS` only — never from a body grep — so `reviewer.md` (carries the preamble but no web grant) and prose references in `ref-*.md` are not candidates.

Check 3 (FAIL) — `hooks/*.sh` containing MVP injection anti-patterns: `eval` of an interpolated variable/command substitution; `curl`/`wget` piped to `bash`/`sh`; `rm -rf` of an unquoted shell variable. Hardened idioms (`2>/dev/null`, standalone `curl`, `base64`, local-script pipelines, quoted interpolation) are explicitly excluded.

Check 4 (WARN — exit 0) — `.claude-plugin/hooks.json` and `hooks/config.json` where a `command` is not the canonical `bash <root>/hooks/<script>.sh` form, or contains inline shell operators (`;`/`&&`/`||`/`eval`/`$()`), or a matcher is a bare `.*` on a mutating event.

Check 5 (FAIL) — high-confidence secret patterns (AWS AKIA, GitHub ghp_/gho_/ghs_/ghr_/ghu_/github_pat_, PEM private key, OpenAI sk-, Google AIza, Stripe sk_live_/rk_live_, GitLab glpat-, Slack xoxb-) in `agents/`, `skills/`, `hooks/`, `.claude-plugin/`. Detector-file allowlist (`hooks/policy-block.sh`, `tests/test_policy_block.sh`, `tests/test_security_scan.py`) is skipped wholesale. `.env.example`/`.env.sample`/`.env.template` files, dependency caches (`.venv`, `node_modules`), and binary file types are also excluded.

Positive fixtures (AC-9): each check carries a synthetic in-memory fixture proving it goes red on a regression — synthetic read-only agent with `Bash` (check 1), synthetic web-facing agent missing preamble (check 2), synthetic `curl … | bash` hook (check 3), non-canonical chained command (check 4), programmatically-constructed AWS key value (check 5). Fixture secret values are constructed at runtime via string concatenation — no concrete literal appears in the source.

Self-referential guard: `Suite 12` and `security-self-scan` present in `docs/testing.md` (canonical registry); `Suite 12` NOT in `CLAUDE.md §11` (hygiene contract). Marker: `security-self-scan`.

### Suite 101 — session-handoff

16 checks. Structural drift guard for the `/th:save-session` + `/th:resume-session` session handoff skills. File: `tests/test_agent_structure.py`. Asserts the two standalone skills exist and are correctly shaped, the shared three-field schema (`### What Worked`, `### What NOT to Retry`, `### Next Step`) is pinned on both sides, the confirmation gate and single write target are documented in `save-session`, the REPORT-only boundary in `resume-session` declares no file write / no agent dispatch / no `--apply` path, both skills carry `## Voice` and `## Output Discipline` blocks, neither names an external project or external method (TH-native framing), both are listed as Standalone in `skills/README.md`, and `docs/testing.md` registers `Suite 101` + `session-handoff`. All content checks use the anchor-scoped 3-arg `_slice_section` idiom (anti-false-green: missing anchor → empty slice → check fails). Pure text/file reads — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `session-handoff`.

### Suite 102 — kg-overlap-gate-ttl

16 checks. Structural drift guard for the KG overlap-gate (Save/Absorb/Drop verdict) on the passive-capture write path plus the TTL freshness sweep on the `/th:kg prune` path (v2.93.0 batch). File: `tests/test_agent_structure.py`. All content checks use the anchor-scoped 3-arg `_slice_section` idiom (anti-false-green: missing anchor → empty slice → check fails). Asserts the additive change across `agents/_shared/kg-write-policy.md`, `agents/delivery.md`, `skills/kg/SKILL.md`, `docs/testing.md`, and the test file itself: the `## Overlap gate (Save / Absorb / Drop verdict)` section names all three verdicts, defines Drop as skip-on-full-coverage-with-no-new-observation, maps Absorb to `add_observations`, documents the `confirmed: YYYY-MM-DD` confirmation convention, restates same-`node_type`-only isolation, and carries the best-effort failure clause; delivery Step 11.5 names the overlap-gate section and carries the Drop log outcome with an unchanged `tools:` frontmatter; the `## TTL freshness sweep` sub-section scopes a 30-day UNCONFIRMED window to `process-insight` and routes flagged entries through operator-confirmed reversible `mark_superseded` soft-delete; registry + §11 hygiene + self-ref guards. Pure text/file reads — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `kg-overlap-gate-ttl`.

### Suite 13 (run-all.sh) — harness-scorecard informational

Informational entry in `tests/run-all.sh`. Runs `python3 tests/harness_scorecard.py` (the deterministic harness health scorer) and records its exit code. Exit posture: exit 0 unless the scorer itself crashes (a crash means the scorer is broken, which IS a CI failure). Does not fail CI on a low score — that is an advisory metric. CI gets a per-commit printed score record. The hard structural gate for the scorecard contract is Suite 103 (structural). File: `tests/harness_scorecard.py`. Marker: `harness-scorecard`.

### Suite 103 — harness-scorecard

15 checks. Structural drift guard for the deterministic harness health scorecard (`/th:harness-audit`). File: `tests/test_agent_structure.py`. The headline assertion is check (4) — determinism: two independent `subprocess.run([sys.executable, scorer_path, "--json"], ...)` invocations produce byte-identical stdout, proving the same-commit-same-score property mechanically. All other checks are pure file reads. Checks: (1) `tests/harness_scorecard.py` exists on disk; (2) `skills/harness-audit/SKILL.md` exists on disk; (3) `tests/harness_scorecard_baseline.json` exists and parses as valid JSON with a `score` integer and a `categories` object; (4) two independent subprocess runs of `--json` produce byte-identical stdout (same-commit-same-score); (5) the canonical JSON `categories` map contains exactly the 12 expected keys in fixed order, no more, no fewer; (6) scorer source contains none of the determinism-hazard import tokens (`import time`, `import random`, `import datetime`, `from datetime`, `os.urandom`, `import socket`, `import urllib`, `import http`, `import requests`, `time.time`, `datetime.now`); (7) scorer write path is confined to `--write-baseline` targeting only `harness_scorecard_baseline.json` (REPORT-only assertion); (8) scorer `max_total` equals 102 (sum of all category max values per design); (9) skill declares REPORT-only, omits `--fix`/`--apply`/`auto-remediat`, says `do NOT invoke the \`orchestrator\``, carries `## Voice` and `## Output Discipline`; (10) skill names no external project or external method (TH-native framing, provenance guard); (11) `tests/run-all.sh` contains `Suite 13` and `harness_scorecard.py` (scorer wired as informational suite); (12) `docs/testing.md` registers `Suite 103` and `harness-scorecard` marker; (13) `CLAUDE.md` does NOT contain `Suite 103` (§11 hygiene contract); (14) test file self-references `Suite 103`, `_slice_section`, and `harness-scorecard`; (15) Suite 103 non-comment source contains no `Agent(` or `subagent_type` token (free-suite guarantee). The subprocess-twice determinism check is the must-fix from plan review (SC-1): it proves the same-commit-same-score property with independent process invocations, not a shared in-process state. Integer floor division `//` is used throughout all category scorers (SC-2 fix). Pure text/file reads plus two subprocess runs — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `harness-scorecard`.

### Suite 104 — skill-audit-lens

21 checks. Structural drift guard for Check 9 (skill dedup / SEARCH-BEFORE-CREATE) and Check 10 (skill quality quick-scan) added to `/th:lint` (v2.93.0), plus the SEARCH-BEFORE-CREATE note added to `agents/agent-builder.md` Phase 1. File: `tests/test_agent_structure.py`. All checks use the 3-arg anchor-scoped `_slice_section` idiom (anti-false-green: missing anchor → empty slice → check fails). `STOP = ("\n## ", "\n---\n")`.

Checks: (1) `skills/lint/SKILL.md` contains `## Check 9 — Skill overlap` section; (2) Check 9 slice contains `SEARCH-BEFORE-CREATE` and `dedup`; (3) Check 9 names the three overlap dimensions (`name`, `description`, `keyword`/`trigger`); (4) Check 9 declares `--against` search-before-create mode arg; (5) Check 9 slice contains `REPORT-only` and `never FAIL`/`advisory`; (6) Check 9 carries the expected-overlap `allowlist` naming the diagram family; (7) `skills/lint/SKILL.md` contains `## Check 10 — Skill quality quick-scan` section; (8) Check 10 slice contains all five Q1–Q5 criteria (`single responsibility`, `Voice`, `Output discipline`, `orphaned`, `routing`); (9) Check 10 declares `--changed` changed-only mode; (10) Check 10 slice declares `WARN` and `never FAIL`/`advisory`; (11) lint SKILL.md contains `--against` and `--changed` at file level; (12) lint SKILL.md contains `/ 10 checks passed` (positive) AND does NOT contain `/ 8 checks passed` (negative — prevents denominator drift); (13) lint SKILL.md contains `Check 9:` and `Check 10:` in its Output Format; (14) lint SKILL.md preserves the standalone guard (`do NOT invoke the` + `orchestrator`); (15) `agents/agent-builder.md` contains `SEARCH-BEFORE-CREATE`, `--against`, and `extend the existing skill`; (16) agent-builder SEARCH-BEFORE-CREATE slice contains `REPORT-only`/`never deletes` and `Check 9`; (17) `skills/README.md` lists `/th:lint` as Standalone; (18) provenance guard — lint SKILL.md and agent-builder.md contain no external-project/method tokens (assembled programmatically: `"EC"+"C"` and `"Agent"+"Shield"`); (19) `docs/testing.md` registers `Suite 104` + `skill-audit-lens` marker; (20) `CLAUDE.md` does NOT contain `Suite 104` (§11 hygiene contract); (21) test file self-references `Suite 104`, `_slice_section`, and `skill-audit-lens`. Both Check 9 and Check 10 are REPORT-only (never FAIL) — the denominator check (12) is a paired positive+negative assertion to prevent denominator drift. Pure text/file reads — no agent invocation, no paid spend. Written by implementer (2026-06-15). Marker: `skill-audit-lens`.

### Suite 105 — opencode-distribution-roadmap

10 checks. Thin doc-structure guard for the forward-looking opencode distribution-layer
design spec (Tier-4, design only — no installer code). File: `tests/test_agent_structure.py`.
Pure text/file reads — no agent invocation, no paid spend. Asserts: (1) `docs/opencode-distribution-roadmap.md`
exists; (2) it carries a `design only` status banner referencing the frozen `cmd/install`;
(3) it contains a buildable-now-vs-defer assessment naming all three items; (4) Item 1 defines
the adapter descriptor + shim normalization contract; (5) Item 2 defines the two-layer
manifest + plan/apply split + ownership-based uninstall; (6) Item 3 names the TH-native
`TEAM_HARNESS_DATA_HOME` env var + a resolution order; (7) provenance guard — the committed
doc carries no external-project/method token (TH-native only); (8) `docs/testing.md` registers
Suite 105 + marker; (9) `CLAUDE.md` does NOT contain `Suite 105` (§11 hygiene contract);
(10) test file self-references Suite 105 + `_slice_section` + marker. All content checks use
anchor-scoped `_slice_section` (anti-false-green: missing anchor → empty slice → check fails).
Marker: `opencode-distribution-roadmap`.

### Suite 106 — parallel-batch-implementation

12 checks. Doc-structure guard for the orchestrator's parallel-batch-implementation contract
(in-message implementer fan-out across worktrees for a single-repo additive batch, consolidated
to ONE PR). File: `tests/test_agent_structure.py`. Pure text/file reads — no agent invocation,
no paid spend. Asserts: (1) `agents/orchestrator.md` carries the `## Parallel Batch Implementation`
section; (2) the section carries its trailing marker; (3) worktree-isolation language; (4) the
item-local-vs-shared-serial edit-class split; (5) the consolidation step (wholesale checkout +
reserved-order insertion splice); (6) concurrent implementer fan-out capped by `batch_concurrency`;
(7) per-item verify is `python3 tests/test_agent_structure.py`, not concurrent `run-all.sh`;
(8) the single consolidated full-suite safety net; (9) PR #338 cited as empirical basis;
(10) `docs/parallel-batch-implementation.md` exists and carries the edit-class split +
reserved-order splice; (11) provenance guard — no external-project/method token, plain URLs only;
(12) registry + hygiene (`CLAUDE.md` lacks `Suite 106`) + self-ref + no-agent-token free-suite
guard. All content checks use anchor-scoped `_slice_section` (missing anchor → empty slice →
check fails). Marker: `parallel-batch-implementation`.

### Suite 107 — convergence-stage3

Pure text/file reads — no agent invocation, no paid spend. Asserts the SDD pipeline's
Phase 4.5 internal review wires the EXISTING dual-review convergence loop: (1) Phase 4.5
section has a convergence sub-step; (2) it points to the canonical ref-direct-modes
contract (no second copy); (3) A/B context-isolation + disjoint -A/-B draft paths;
(4) three-branch comparator; (5) max 3 rounds; (6) STOP-and-escalate never-auto-resolve;
(7) runs BEFORE STAGE-GATE-3 + never calls a write verb; (8) Tier-4 auto-on + opt-in +
OFF default trigger policy; (9) 00-state.md convergence block + review.convergence.round
event; (10) ref-direct-modes carries the Phase 4.5 cross-pointer; (11) docs/testing.md
registers Suite 107 + marker; (12) CLAUDE.md does NOT contain Suite 107 (§11 hygiene);
(13) Suite 107 non-comment source has no agent-invocation token; (14) test file
self-references Suite 107 + _slice_section + marker. All content checks use anchor-scoped
3-arg _slice_section.

Marker: `convergence-stage3`.

## When to add a test

Any new pattern in `policy-block.sh` (new denylist or allowlist case) MUST be backed by an `assert_deny` / `assert_allow` line. Any new pipeline phase, new agent contract field, or new mandatory section MUST be backed by a `check(...)` line in the appropriate suite of `test_agent_structure.py`. Any new agent file in `agents/` is picked up automatically by `test_agent_frontmatter.py` — no manual addition needed; the test fails immediately if its YAML does not parse. All three files are append-only by design — refactor an assertion only when the assertion itself is wrong.

## What the tests do NOT cover

Agent prompt behaviour (whether Claude actually applies the implementer's `Reviewability self-check` is a behavioural question), hook integration with Claude Code (whether the harness invokes `policy-block.sh` on every Bash/Write/Edit/NotebookEdit depends on `~/.claude/settings.json`), and live pipeline runs (Phase 2.5 / 4.5 only fire inside a real pipeline). For those, restart Claude Code and smoke-test by hand.
