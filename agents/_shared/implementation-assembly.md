# Implementation assembly

Group dependency-ready tasks into coherent work. Assign explicit ownership;
independent files may proceed concurrently. Serialize overlapping edits and Git
mutations. Implementers own ordinary tests; additional testers and cleaners run
when useful.

Check the actual diff and evidence before integration. Preserve unrelated edits.
Run focused checks while implementing and repository-required checks on the
completed candidate; reuse sufficient unchanged evidence.
